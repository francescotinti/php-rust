# Criterio S-128 p.2 — rimisura compoff (voce mappa RIAPERTA dopo cure ondata-2)

1. Oggetto: REGISTRAZIONE (non leva) del rapporto phpr(pin s127b ccb63dcaf565cffc)/oracle su composer install OFFLINE; le cure S-127b (§3.19-bis/-ter + FILTER_FLAG_EMAIL_UNICODE + SimpleXML + iconv) rendono il numeratore ESEGUIBILE per la prima volta.
2. Metodo = criterio mappa2 p.3+p.7 con EMENDA S-127 integrale: cifra canonica = **ratio_NET** (netto-pavimento per-binario, raw companion); gate contesa in **ictx/s** (ictx/wall della gamba, segnalata >1,5× la mediana); 2 gambe per lato, dentro ogni gamba oracle PRIMA poi phpr, workspace ri-untarrato per gamba, `COMPOSER_DISABLE_NETWORK=1`, cache calda, vendor rimosso; oracle SEMPRE `-d memory_limit=-1` (§3.14 dichiarata).
3. Strumento = composer ESTRATTO (`Phar::extractTo` con l'oracle), STESSO sui due lati; pavimento = med3 `composer-x/bin/composer --version` per binario.
4. Rebuild: il tarball congelato è oggi SENZA composer-x → riestrarre dal phar, smoke bilaterale rc=0 ESATTO (forge-silent-failure), poi RICONGELARE `compoff-work.tgz` CON composer-x PRIMA delle gambe.
5. Gamba valida se rc=0 E `vendor/autoload.php` esiste; se le gambe dello stesso lato divergono >3% E una è segnalata ⇒ gamba segnalata NULLA.
6. In CODA alla pair (attesa `wp128-harness/pair-out/pair-intercal.done`): run pesanti sequenziali; workspace `~/Claude/s128-sp` (mai /private/tmp per asset di run detached).
7. Verdetto `s128-compoff-verdetto.out`; rc autoritativo = SOLO `wp128-harness/compoff-out/compoff.done` scritto dallo script stesso (az.rev. S-127 #2); PERF_MAP.md si aggiorna SOLO da quel verdetto.
8. Questo criterio è committato PRIMA del codice dello script (az.rev. S-127 #5, commit distinti). Nessuna predizione di magnitudine (prima misura col numeratore vivo); osservazione collaterale attesa: micro calls 4,9(*) resta da osservare al gate, NON qui.
