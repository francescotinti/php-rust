# Criterio S-128 p.3 — L-OL1 seg.2: istruttoria d'ammissione (PRIMA della forma)

1. Eleggibilità (regola di nomina s127, cifre da s127-submicro-verdetto.out run pulito): **Δins = objdatains − objalloc = 320,0 ns/iter phpr vs 36,6 oracle (~8,7×)**, primo residuo del churn dopo F1 (additività chiusa 3,4 ns). Meccanismo NOMINATO da verificare (non assumere): `$e->data['k']=$i` = fetch-per-scrittura proprietà → COW `make_mut` sul default condiviso post-stampo → insert PhpArray (+KeyIndex) → drop array non vuoto.
2. Admission ORDINATA prima di ogni forma (protocollo s126-leva-nominata + emenda): **census** R=2 identici (patch census-clite wp119, target separato, tree ripristinato e verificato pulito) su TUTTE le categorie micro-orm al pin **s127b ccb63dcaf565cffc**; per-iter=(cat−empty)/3e6. Collaudo differito della predizione F1: atteso **−1,00 ESATTO** vs s127-admission su objalloc/objallocni/objdatains/objdropdef/objchurn, objmap 0,00 — scarto ⇒ indagine PRIMA di procedere. Δins_alloc = datains−alloc (rif. pre-F1: 13−9 = 4 alloc/insert).
3. **Disasm** «prima» (protocollo S-104): bl-count del sentiero insert-su-proprietà sul pin s127b (siti: write-fetch prop + make_mut + insert/KeyIndex); nomi dei bl caldi a verbale.
4. **Profilo campionato** su objdatains.php: indizio unilaterale, nessuna cifra da componenti (REGOLE §3).
5. Census/disasm/profilo SOLO a run detached concluse (pair.done E compoff.done): niente build né strumenti in finestra di misura (incidente S-127 LSP; contesa).
6. Forme candidate DA CENSIRE, non predette: riduzione alloc dell'insert (make_mut/clone del default) · KeyIndex lazy o inline per array piccoli · insert per indice. La FORMA verrà nominata in una lettura separata col SUO criterio A/B ri-committato PRIMA del codice della forma (az.rev. S-127 #5).
7. Criterio A/B della forma (vincoli già fissati qui): giudice `objdatains.php` (N=3e6 emesso dal sorgente), R=5 ABAB user CPU netto-pavimento, segno: objdatains phpr CALA; soglia max(4 ns/iter, rumore R=5, banda-layout prop); smoke R=2 early-stop; guardie SOLO-REGRESSIONE = sei micro + objalloc/objchurn/objmap; **bande objchurn/objmap: nessuna banda storica ⇒ DICHIARATO «default 4 ns» + spread del run** (az.rev. S-127 #4) e commento «(wp97)» ripulito nell'arbitro.
8. rc autoritativo di ogni script di questa istruttoria = SOLO il file rc scritto dallo script stesso, nominato nel suo verdetto (az.rev. S-127 #2). Verdetto avverso eventualmente emerso si committa PRIMA di ogni emenda che ne discende (az.rev. S-127 #3).
