2,6c2,9
< # s158-orm-coppia.sh — coppia dbal+ORM sul pin s157 = rimisura DOVUTA (pin
< # nuovo) con attesa L-AL1 SOTTO-risoluzione (criterio s158-criterio-orm.md; copia
< # DICHIARATA di s157-orm-coppia.sh, manifest s158-orm-copia.diff; adattamenti:
< # pin s157 76787303716acd4e, path wp158, blocco ATTESA-AL1 con EMENDA S-157
< # (giudizio CANONICO su net ORACLE-NORMALIZZATO, ORA_REF=4,94; assoluto e
---
> # s159-orm-coppia.sh — coppia dbal+ORM sul pin s158 = rimisura DOVUTA (pin
> # nuovo) con attesa L-RF2 SOTTO-risoluzione + REPLICA attesa-AL1 (criterio
> # s159-criterio-orm.md; copia DICHIARATA di s158-orm-coppia.sh, manifest
> # s159-orm-copia.diff; adattamenti: pin s158 92b0aea36573955a, path wp159,
> # blocco ATTESA a DOPPIO GIUDIZIO oracle-normalizzato (az.rev. S-158 #3):
> # (a) attesa-RF2 vs RIF s158 [34,90;35,25] CON NOTA CONTESA, ORA_REF2=4,995;
> # (b) replica-AL1 vs RIF s157 [34,82;34,91], ORA_REF1=4,94 — chiude SOLO con
> # gambe ictx pulite e assunzione RF2-sotto-risoluzione; cattura orm_flags;
18c21
< H="$REPO/wp158-harness"
---
> H="$REPO/wp159-harness"
25c28
< VERD="$H/s158-orm-coppia-verdetto.out"
---
> VERD="$H/s159-orm-coppia-verdetto.out"
30c33
< [ "$PINM" = "76787303716acd4e" ] || { echo "rc=9 pin!=s157" > "$OUT/rimisura.done"; exit 9; }
---
> [ "$PINM" = "92b0aea36573955a" ] || { echo "rc=9 pin!=s158" > "$OUT/rimisura.done"; exit 9; }
122c125
< print("coppia-s158 (user CPU; pavimenti PARZIALI phpunit --version: orm o=%.3f p=%.3f · dbal o=%.3f p=%.3f)" % (fo, fp, dfo, dfp))
---
> print("coppia-s159 (user CPU; pavimenti PARZIALI phpunit --version: orm o=%.3f p=%.3f · dbal o=%.3f p=%.3f)" % (fo, fp, dfo, dfp))
124a128
> orm_flags = []
140a145
>     if w == "orm": orm_flags = flag
148,152c153,159
< # --- ATTESA L-AL1 (criterio s158-criterio-orm.md p.2-5, aritmetica meccanica) ---
< # EMENDA S-157 (indagine HD2, az.rev. #4): giudizio CANONICO su net
< # ORACLE-NORMALIZZATO; assoluto e rapporto stampati come companion.
< REF_MIN, REF_MAX = 34.82, 34.91   # phpr ORM net @ s157 (NUOVO rif., nota drift oracle +0,9/1,3%)
< ORA_REF = 4.94                     # oracle net della finestra s157 (stessa del rif. assoluto)
---
> # --- ATTESA L-RF2 + REPLICA attesa-AL1 (criterio s159-criterio-orm.md p.2-4, aritmetica meccanica) ---
> # Giudizio CANONICO su net ORACLE-NORMALIZZATO (emenda S-157) EREDITATO;
> # scaletta meccanica del giudice INVARIATA (s158), applicata DUE volte.
> REF2_MIN, REF2_MAX = 34.90, 35.25  # RIF s158 CON NOTA CONTESA (estremi da gambe ictx-segnalate: phpr1=3956, oracle2=1897)
> ORA_REF2 = 4.995                   # media DICHIARATA gambe oracle finestra s158 (4,97/5,02)
> REF1_MIN, REF1_MAX = 34.82, 34.91  # RIF s157 (replica attesa-AL1, catena ereditata)
> ORA_REF1 = 4.94                    # oracle net finestra s157
154,155c161,162
< RREF_LO, RREF_HI = 7.028, 7.067   # rapporto net registrato S-157 (companion)
< ATT_LO, ATT_HI = 0.02, 0.05       # attesa L-AL1 pre-registrata SOTTO risoluzione
---
> RREF_LO, RREF_HI = 6.952, 7.093    # rapporto net registrato S-158 (companion)
> ATT_LO, ATT_HI = 0.03, 0.12        # attesa L-RF2 pre-registrata SOTTO risoluzione (~7 ns x 11,66M siti)
160,165c167,180
<     d_min = REF_MIN - nn[-1]; d_max = REF_MAX - nn[0]
<     print(f"attesa-AL1 assoluto (companion, nota drift): phpr_orm_net_new=[{nn[0]:.2f}; {nn[1]:.2f}] ref=[{REF_MIN}; {REF_MAX}] Delta=[{d_min:+.2f}; {d_max:+.2f}] s")
<     normed = sorted(pn * ORA_REF / o for pn, o in zip(new_net["orm"], on))
<     dn_min = REF_MIN - normed[-1]; dn_max = REF_MAX - normed[0]
<     print(f"attesa-AL1 ORACLE-NORMALIZZATO (CANONICO, emenda S-157): net_norm=[{normed[0]:.2f}; {normed[1]:.2f}] (ORA_REF={ORA_REF}, oracle_net_gambe={[round(x,2) for x in on]}) Delta_norm=[{dn_min:+.2f}; {dn_max:+.2f}] s")
<     print(f"rapporto net (companion): [{rr[0]:.3f}; {rr[1]:.3f}] vs registrato S-157 [{RREF_LO}; {RREF_HI}]")
---
>     d_min = REF2_MIN - nn[-1]; d_max = REF2_MAX - nn[0]
>     print(f"attesa-RF2 assoluto (companion, RIF CONTESO): phpr_orm_net_new=[{nn[0]:.2f}; {nn[1]:.2f}] ref=[{REF2_MIN}; {REF2_MAX}] Delta=[{d_min:+.2f}; {d_max:+.2f}] s")
>     def _norm(ora_ref, ref_min, ref_max):
>         normed = sorted(pn * ora_ref / o for pn, o in zip(new_net["orm"], on))
>         return normed, ref_min - normed[-1], ref_max - normed[0]
>     def _classe(dn_min, dn_max):
>         if dn_min >= RES: return "GIU"
>         if dn_max < -RES: return "REGR"
>         if dn_max < RES: return "COMP"
>         return "NR"
>     n2, dn2_min, dn2_max = _norm(ORA_REF2, REF2_MIN, REF2_MAX)
>     print(f"attesa-RF2 ORACLE-NORMALIZZATO (CANONICO coppia, RIF CONTESO dichiarato): net_norm=[{n2[0]:.2f}; {n2[1]:.2f}] (ORA_REF={ORA_REF2}, oracle_net_gambe={[round(x,2) for x in on]}) Delta_norm=[{dn2_min:+.2f}; {dn2_max:+.2f}] s")
>     print(f"rapporto net (companion): [{rr[0]:.3f}; {rr[1]:.3f}] vs registrato S-158 [{RREF_LO}; {RREF_HI}]")
>     c2 = _classe(dn2_min, dn2_max)
167,173c182,188
<         print("attesa-AL1: CIFRA NULLA (parita' rotta) — nessun giudizio")
<     elif dn_min >= RES:
<         print(f"attesa-AL1: DIREZIONE GIU' FUORI RUMORE (Delta_norm_min {dn_min:.2f} >= {RES}) — OLTRE l'attesa sotto-risoluzione 0,02-0,05: dichiarare, nessuna attribuzione senza sonda")
<     elif dn_max < -RES:
<         print(f"attesa-AL1: REGRESSIONE SEGNALATA (Delta_norm_max {dn_max:.2f} < -{RES}) — indagine PRIMA di ogni altra leva")
<     elif dn_max < RES:
<         print(f"attesa-AL1: COMPATIBILE (Delta_norm dentro il rumore ±{RES}; attesa 0,02-0,05 SOTTO risoluzione — esito dichiarato, nessuna cifra attribuita a L-AL1)")
---
>         print("attesa-RF2: CIFRA NULLA (parita' rotta) — nessun giudizio")
>     elif c2 == "GIU":
>         print(f"attesa-RF2: DIREZIONE GIU' FUORI RUMORE (Delta_norm_min {dn2_min:.2f} >= {RES}) — OLTRE l'attesa sotto-risoluzione {ATT_LO}-{ATT_HI}: dichiarare, nessuna attribuzione senza sonda")
>     elif c2 == "REGR":
>         print(f"attesa-RF2: REGRESSIONE SEGNALATA (Delta_norm_max {dn2_max:.2f} < -{RES}) — indagine PRIMA di ogni altra leva")
>     elif c2 == "COMP":
>         print(f"attesa-RF2: COMPATIBILE (Delta_norm dentro il rumore ±{RES}; attesa {ATT_LO}-{ATT_HI} SOTTO risoluzione — esito dichiarato, nessuna cifra attribuita a L-RF2)")
175c190,210
<         print(f"attesa-AL1: NON RISOLTA (intervallo Delta_norm a cavallo di {RES}) — si dichiara, replica eventuale")
---
>         print(f"attesa-RF2: NON RISOLTA (intervallo Delta_norm a cavallo di {RES}) — si dichiara (RIF CONTESO: contaminazione del riferimento possibile)")
>     # (b) replica attesa-AL1 (az.rev. S-158 #3): chiude SOLO con gambe pulite e assunzione RF2-sotto-risoluzione
>     n1, dn1_min, dn1_max = _norm(ORA_REF1, REF1_MIN, REF1_MAX)
>     print(f"replica-AL1 ORACLE-NORMALIZZATO: net_norm=[{n1[0]:.2f}; {n1[1]:.2f}] (ORA_REF={ORA_REF1}) Delta_norm=[{dn1_min:+.2f}; {dn1_max:+.2f}] s vs RIF s157")
>     if parita_rc != 0:
>         print("replica-AL1: CIFRA NULLA (parita' rotta) — attesa AL1 RESTA APERTA")
>     elif orm_flags:
>         print(f"replica-AL1: gambe ictx SEGNALATE {orm_flags} — attesa AL1 RESTA APERTA (criterio p.2b)")
>     elif c2 == "GIU":
>         print("replica-AL1: assunzione RF2-sotto-risoluzione CADUTA (attesa-RF2 fuori rumore in giu') — attesa AL1 RESTA APERTA")
>     else:
>         c1 = _classe(dn1_min, dn1_max)
>         lbl = "replica-AL1 (gambe pulite, assunzione RF2 ok — CHIUDE l'attesa):"
>         if c1 == "GIU":
>             print(f"{lbl} DIREZIONE GIU' FUORI RUMORE (Delta_norm_min {dn1_min:.2f} >= {RES}) — OLTRE l'attesa AL1 0,02-0,05: dichiarare, nessuna attribuzione senza sonda")
>         elif c1 == "REGR":
>             print(f"{lbl} REGRESSIONE SEGNALATA (Delta_norm_max {dn1_max:.2f} < -{RES}) — indagine PRIMA di ogni altra leva")
>         elif c1 == "COMP":
>             print(f"{lbl} COMPATIBILE (Delta_norm dentro ±{RES}; attesa AL1 0,02-0,05 sotto risoluzione CONFERMATA — nessuna cifra attribuita)")
>         else:
>             print(f"{lbl} NON RISOLTA anche su gambe pulite (a cavallo di {RES}) — si dichiara: segnale sotto il potere risolutivo a N=2, attesa chiusa come NON-RISOLVIBILE a questo N")
178c213
< { echo "== s158 coppia dbal+ORM (pin s157 MISURATO $PINM vs oracle 8.5.7; criterio s158-criterio-orm.md) =="
---
> { echo "== s159 coppia dbal+ORM (pin s158 MISURATO $PINM vs oracle 8.5.7; criterio s159-criterio-orm.md) =="
2,3c2,3
< # s158-lancio-orm.sh — catena p.1: attende pair158-t8.done; SOLO se rc=0
< # lancia s158-orm-coppia.sh (MAPPA_SP dedicato APFS). Un pair fallito NON
---
> # s159-lancio-orm.sh — catena p.1: attende pair159-t9.done; SOLO se rc=0
> # lancia s159-orm-coppia.sh (MAPPA_SP dedicato APFS). Un pair fallito NON
8c8
< H="/Volumes/Extreme Pro/Claude/php-rust-experiment/php-rust/wp158-harness"
---
> H="/Volumes/Extreme Pro/Claude/php-rust-experiment/php-rust/wp159-harness"
10c10
< PD="$H/pair-out/pair158-t8.done"
---
> PD="$H/pair-out/pair159-t9.done"
14c14
<   echo "$(date '+%F %T') pair t8 NON rc=0 ($(cat "$PD")) — ORM NON lanciato" >> "$LOG"
---
>   echo "$(date '+%F %T') pair t9 NON rc=0 ($(cat "$PD")) — ORM NON lanciato" >> "$LOG"
18c18
< SPD=/private/tmp/phpr-s158-orm; mkdir -p "$SPD"
---
> SPD=/private/tmp/phpr-s159-orm; mkdir -p "$SPD"
20c20
< MAPPA_SP="$SPD" exec /bin/bash "$H/s158-orm-coppia.sh"
---
> MAPPA_SP="$SPD" exec /bin/bash "$H/s159-orm-coppia.sh"
