# s159-criterio-am1.md — leva L-AM1 «array_map plumbing 0-alloc per-elemento» — scritto PRIMA di ogni edit
1. Oggetto: `ho_array_map` caso 1-array + closure ANONIMA `simple_call && n_params==1`: ammissione UNA volta per chiamata (hoist del dispatch invoke_value), per-elemento chiamata SENZA `vec![v]` via `push_closure_frame_one` (intake arità-1 = braccio WP-37 di bind_params per n=1: `argc=1; slots[0]=decay_arg(arg)` — stessa ammissione, provabilmente uguale) + `call_closure_one` specchio di call_callable (baseline+drive_to_return); OGNI altro caso (string/array-callable, multi-array, null-cb, non-simple, arità≠1, ArgPlace) resta sul cammino pieno INVARIATO per costruzione.
2. Giudice: `m-arrmap.php` (NUOVO: 10.000 chiamate × 1.000 elementi = 10M invocazioni-elemento; parità AM-OK 10000000 bilaterale GIÀ verificata; bilaterale companion ~3,9×; tick 1,0 ns = soglia/4, REGOLE §3); N dichiarato dal sorgente.
3. A/B: braccio A = GEMELLO dal tree corrente (§7-bis), identità a CONTENUTO con regioni ammesse s158 (LC_UUID 16B · banner __DATE__ mimalloc ≤32B · firma 2×32B; byte fuori regione ⇒ STOP); B = tree + edit. R=5 ABAB, floor3, mediane, rumore drop-1 (matematica s158 INVARIATA); soglia giudice = max(4, drop-1).
4. UB-alloc FALSIFICABILE = 1 alloc-sito/elemento × coeff TARATO s159 = **12,0 ± 2,5 ns/iter** (verdetto sonda); componenti non prezzate DICHIARATE per nome: dispatch invoke_value (match variante + named-check), `cb.clone()` Rc-bump per-elemento, baseline-check di call_callable.
5. **BANDA SMOKE VINCOLANTE** (az.rev. S-158 #4): smoke R=2 con early-stop a segno opposto; D_smoke ∈ **[8; 22]** ⇒ si procede al R=5; FUORI banda ⇒ **ARBITRATO DEDICATO PRIMA del R=5**: conteggi census m-arrmap sui due bracci (apparato sonda riusato; Δ alloc/elemento atteso 1 ESATTO) — conteggio ESATTO ⇒ si procede DICHIARANDO il fuori-banda; conteggio fuori ⇒ STOP e revert. D_smoke < soglia ⇒ STOP.
6. Guardie R=5 a SOLO-REGRESSIONE, comparatore e soglie EREDITATI s158: refl (L-RF2 non deve regredire) + missload + hostargs + backtrace24 + obj*6 + sei micro.
7. Semantica: output A==B su m-arrmap pena rc=2; fixture bilaterale NUOVA `fx-am.php` (closure-1 · closure arità≠1 · string-callable · [obj,m] · null-cb 1-array e multi · multi-array · variadica · param by-ref→pieno · chiavi stringa preservate · array vuoto · eccezione nel callback) oracle==candidato BYTE-ID al gate promo.
8. disasm run_loop bl-count A vs B agli atti; salita denti loc PRE-DICHIARATA: host.rs +≈25 · mod.rs +≈20 · calls.rs +≈15 (cap aggiornati SOLO in promozione, dichiarati).
9. Promozione SOLO se R=5 SOPRA SOGLIA e guardie 15/15: catena piena via pin-phpr.sh (batteria + corpus 1412 per NOME ×2 modi + fixture + micro R=5 + ORM 16 nomi + hk 0E/0F); churn dichiarato.
10. Esiti a FILE: rc autoritativo `ab-out/am1.rc`; verdetto `s159-am1-verdetto.out`; identità gemello `s159-gemelloA-identita.out`.
