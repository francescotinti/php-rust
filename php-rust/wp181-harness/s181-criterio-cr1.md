# S-181 criterio — LEVA «L-CR1 Call/Ret magri» su calls (NEXT §S-181 p.1) — PRE-registrato PRIMA di build e misura
1. QUESITO: calls 4,5× è il peggior rapporto micro (pin s180: phpr 1,99 s / oracle 0,44 s su 20M chiamate ⇒ ≈99 ns/iter vs ≈22, +77 ns per chiamata). Bytecode del driver (PHPR_DUMP_OPS, pin s180): 6 op nel chiamante (CmpJmpSC · LoadVarPushConst · Call · StoreSlot · Sweep · IncDecSlotJmp) + 3 nel callee (CheckArity · BinarySS · Ret) = 9 op/iter ≈ 11 ns/op contro il dispatch 1,75 (S-169) ⇒ il costo è nei CORPI di Call/Ret (rotta S-169). CENSUS ORM = RIUSO di S-176 (census-op-orm.txt, conteggi mai tempo): Ret 33,3M (6,11 % di 544M op) · MethodCall 14,7M · CheckArity 12,9M (2,37 %) · bigramma MethodCall→CheckArity 7,09M; media: Ret 61k, CheckArity 46k. Perimetro di TEMPO su ORM (tetto, da dichiarare): 33M ritorni × Δ ≤10 ns ≈ 0,3 s / 34 s ≈ 1 % ⇒ su ORM/WP SOLA DIREZIONE attesa; il bersaglio è il rapporto micro calls.
2. OGGETTO L-CR1 (safe Rust, ZERO unsafe, semantica IDENTICA per costruzione): (a) `Op::Call` fast path (simple_call ad arità esatta): il frame viene costruito DENTRO `frames` (push di `Frame::with_buffers` coi buffer del pool) e gli argomenti passano dalla pila del chiamante agli slot via `split_at_mut` — sparisce il passaggio del Frame (176 B) per valore attraverso `enter_callee`, il cui ramo simple_call è solo trace+push (replicato al byte; `decay_arg` non esegue codice utente ⇒ l'ordine push/pop non è osservabile); (b) arità ESATTA per costruzione (argc == n_params ≥ required) ⇒ il `CheckArity` in testa al corpo non può fallire: ingresso a ip=1 su `Call` fast e su `methodcall_fast` (stessa ammissione); (c) `Op::Ret`: la guardia magic (`guard_release`) si prende SOLO se `ext` esiste (Option, niente Vec vuoto costruito/iterato). Nessun altro testo toccato. Rischio nominato: (b) — presidio = fixture bilaterale fx-cr1 (troppo pochi argomenti ⇒ ArgumentCountError col messaggio Zend su funzione/metodo/3-arietà, surplus + func_get_args, default, by-ref, ricorsione, dtor a fine frame, closure/dyn/cuf, hint) + MUTANTE «ip=1 anche sul cammino LENTO di Call» che DEVE rompere fx-cr1 (esito ESATTO: le righe ACE divergono); se il mutante non morde, la fixture non presidia e la leva NON si misura.
3. BRACCI (copia dichiarata di s177-leva-build.sh → s181-leva-build.sh: `git archive` del commit + SOURCE_DATE_EPOCH=0 + CARGO_INCREMENTAL=0 + target separata): A = pin s180 (884399fc) · Z = ricostruzione del sorgente del pin (243682e7) nella target separata (se byte-id col pin: SL in-run NON stimabile, dichiarato; soglia = max(4, rumore)) · B = tree + L-CR1 (commit per hash). R=5 ABAB (rotazione dei 3 bracci per coppia), floors med3 per binario, mediane per colonna E appaiate, rumore drop-1. E1: banda same-binary |A−PREV| ≤4 su prop-dq (PREV 34,20, pin s180) — arith-dq e calls-dq NON hanno PREV same-toolchain (s175 = 1.96.0): la loro A di oggi diventa PREV (dichiarato). E2: calma CPU totale <150 % ×4 nel lanciatore prima di s129.
4. GIUDICE NUOVO calls-dq (wp181-harness/calls-dq.php = driver micro calls con N=60M, N letto dal sorgente): D = A−B in ns/iter; NOMINATA se ≥ max(4, rumore, SL) con ENTRAMBI gli stimatori; attesa [4; 12] ns/iter (un dispatch+handler CheckArity ≈3-4 + un passaggio di Frame per valore + call ≈3-6 + guardia Ret ≈1). GUARDIE a SOLA REGRESSIONE: prop-dq (nessun Call nel loop: attesa |D| < 1) e arith-dq (idem); regressione = D < −max(4, rumore).
5. ESITI: **rc=0 PROMOZIONE** (D nominata da entrambi gli stimatori, guardie senza regressione) ⇒ copia dichiarata di s180-promozione.sh (tag s181, candidato = commit B; HEAD + `git status --porcelain` PRIMA della build, `hk.rc` nel verdetto — az.rev. S-180 3/4) + coppia t22 (copia di s180-pair.sh) + ORM E3/E4 (istruttoria regola 4 se leg2 resta >7,05) · **rc=3 SOLA DIREZIONE** (max(rumore,SL) ≤ D < soglia, segni 5/5): L-CR1 TENUTA nel tree (keep-partial-wins), non promossa · **rc=4 KILL** (D < max(rumore, SL) o segni misti): revert al byte del commit L-CR1, meccanismo a verbale · rc=5 regressione di una guardia (leva SOSPESA) · rc=8 finestra contaminata (E1) o SL >4.
6. PARITÀ prima della misura (s181-leva-build.sh): B == oracle byte-id su fx-cr1 (nuova, marcatore FX-CR1 DONE), fx-sw1, fx-sl1/sl2/sl3 (opzioni oracle della promozione); B == pin a blocchi su fx-sw2-gc; mutante ≠ oracle su fx-cr1 (righe ACE); disasm run_loop di B agli atti (istr/bl/blr/sp_refs vs pin s180, atteso bl −1 per la chiamata `enter_callee` sparita dal fast path).
7. IGIENE: lock col TOKEN s181 · CI: tutti i job fino a HEAD 243682e7 OK (feed letto in apertura) · nessuna build sulla target canonica fuori catena · quiescenza s129 + watchdog Data/updater + sentinelle nel .out · esiti da `.done`/`.rc`/verdetto, mai log interi · Data 11G all'apertura (≥10, a filo: watchdog attivo).

**EMENDA E3 dichiarata (S-181, 14:35, PRIMA della corsa cr1c)**: cr1 e cr1b hanno dato verdetti CONCORDI (D=[+8,17;+8,50] e [+8,33;+8,50], segni 5/5, A same-binary 99,17/99,00, prop-dq A 34,00/34,27 vs PREV 34,20) ma entrambi a GUARDIA per UN campione da 30 s (su 13/12) di `GoogleUpdater --wake-all` (sveglia periodica, non un download: Data stabile 11-12G). La guardia si emenda solo rieseguendo il criterio emendato (REGOLE §3): nel lanciatore cr1c l'updater è ALLARME solo se persiste in ≥2 campioni consecutivi (≥60 s); Data <10G resta allarme secco. cr1c = misura di RECORD; cr1/cr1b restano agli atti come repliche a guardia. Se anche cr1c fosse sporca, la leva resta «nominata due volte a guardia» = SOLA DIREZIONE dichiarata, nessuna promozione.
