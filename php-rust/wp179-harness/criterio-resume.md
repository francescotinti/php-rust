# S-179 ripresa — criterio PRIMA di build e misure
1. Richiesta: costo lento private non-RO sul carico ORM completo S-178 e quota guardie compatibili; nessun fill implementato/pin promosso. Autorizzati e terminati i soli quattro yes, identità verificata.
2. E2 CPU totale <150% ×4 a 30s prima di ogni corsa temporale (fino a 10 tentativi registrati, 60s tra tentativi falliti), Data≥10GiB, lock s179-cost-resume-cb7f, watchdog 1800s, nessun build/census concorrente; sentinelle ogni 5s con CPU concorrente separata dal gruppo del workload (≥150% invalida).
3. Sorgente a1eb40e8 archiviato, Rust +stable verificato 1.98.1, release fat LTO/1CGU, SOURCE_DATE_EPOCH=0 CARGO_INCREMENTAL=0; nuova target APFS, binario controllo non strumentato e sonda dalla stessa ricetta.
4. Count separato: tutti i miss prop_set_entry, categoria private×RO, ricevente/file; bit scope==classe, slot, hook-negative, no __set strutturale, stato lazy/enum/presenza/ref compatibile. Coercizione typed conservata, non eliminata; quota candidata non prova completa di correttezza o hit evitabili.
5. Timer Instant monotono: campione Bernoulli pseudo-casuale seed esplicito sui miss 1/256,1/1024; da dopo IC miss al Drop della sonda prima del rilascio finale del ricevente (include resolve/coerce/write/gc_note, esclude forwarding lazy precedente e hook PHP differito). Metadata e aggregazione fuori regione; nessuna Weak. Classificazione due bit interna, guardie aggiuntive SOLO count. Timer vuoto prima del run: mediana/p95, sottrazione floor per campione con clamp 0, raw conservato.
6. Stima Horvitz-Thompson sum(ns-floor)/p dal workload stesso, non count×microcosto. Quota su wall elapsed della stessa corsa, non su CPU; report anche user CPU esterno. Classi PDOStatement/UnitOfWork/PersistentCollection e priv non-RO totali, famiglie e file separati. Incertezza tra repliche, niente costo dei soli ammissibili se non attribuito temporalmente.
7. Validazione prima suite: fixture oracle=pin=sonda off/count/time stdout/stderr/rc, attesi positivi/negativi scope/RO/magic/slot; conteggi noti. Workload senza AppleDouble; cache risultati S-178 pulita, hash registrato e ripristinata prima di ogni run. Parità ORM ogni run: 3484/11989, 3E13F59S2I, stessi baseline16 per nome; non full-output parity.
8. Controllo nullo stessa sonda off e controllo puro interleaved R=3; sonda off/time256/off/time1024 ×3 con seed distinti, warmup escluso; confronto mediane user CPU contro off e off contro puro: perturbazione >5% ⇒ costo non validato. Deriva off first/last >5% ⇒ nessun verdetto; E1 4ns/iter resta per futuri micro/A-B, non si applica senza giudice N.
9. Stabilità costo priv non-RO aggregato: mediane stimate tra densità entro 5%, altrimenti nessuna cifra costo validata; classi con <200 campioni complessivi dichiarate sottocampionate. R=5 ABAB/floor per binario/soglia max(4ns/iter,rumore,layout) necessari SOLO per futuro A/B di speedup, non eseguito qui.
10. Arresto per guardia fallita: conservare raw, completare analisi count se possibile, conclusione condizionata e revisore adversariale misura ≤400 parole. Report e sessione distinguono blocco precedente/ripresa.
