# S-173 criterio — AZ.REV. della revisione S-172 (wp172-harness/revisione.md, lente PROCESSO); PRE-registrato PRIMA di ogni run (NEXT §S-173 p.2)
1. **(a) Mutante abortivo P1/P2** (`s173-mutante-sl2.sh`, copia dichiarata di `wp171-harness/s172-mutante-sl1.sh`): MP1 = +1 sul risultato del probe sigillato del bigramma fuso (run.rs 4762), MP2 = +1 sul fast path di BinarySTDst (run.rs 2102); giudice = fx-sl2 (120 righe, forme P1 in loop ≥2 = IC calda) confrontata per BLOCCO-etichetta col pin s172. Attese ROTTE/INTATTE/a-verdetto pre-registrate nella testa del copione (lette dalle guardie run.rs 4751-4802, 2099-2116): rc=0 = fast path preso su tutte le attese e mai fuori dominio; rc=1 = una attesa non rotta (presidio vuoto: fixture da riscrivere); rc=5 = presa FUORI dominio (BUG semantico, leva a verdetto); un `p1-readonly-inscope#1` ROTTO = scrittura in place su readonly = rc=5. Build in target sul disco ESTERNO (Data ~10G), sorgente da `git archive HEAD`, repo intatto al byte (hash run.rs).
2. **(b) A/B alternato B↔C** (`s173-ab-bc.sh`, copia dichiarata di `s172-ab-leva.sh`): bracci dallo STASH (B=phpr-s172-sl2-B 260fbc3b = P1, C=phpr-s172-sl2-C e396498b = P1+P2; hash MISURATI), via symlink neutri `/private/tmp/s173-bins/{B,C}`, ordine BC/CB per coppia, R=5, giudice prop-dq (N dal driver), floor per-binario, rumore drop-1. **D = B−C**; **nominata** D ≥ max(4, rumore, 0,94) ⇒ CIFRA a P2 = D (prima cifra: S-172 aveva solo la direzione +4,67 non alternata); **attesa D ∈ [2; 8]**; 0,94 ≤ D < soglia ⇒ solo direzione (rc=4); **KILL-2**: |D| sotto il rumore ⇒ P2 non attribuibile (rc=6: la fetta 3 «peephole PropGetSlot+BinarySTDst» perde il suo pavimento e va ri-fondata sul census); D < −max(4, rumore) ⇒ regressione, P2 a verdetto (rc=5). Guardia arith-dq a SOLA regressione, dichiarata CIECA a P2 (BinarySCSCDst ≠ BinarySTDst): la guardia vera di P2 sono le micro calls/str/arr/re del pin, già R=5 in promozione S-172. Nessuna promozione: il verdetto è di attribuzione.
3. **(c) PIN_REGISTRY dei bracci**: `pin-phpr.sh --braccio` prende il commit sorgente come 3° argomento OBBLIGATORIO (verificato con `git cat-file -e`) e lo scrive a registro accanto a HEAD-allo-stash; le righe s172-sl2-B/C corrette a mano (57b21f4b→c419f29a, 18fe36c9→59ca87fb) con nota «corretto S-173».
4. **(d) forme scoperte di fx-sl2** (lazy, enum, `$this` privato, `?float`, promozione costruttore per §3.30): SOLO dopo (a) e col suo esito in mano — se una forma a verdetto risulta PRESA dal probe (typed/dynamic), la fixture si estende su quella forma e il mutante si rilancia con `SOLO=1` sui binari conservati (nessuna build).
5. ORDINE e IGIENE: mutante (build 2×, detached) → A/B B↔C SOLO a mutante finito (quiescenza s129, nessuna build durante la misura) → (c) commit → (d) → poi fetta 3 (criterio proprio, separato). Lock `s173` VIVO (creato 01:52 in pre-flight). Esiti da file; output di run mai nel repo.
