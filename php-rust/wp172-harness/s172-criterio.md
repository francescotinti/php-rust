# S-172 criterio — LEVA L-SL2 «forma sigillata Long» fetta 2 = categoria PROP (generalizzazione a forme, NEXT §S-172 p.3); PRE-registrato PRIMA di build/misura
1. CENSUS DELLE FORME (letto dal sorgente, dump `PHPR_DUMP_OPS=1` di micro/prop.php): il loop è 8 op — CmpJmpSC · PropGetSlotRecv+BinaryTCPropSetPop (bigramma FUSO L-A S-114, run.rs 4654-4740) · Sweep · PropGetSlot · BinarySTDst · Sweep · IncDecSlotJmp. Forma del bigramma fuso sul hit: `yv = deref_clone` (Zval temp) + `cv = to_zval` (Zval temp) + `binary_fast → Option<Zval>` (funnel) + `write_property_at` (borrow ×2, `replace_slot`, `old` Zval) + `gc_note(old)`; forma di PropGetSlot+BinarySTDst: push di un Zval in pila + pop + `read_slot` (clone) + funnel `binary_fast`/`binary_value_ab` + `reg_store_slot` (typed_refs, store_slot, gc_note). = la STESSA forma «Zval temporaneo + funnel Option<Zval> + clone/drop» di BinarySCSCDst pre-S-171 (lezione S-170: pesa la FORMA, non le righe). Le categorie calls (Call/BinarySS/Ret) e str (ConcatNConst/CallBuiltin substr) hanno forme DIVERSE (frame e stringhe): fette successive, NON in questa sessione.
2. OGGETTO (leva L-SL2, safe Rust, ZERO `unsafe`, semantica PRESERVATA per costruzione; corpo esatto fuori linea `#[cold] #[inline(never)]`): **(a) P1** nel bigramma fuso, DOPO le stesse guardie get-side (IC get hit, class_id, lazy) e con le stesse guardie set-side VERBATIM (IC set hit, class_id, lazy, enum, slot presente; Ref ⇒ typed_refs vuoto): se il prop letto è `Zval::Long`, il const è `Const::Int` e `long_arith_i64` (S-171, arm Long di binary_fast) risponde `Some(r)`: scrittura IN PLACE su `props.get_slot_mut(sslot)` quando quel slot è `Zval::Long` (≡ `replace_slot` + `gc_note` di un Long = no-op), altrimenti `write_property_at(Zval::Long(r))` + gc_note come oggi; nessun Zval temp (yv/cv/value/old) sul hit; ogni `None`/miss ⇒ sentiero fuso ORIGINALE, che ricomputa da zero (il probe non ha effetti). **(b) P2** `Op::BinarySTDst`: se `stack.last()` è Long e `slots[l]` è Long e `long_arith_i64(op)` risponde `Some(r)`: pop del Long, scrittura in place su dst Long altrimenti `reg_store_slot(Zval::Long(r))`; miss ⇒ corpo ORIGINALE in `binary_st_dst_slow` `#[cold]`. `scn!` resta in testa all'arm; `dcn!` restano nel corpo esatto (fast path: nessun Zval materializzato ⇒ nessun `dcn!`, nota census S-172 p.2d dell'az.rev.). NESSUN peephole nuovo (PropGetSlot+BinarySTDst restano due op: il push/pop del Long resta = fetta 3 a verdetto).
3. GIUDICE (bersaglio): **prop-dq** = `wp172-harness/prop-dq.php` = copia di micro/prop.php con N=150000000 (×5 DICHIARATO: tick 0,01 s/150M = 0,067 ns/iter ≤ soglia/4), N LETTO DAL DRIVER. Bracci nello STESSO run: **A = pin s171** (b360b2933eddfe18, hash MISURATO), **B = P1** (commit dichiarato, ricetta pin, target dedicato /private/tmp/s172-leva-tgt), **C = P1+P2** (commit successivo, stessa ricetta e target). R=5 ABAB su A/B; C misurato una volta per coppia dopo A e B; oracle per coppia (lettura). ns/iter=(med raw−floor)/N, floor PER-binario su empty.php (`[ -e ]`), rumore drop-1; **nominata** D≥max(4, rumore, 0,94); direzione firmata |D|≥max(rumore, 0,94). Parità A/B/C vs ATTESO oracle (rc=2 se ≠). Copione `s172-ab-leva.sh` = COPIA DICHIARATA di `wp171-harness/s171-ab-leva.sh` (manifest `s172-ab-leva-copia-v3.diff`, copia-gate per TOKEN s172); adattamenti: giudice prop-dq al posto di dq, arith-dq da giudice a GUARDIA a sola regressione (i bracci non toccano il loop dq: BinarySCSCDst intatto), C = stesso albero (contrasto C−B = DIREZIONE+BANDA dichiarata, mai magnitudine composta).
4. SOGLIE (NESSUNA cifra composta): decomposizione dichiarata di prop 72,7 ns/iter (2,18 s/30M, pin s171) = dispatch 8×1,75=14 + CmpJmpSC/IncDec ≈7 + Sweep×2 ≈5,8 + **corpi prop ≈46**; attesa **D_B (P1) ∈ [8; 20]**, **D_C (P1+P2) ∈ [12; 30]** (le guardie IC/borrow restano: la forma sigillata toglie i temporanei, non le guardie); **KILL-1**: D_C < 6 ⇒ la forma sigillata NON paga su prop: il peso è in guardie/borrow ⇒ non si generalizza a calls/str per analogia, la prossima è xctrace/mock sulle guardie (S-173). **Guardia arith-dq**: regressione (D < −max(4, rumore)) ⇒ rc=5, leva SOSPESA. **Promozione** = braccio con D maggiore purché NOMINATO e senza regressione (C atteso; se C−B < −rumore si promuove B e P2 cade a verdetto); una D nominata su B con C non nominata = P2 caduto, B promosso (feedback utente: il guadagno misurato non si butta).
5. GUARDIE non-bersaglio a SOLA REGRESSIONE alla promozione: micro R=5 (arith/calls/str/arr/re ≤ pin + tick; prop = scoreboard); disasm `bl` run_loop pin vs candidato (attesa: Δ NEGATIVO o piccolo = corpi usciti in `#[cold]`, emenda S-171; un Δ ≥ +20 = inliner flippato ⇒ si dichiara); dente `wc -l run.rs` (cap 7091: se morde, alzato DICHIARANDO, REGOLE §5).
6. GATE PIENI (REGOLE §5-6, pin nuovo s172): copione `s172-promozione.sh` = COPIA DICHIARATA di `s171-promozione.sh` (manifest + copia-gate) con **fx-sl2 NUOVA bilaterale** (oracle==pin): `$o->x = $o->y OP C` con OP ∈ {+,−,*,&,|,^,<<,>>,/,%,**} su prop Long, overflow (prop non tipizzata → float; `int` tipizzata → TypeError/coerc.), prop Ref-ata (`$r=&$o->x`), prop Double/string numerica/null/bool, prop dinamica, prop assente (warning/__get), readonly (2ª scrittura), hook `set`, `float` tipizzata con risultato Long, `$s += $o->x` con $s Double/string/Ref/typed-ref (prop `int` by-ref), dst==l, overflow di `$s += big`, `$s .= $o->x` (Concat → miss), `$s += $a[0]`; **fx-sl2-div** (pin==stash s171) per le forme con diag pre-esistenti se ne emergono; fx-sl1 (estesa S-172) e le 14 fixture S-171 restano nel gate.
7. COPPIA WP+ORM DOVUTA se il pin cambia: `s172-lancio-pair.sh`/`s172-pair.sh` t18 e `s172-lancio-orm.sh`/`s172-orm-coppia.sh` = copie dichiarate di S-171 con pin s172; attesa WP direzione ≤0 (quota prop-Long in WP non censita: SOTTO-risoluzione), ORM direzione ≤0.
8. IGIENE: lock `s172` VIVO (creato 23:04 in pre-flight); quiescenza s129 prima di ogni A/B con bracci via symlink NEUTRI `/private/tmp/s172-bins/{A,B,C}` (veto S-171: mai `phpr` nell'argv delle attese); esiti da file (`.out`/`.rc`/`.done`); output di run MAI nel repo; target dedicati rimossi in epilogo (binari nello stash via `--braccio`); nessun xctrace (Data < 20G).
9. ORDINE: az.rev. S-171 (mutante abortivo `s172-mutante-sl1.sh`, fixture estesa, dump, nota census) → criterio (questo, commit) → codice P1 (commit) → codice P2 (commit) → build B e C (`s172-leva-build.sh`, sequenziali, detached) → parità + dump loop (prop: 8 op invariati) → `--braccio` → disasm → A/B R=5 → sintesi `s172-verdetto.out` → SOLO SE promozione ammessa: fx-sl2 + `s172-promozione.sh` (detached) → coppia (detached, in catena) → revisione (lente PROCESSO, rotazione: S-171 semantica) → rotazione.
