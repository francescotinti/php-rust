--- s172-lancio-pair.sh	2026-09-08 00:20:52
+++ s173-lancio-pair.sh	2026-09-10 02:27:08
@@ -1,16 +1,16 @@
 #!/bin/bash
-# s172-lancio-pair.sh — catena p.1: attende la quiete CI (cargo/rustc/
+# s173-lancio-pair.sh — catena p.1: attende la quiete CI (cargo/rustc/
 # phpt-runner: job CI in volo al lancio; il lock S-171 tiene
 # ferma la coda), POI esige quiete CONTINUA anti-flare (criterio p.6,
 # az.rev. S-163 #5: 6 campioni consecutivi x30s con mediaanalysisd <5%,
 # predicato del riaggancio-r5b s163 ora PRE-registrato), DICHIARA la
-# finestra quieta (uptime + top nel log) e lancia s172-pair.sh t18.
-# Adattamento DICHIARATO di s166-lancio-pair2.sh: AGGIUNTO il blocco
-# anti-flare tra attesa CI e dichiarazione; il resto INVARIATO.
+# finestra quieta (uptime + top nel log) e lancia s173-pair.sh t19.
+# COPIA DICHIARATA di s172-lancio-pair.sh (manifest s173-lancio-pair-copia.diff):
+# soli nomi s173/t19; il resto INVARIATO (blocco anti-flare EREDITATO).
 set -u
 export PATH=/usr/bin:/bin:/usr/sbin:/opt/homebrew/bin
 H="/Volumes/Extreme Pro/Claude/php-rust-experiment/php-rust/wp172-harness"
-LOG="$H/pair-out/lancio-t18.log"; mkdir -p "$H/pair-out"
+LOG="$H/pair-out/lancio-t19.log"; mkdir -p "$H/pair-out"
 echo "$(date '+%F %T') attesa quiete CI" >> "$LOG"
 while pgrep -qx cargo || pgrep -qx rustc || pgrep -qf phpt-runner; do sleep 60; done
 sleep 60
@@ -28,10 +28,10 @@
   [ "$calm" -lt 6 ] && sleep 30
 done
 # dichiarazione finestra QUIETA (criterio p.6c): uptime + top
-Q="$H/pair-out/quiet-decl-t18.txt"
-{ echo "== dichiarazione finestra quieta t18 $(date '+%F %T') =="
+Q="$H/pair-out/quiet-decl-t19.txt"
+{ echo "== dichiarazione finestra quieta t19 $(date '+%F %T') =="
   uptime
   top -l 1 -n 8 -o cpu -stats pid,cpu,command 2>/dev/null | tail -12
 } > "$Q" 2>&1
-echo "$(date '+%F %T') quiete raggiunta — dichiarazione in quiet-decl-t18.txt — lancio pair t18" >> "$LOG"
-exec /bin/bash "$H/s172-pair.sh" t18
+echo "$(date '+%F %T') quiete raggiunta — dichiarazione in quiet-decl-t19.txt — lancio pair t19" >> "$LOG"
+exec /bin/bash "$H/s173-pair.sh" t19
