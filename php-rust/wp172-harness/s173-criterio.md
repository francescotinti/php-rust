# S-173 criterio — LEVA L-SL2 fetta 3 = PROP, residuo del corpo (NEXT §S-173 p.3); PRE-registrato PRIMA di build/misura
1. CENSUS DELLE FORME (dump `PHPR_DUMP_OPS=1` del pin s172 su micro/prop.php, S-173, 8 op/iter INVARIATE da S-172: 0012 CmpJmpSC · 0013 PropGetSlotRecv{recv:0,slot:0} + 0014 BinaryTCPropSetPop (bigramma fuso, probe sigillato P1) · 0015 Sweep · 0016 PropGetSlot{slot:0} · 0017 BinarySTDst{l:1,dst:1} (fast P2) · 0018 Sweep · 0019 IncDecSlotJmp). Residuo C vs oracle 38,9 ns/iter (52,87−14,00). Forme del residuo LETTE dal sorgente: (i) PropGetSlot hit = scope_key + `ic.get` + `borrow` + `get_slot` + `deref_clone` + **push in pila** e BinarySTDst = dispatch + `stack.last` + **pop** ⇒ un dispatch (1,75) e un round-trip di pila per un Long; (ii) P1 con **recv == slot** (stesso oggetto: `$o->x = $o->y OP C`) fa `o.borrow()` (lettura) + `ro.borrow()` (guardie set) + `ro.borrow_mut()` (scrittura) = 3 accessi RefCell e 2 controlli class_id sullo STESSO oggetto.
2. OGGETTO (safe Rust, ZERO `unsafe`, semantica PRESERVATA per costruzione, guardie VERBATIM dai sentieri esistenti, ogni miss ⇒ sentiero storico che ricomputa da zero): **P3** = peephole runtime in `Op::PropGetSlot`: se `func.ops[ip+1]` è `BinarySTDst{op,l,dst}`, IC get hit (scope_key, class_id, lazy), prop Long, `slots[l]` Long e `long_arith_i64` risponde `Some(r)` ⇒ store in place su `slots[dst]` Long (altrimenti `reg_store_slot(Long(r))` = P2 verbatim), `ip+2`, nessun push/pop; sotto lo stesso `#[cfg(not(census))]` del bigramma L-A. **P4** = nel probe sigillato P1, quando `recv == slot`: UN solo `borrow_mut` copre guardie get (class_id, lazy) + set (class_id, enum, slot Long) + lettura di y + scrittura in place di r; qualsiasi miss ⇒ sentiero sigillato/fuso esistente. Corpi non aggiunti: niente `#[cold]` nuovo (P3 riusa `reg_store_slot`; il tail di miss è il braccio storico).
3. GIUDICE (bersaglio): **prop-dq** (`wp172-harness/prop-dq.php`, N=150M LETTO dal driver, tick 0,067 ≤ soglia/4). Bracci nello STESSO run: **A = pin s172** (stash phpr-s172, 5f2dff7d17ebed79, hash MISURATO), **B = P3** (commit dichiarato, ricetta pin, target dedicato `/private/tmp/s173-leva-tgt`, sorgente `git archive` + touch), **C = P3+P4** (commit successivo). **R=5 con ordine dei TRE bracci RUOTATO per coppia** (ABC, CBA, BCA, ACB, CAB — az.rev. S-172 #2: C mai più in posizione fissa) su giudice e guardia; oracle per coppia. ns/iter=(med raw−floor)/N, floor per-binario su empty.php, rumore drop-1; **nominata** D ≥ max(4, rumore, 0,94); direzione firmata |D| ≥ max(rumore, 0,94). Parità A/B/C vs ATTESO oracle (rc=2). Copione `s173-ab-leva.sh` = COPIA DICHIARATA di `s172-ab-leva.sh` (manifest) coi soli adattamenti: rotazione dei tre bracci, bande p.4, contrasto C−B **alternato** (cifra ammessa se nominata).
4. SOGLIE (nessuna cifra composta): decomposizione dichiarata del residuo 38,9 = dispatch 8×1,75=14 + Sweep×2 ≈5,8 + CmpJmpSC/IncDec ≈7 + **corpi prop ≈12** (P1 sigillato: 2 IC get + 3 RefCell + 2 class_id; P2: pila). Attesa **D_B (P3) ∈ [2; 8]** (un dispatch + round-trip di pila), **D_C (P3+P4) ∈ [2,5; 12]**, contrasto C−B (P4) ∈ [0,5; 4] se firmato. **KILL-3**: D_C < 2 ⇒ né la pila né i borrow sono il residuo: la classe «guardie/borrow» è MORTA su prop, il residuo va cercato con xctrace #2 (Data ≥20G) o mock magri sulle guardie IC (`ic.get` = Cell read + scope_key), niente fetta 4 per analogia. **Guardia arith-dq** a SOLA regressione (BinarySCSCDst intatto: guardia CIECA a P3/P4, dichiarata) ⇒ regressione = rc=5, leva SOSPESA. **Promozione** = braccio con D maggiore purché NOMINATO e senza regressione (C atteso; C−B < −rumore ⇒ B promosso, P4 cade a verdetto; B nominata e C no ⇒ B promosso).
5. GUARDIE non-bersaglio a SOLA REGRESSIONE alla promozione: micro R=5 (arith/calls/str/**arr**/re ≤ pin + tick; prop = scoreboard) — arr è la guardia vera di P3 (`$s += $a[k]` = FetchDim + BinarySTDst, NON coperta dal peephole: deve restare 3,0); disasm `bl` run_loop pin vs candidato (attesa Δ ≤ +10: P3/P4 aggiungono solo confronti inline; Δ ≥ +20 = inliner flippato ⇒ si dichiara); dente `wc -l run.rs` (cap 7200: se morde, alzato DICHIARANDO).
6. GATE PIENI (REGOLE §5-6, pin nuovo s173): copione `s173-promozione.sh` = COPIA DICHIARATA di `s172-promozione.sh` con **fx-sl3 NUOVA bilaterale** (oracle==pin): forme P3 `$s OP= $o->x` con $s Long/Double/string/null/Ref/typed-ref, $o->x Long/Double/Ref/assente/__get/hook get/lazy/enum, dst==slot (`$o += $o->x`), overflow, shift, Div/Mod/Pow/Concat, IC fredda→calda (loop ≥2), PropGetSlot NON seguito da BinarySTDst (controllo negativo); forme P4 `$o->x = $o->y OP C` su stesso oggetto con x readonly/typed/Ref/assente, y Ref/Double, enum, lazy, `$this` privato; fx-sl1/fx-sl2 e le 17 fixture restano nel gate; mutante abortivo P3/P4 (copia di `s173-mutante-sl2.sh`) PRIMA della promozione (lezione S-172: il presidio si prova col mutante).
7. COPPIA WP+ORM DOVUTA se il pin cambia (copie dichiarate S-172 con pin s173): attesa WP direzione ≤0, ORM direzione ≤0; ORM: se net resta > 7,05 scatta la regola 4 (NEXT p.1).
8. IGIENE: lock `s173` VIVO; quiescenza s129 prima di ogni A/B, bracci via symlink neutri `/private/tmp/s173-bins/{A,B,C}`; NESSUNA build durante misure (mutante e A/B B↔C finiti PRIMA delle build dei bracci); esiti da file; output di run mai nel repo; target dedicato rimosso in epilogo; nessun xctrace (Data < 20G).
9. ORDINE: az.rev. S-172 (a)(b)(c) → questo criterio (commit) → codice P3 (commit) → codice P4 (commit) → build B e C (`s173-leva-build.sh`, sequenziali, detached) → parità + dump loop (8 op invariati: il peephole è runtime) → `--braccio` (col commit sorgente) → disasm → A/B R=5 ruotato → sintesi `s173-verdetto.out` → SOLO SE ammessa: fx-sl3 + mutante P3/P4 + `s173-promozione.sh` (detached) → coppia (detached, in catena) → revisione (lente MISURA, rotazione: S-172 processo) → rotazione.
