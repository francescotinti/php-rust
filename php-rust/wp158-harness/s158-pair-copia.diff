--- wp157-harness/s157-pair.sh	2026-08-23 23:27:40
+++ wp158-harness/s158-pair.sh	2026-08-25 01:35:17
@@ -1,31 +1,33 @@
 #!/bin/bash
-# s157-pair.sh <tentativo> — S-157 p.1: coppia full/media WP @ pin s156 (DOVUTA: pin nuovo)
-# criteri s132/s146/s148 EREDITATI INVARIATI + s157-criterio-pair-t7.md
-# (GIUDIZIO CANONICO a MEDIANA per finestra vs t1=1,799 t2=1,738 t3=1,789 t4=1,781 t5=1,757 t6=1,771 —
+# s158-pair.sh <tentativo> — S-158 p.1: coppia full/media WP @ pin s157 (DOVUTA: pin nuovo)
+# criteri s132/s146/s148 EREDITATI INVARIATI + s158-criterio-pair-t8.md
+# (GIUDIZIO CANONICO a MEDIANA per finestra vs t1=1,799 t2=1,738 t3=1,789 t4=1,781 t5=1,757 t6=1,771 t7=1,795 —
 # az.rev.3 S-149; banda-unione degradata a companion descrittivo).
-# COPIA DICHIARATA di wp155-harness/s155-pair.sh (collaudo: copia-gate +
-# manifest s157-pair-copia.diff) coi SOLI adattamenti: nomi s157/pair157/
-# wp157-harness; pin s156 (phpr 42efea3e + server ef89630f); header del
-# giudice; mediane storiche con t6=1,771 (banda giudizio INVARIATA [1,738;1,799]);
-# attesa di leva nel messaggio GIU': HD2-hostcall (non CE1), dichiarata piccola/nulla.
+# COPIA DICHIARATA di wp157-harness/s157-pair.sh (collaudo: copia-gate +
+# manifest s158-pair-copia.diff) coi SOLI adattamenti: nomi s158/pair158/
+# wp158-harness/t8; pin s157 (phpr 76787303 + server bdd32a98); mediane
+# storiche con t7=1,795 (banda giudizio INVARIATA [1,738;1,799]); attesa di
+# leva nel messaggio GIU': L-AL1 (non HD2-hostcall), dichiarata piccola/nulla;
+# EMENDE az.rev. S-157 #3-#4: header del verdetto coi pin dall'HASH MISURATO
+# + sentinella language-server registrata nel verdetto (inizio e fine).
 # Le emende S-136 (assestamento a STREAK + retry gate x3) EREDITATE INVARIATE.
 # Tutto il resto INVARIATO: warm-up media-only MAI giudicata, quiescenza gate
 # SEPARATO per gamba, gate ictx/s a mediana PER MOTORE (pair109 INVARIATA).
-# rc autoritativo = SOLO pair-out/pair157-<tentativo>.done scritto QUI.
+# rc autoritativo = SOLO pair-out/pair158-<tentativo>.done scritto QUI.
 set -u
 export PATH=/usr/bin:/bin:/usr/sbin:/opt/homebrew/bin
 REPO="/Volumes/Extreme Pro/Claude/php-rust-experiment/php-rust"
-H="$REPO/wp157-harness"
+H="$REPO/wp158-harness"
 H109="$REPO/wp109-harness"
 QUIESCE="$REPO/wp129-harness/s129-quiescenza.sh"
 T="${1:?tentativo (es. t2)}"
 OUT="$H/pair-out"; mkdir -p "$OUT"
-VERD="$H/s157-pair-verdetto-$T.out"
+VERD="$H/s158-pair-verdetto-$T.out"
 if [ -e "$VERD" ]; then
   echo "verdetto $VERD ESISTE — tentativo nuovo = file nuovo (az.rev. S-131 #5)" >&2
   exit 7
 fi
-DONE="$OUT/pair157-$T.done"
+DONE="$OUT/pair158-$T.done"
 WPDEV="/Users/francescotinti/Claude/wpdev"
 ORACLE=/opt/homebrew/opt/php/bin/php
 PHPR="$HOME/Claude/php-rust-output/release/phpr"
@@ -34,8 +36,8 @@
 : > "$OUT/progress-$T.txt"
 rm -f "$DONE"
 
-PIN_ATTESO="42efea3e34feb390"
-SRV_ATTESO="ef89630f9c7408c3"
+PIN_ATTESO="76787303716acd4e"
+SRV_ATTESO="bdd32a989ae1f492"
 PIN="$(shasum -a 256 "$PHPR" | cut -c1-16)"
 SRV="$(shasum -a 256 "$HOME/Claude/php-rust-output/release/php-server" | cut -c1-16)"
 if [ "$PIN" != "$PIN_ATTESO" ] || [ "$SRV" != "$SRV_ATTESO" ]; then
@@ -43,6 +45,12 @@
   echo "rc=9 pin-mismatch" > "$DONE"; exit 9
 fi
 
+# sentinella language-server (az.rev. S-157 #4): registrata nel verdetto,
+# inizio e fine finestra; presenza != esclusione (misure a user-CPU), si dichiara.
+ls_sentinel(){ pgrep -fl 'language[_-]server|Antigravity|rust-analyzer|intelephense|serena|gopls|pylsp|solargraph' 2>/dev/null | grep -v pgrep | awk '{print $2}' | sort -u | tr '\n' ' '; }
+LS_START="$(ls_sentinel)"
+step "sentinella LS inizio: ${LS_START:-nessuno}"
+
 quiesce_gate(){ # $1=etichetta — gate SEPARATO, rc nel suo file (mai nel comando del lancio)
   # EMENDA DICHIARATA (t3, dopo rc=8 t1 E t2 allo STESSO confine leg2-off):
   # attesa di ASSESTAMENTO per mediaanalysisd (digerisce il churn immagini
@@ -130,9 +138,10 @@
 # ---- giudice meccanico dai .time: header con rc quiescenza, ictx/s mediana PER
 # MOTORE, az.rev. #3: riferimento PER CONFIGURAZIONE (ON-ONLY canonico) + firma
 # per gamba (ictx% della mediana del motore, rank CPU oracle) + DERIVA (s148 p.4).
-python3 - "$OUT" "$T" > "$VERD" <<'PY'
+python3 - "$OUT" "$T" "$PIN" "$SRV" > "$VERD" <<'PY'
 import re, sys, statistics, os
 out, T = sys.argv[1], sys.argv[2]
+PINH, SRVH = sys.argv[3], sys.argv[4]  # az.rev. S-157 #3: identita' dall'HASH MISURATO
 def t(f):
     d = {}
     for l in open(f, errors="replace"):
@@ -143,8 +152,8 @@
         m = re.search(r"^\s*(\d+)\s+peak memory footprint", l); d.update(pf=int(m.group(1))) if m else None
     return d
 legs = ["leg1-on", "leg2-on", "leg3-on", "leg4-on", "leg5-on", "leg6-on"]
-print(f"== s157 coppia WP full+media sul pin s156 tentativo={T} (criteri s132/s146/s148 EREDITATI + s157-criterio-pair-t7.md: giudizio a MEDIANA per finestra, az.rev.3 S-149) ==")
-print(f"grade=VERDICT  # derivazione meccanica dai .time; rc autoritativo = pair-out/pair157-{T}.done")
+print(f"== s158 coppia WP full+media sul pin s157 MISURATO phpr={PINH} server={SRVH} tentativo={T} (criteri s132/s146/s148 EREDITATI + s158-criterio-pair-t8.md: giudizio a MEDIANA per finestra, az.rev.3 S-149) ==")
+print(f"grade=VERDICT  # derivazione meccanica dai .time; rc autoritativo = pair-out/pair158-{T}.done")
 print("# full: cpu=user+sys (etichettato); media: CANONICA=user-only + companion (az.rev. S-128 #3)")
 print("# az.rev. S-131 #3: riferimento PER CONFIGURAZIONE, ON-ONLY = canonico; firma per gamba")
 print("# az.rev. S-136 #2: lettura firma PRE-REGISTRATA: >150% med MOTORE = SPORCA (esclusa); 120-150% = ELEVATA (nel canone, annotata); <=120% pulita")
@@ -217,18 +226,18 @@
     print(f"full_{cfg}_only_coppie_proprie={min(props):.3f}-{max(props):.3f} (N={len(props)}){canon}")
     print(f"full_{cfg}_only_intervallo={min(sp.values())/max(so.values()):.3f}-{max(sp.values())/min(so.values()):.3f}")
     if cfg == "on":
-        # s157-criterio-pair-t7.md p.2 (az.rev.3 S-149): GIUDIZIO CANONICO a
-        # MEDIANA per finestra vs mediane storiche t1=1,799 t2=1,738 t3=1,789 t4=1,781 t5=1,757 t6=1,771;
+        # s158-criterio-pair-t8.md p.2 (az.rev.3 S-149): GIUDIZIO CANONICO a
+        # MEDIANA per finestra vs mediane storiche t1=1,799 t2=1,738 t3=1,789 t4=1,781 t5=1,757 t6=1,771 t7=1,795;
         # la banda-unione resta SOLO companion descrittivo.
         MED_LO, MED_HI = 1.738, 1.799
-        med_t7 = statistics.median(props)
-        print(f"mediana_t7={med_t7:.3f} (N={len(props)} coppie proprie ON PULITE; mediane storiche t1=1.799 t2=1.738 t3=1.789 t4=1.781 t5=1.757 t6=1.771)")
-        if MED_LO <= med_t7 <= MED_HI:
-            print(f"giudizio_MEDIANA (canonico, criterio t7 p.2): COMPATIBILE (mediana in [{MED_LO}; {MED_HI}]) — nessun claim")
-        elif med_t7 < MED_LO:
-            print(f"giudizio_MEDIANA (canonico, criterio t7 p.2): direzione GIU' SEGNALATA (mediana {med_t7:.3f} < {MED_LO}) — attesa HD2-hostcall su WP dichiarata piccola/nulla: NON attribuibile senza census proprio")
+        med_t8 = statistics.median(props)
+        print(f"mediana_t8={med_t8:.3f} (N={len(props)} coppie proprie ON PULITE; mediane storiche t1=1.799 t2=1.738 t3=1.789 t4=1.781 t5=1.757 t6=1.771 t7=1.795)")
+        if MED_LO <= med_t8 <= MED_HI:
+            print(f"giudizio_MEDIANA (canonico, criterio t8 p.2): COMPATIBILE (mediana in [{MED_LO}; {MED_HI}]) — nessun claim")
+        elif med_t8 < MED_LO:
+            print(f"giudizio_MEDIANA (canonico, criterio t8 p.2): direzione GIU' SEGNALATA (mediana {med_t8:.3f} < {MED_LO}) — attesa L-AL1 su WP dichiarata piccola/nulla: NON attribuibile senza census proprio")
         else:
-            print(f"giudizio_MEDIANA (canonico, criterio t7 p.2): REGRESSIONE SEGNALATA (mediana {med_t7:.3f} > {MED_HI}) — indagine PRIMA di ogni altra leva")
+            print(f"giudizio_MEDIANA (canonico, criterio t8 p.2): REGRESSIONE SEGNALATA (mediana {med_t8:.3f} > {MED_HI}) — indagine PRIMA di ogni altra leva")
         print(f"companion banda-unione (descrittivo): coppie in [1.722-1.823]: {sum(1 for r in props if 1.722 <= r <= 1.823)}/{len(props)}")
         # s140-criterio-pair.md p.4: FONDAZIONE banda ON-config (N>=5 gambe pulite)
         if len(props) >= 5:
@@ -286,6 +295,10 @@
 else:
     print(f"deriva_test: gambe pulite N={len(clean)} < 6 -> SOTTO-CAMPIONATO, nessuna soglia applicata (s148 p.4)")
 PY
+LS_END="$(ls_sentinel)"
+{ echo "sentinella language-server inizio finestra (az.rev. S-157 #4): ${LS_START:-nessuno}"
+  echo "sentinella language-server fine finestra: ${LS_END:-nessuno}"
+} >> "$VERD"
 echo "rc=$RC" > "$DONE"
 step "DONE rc=$RC (fonte rc: $DONE)"
 exit "$RC"
