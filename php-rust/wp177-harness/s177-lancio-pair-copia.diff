--- wp174-harness/s175-lancio-pair.sh	2026-09-15 02:40:51
+++ wp177-harness/s177-lancio-pair.sh	2026-09-17 01:35:55
@@ -1,16 +1,16 @@
 #!/bin/bash
-# s175-lancio-pair.sh — catena p.1: attende la quiete CI (cargo/rustc/
+# s177-lancio-pair.sh — catena p.1: attende la quiete CI (cargo/rustc/
 # phpt-runner: job CI in volo al lancio; il lock S-171 tiene
 # ferma la coda), POI esige quiete CONTINUA anti-flare (criterio p.6,
 # az.rev. S-163 #5: 6 campioni consecutivi x30s con mediaanalysisd <5%,
 # predicato del riaggancio-r5b s163 ora PRE-registrato), DICHIARA la
-# finestra quieta (uptime + top nel log) e lancia s175-pair.sh t20.
-# COPIA DICHIARATA di s172-lancio-pair.sh (manifest s175-lancio-pair-copia.diff):
-# soli nomi s175/t20; il resto INVARIATO (blocco anti-flare EREDITATO).
+# finestra quieta (uptime + top nel log) e lancia s177-pair.sh t21.
+# COPIA DICHIARATA di s172-lancio-pair.sh (manifest s177-lancio-pair-copia.diff):
+# soli nomi s177/t21; il resto INVARIATO (blocco anti-flare EREDITATO).
 set -u
 export PATH=/usr/bin:/bin:/usr/sbin:/opt/homebrew/bin
-H="/Volumes/Extreme Pro/Claude/php-rust-experiment/php-rust/wp174-harness"
-LOG="$H/pair-out/lancio-t20.log"; mkdir -p "$H/pair-out"
+H="/Volumes/Extreme Pro/Claude/php-rust-experiment/php-rust/wp177-harness"
+LOG="$H/pair-out/lancio-t21.log"; mkdir -p "$H/pair-out"
 echo "$(date '+%F %T') attesa quiete CI" >> "$LOG"
 while pgrep -qx cargo || pgrep -qx rustc || pgrep -qf phpt-runner; do sleep 60; done
 sleep 60
@@ -28,10 +28,10 @@
   [ "$calm" -lt 6 ] && sleep 30
 done
 # dichiarazione finestra QUIETA (criterio p.6c): uptime + top
-Q="$H/pair-out/quiet-decl-t20.txt"
-{ echo "== dichiarazione finestra quieta t20 $(date '+%F %T') =="
+Q="$H/pair-out/quiet-decl-t21.txt"
+{ echo "== dichiarazione finestra quieta t21 $(date '+%F %T') =="
   uptime
   top -l 1 -n 8 -o cpu -stats pid,cpu,command 2>/dev/null | tail -12
 } > "$Q" 2>&1
-echo "$(date '+%F %T') quiete raggiunta — dichiarazione in quiet-decl-t20.txt — lancio pair t20" >> "$LOG"
-exec /bin/bash "$H/s175-pair.sh" t20
+echo "$(date '+%F %T') quiete raggiunta — dichiarazione in quiet-decl-t21.txt — lancio pair t21" >> "$LOG"
+exec /bin/bash "$H/s177-pair.sh" t21
