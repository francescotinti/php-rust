--- wp174-harness/s174-promozione.sh	2026-09-15 02:27:25
+++ wp177-harness/s177-promozione.sh	2026-09-17 01:34:14
@@ -1,50 +1,21 @@
 #!/bin/bash
-# s174-promozione.sh <cand_hash16> <braccio> — gate di promozione LEVA «Sweep-in-op»
-# (B = predicato inerte di Op::Sweep estratto in sweep_idle e interrogato a fine op dal solo
-# sentiero in place su Long di BinarySCSCDst/BinarySTDst/P3/P1-P4 ⇒ Sweep inerte scavalcato
-# senza dispatch; C = B + back-edge IncDecSlotJmp→CmpJmpSC fuso su slot Long/const Int)
-# — S-174, criterio wp174-harness/s174-criterio.md p.5. COPIA DICHIARATA di s173-promozione.sh
-# (manifest s174-promozione-copia.diff); adattamenti: tag s174 (harness wp174: verdetti, promo-out,
-# fx-sw1 NUOVA bilaterale; le fixture ereditate restano in wp172-harness), stash di riferimento
-# phpr-s173 (da4921a52eba0187), candidato phpr-s174-sw-<braccio> (B, C), conferma post-pin
-# arith-dq (giudice della leva) vs stash s173, mutante MS/MC (s174-mutante-sw.sh) ESEGUITO
-# PRIMA di questo copione (verdetto s174-mutante-sw-verdetto.out rc=0 richiesto in PRE).
-# EMENDA S-175 (dichiarata, manifest s174-promozione-tag-s175.diff): la promozione avviene in
-# S-175 (differita da S-174 per Data <10G) ⇒ TAG del pin/stash/micro = s175 (pin-phpr.sh s175,
-# pin-server.sh s175, micro-pin-s175.out, nota finale); verdetti e promo-out restano in wp174-harness.
-# COPIA DICHIARATA di wp171-harness/s171-promozione.sh (manifest
-# s172-promozione-copia-v3.diff + copia-gate v3); DIVERGENZE DICHIARATE:
-#  (1) tag s172, harness wp172 (fixture S-171 ereditate in BYTE-COPIA verificata
-#      cmp); candidato = hash argomento + BRACCIO promosso (B=P1, C=P1+P2; A/B R=5
-#      in s172-leva-verdetto.out: giudice prop-dq, guardia arith-dq); candidato
-#      stashato phpr-s172-sl2-<braccio> via pin-phpr.sh --braccio; stash di
-#      riferimento per i gate «pin==stash» = phpr-s171 (b360b2933eddfe18);
-#      **fx-sl2 NUOVA bilaterale** (presidio DIRETTO L-SL2, oracle==pin già sul
-#      pin s171); conferma post-pin = prop-dq (non arith-dq);
-#  (2) inventario batteria: baseline s125 + denti — i denti aggiunti S-126..S-170
-#      NON sono elencati qui: nomi NUOVI si DICHIARANO (nota), nomi SPARITI
-#      fermano (stop); conteggio a verbale; debug_backtrace_array_fields verde;
-#  (3) corpus: ZERO flip attesi (i fast path sono sottoinsiemi ESATTI degli arm
-#      Long di binary_fast; ogni miss ricomputa il corpo originale) — rc!=0 = STOP;
-#  (4) fixture chain s109 INVARIATA (10 gate) + fx-ce/fx-am/fx-af/fx-sm/fx-au
-#      bilaterali + sonda-bt/fx-refl/fx-sm-div/fx-au-div pin==stash s171
-#      (ereditate in BYTE-COPIA wp171 verificata cmp) + fx-mck bilaterale e
-#      fx-mc/fx-mc2/fx-mc2-fib pin==stash s171 (S-165/166, §3.28/§3.29) +
-#      **fx-sl1 NUOVA bilaterale** (presidio DIRETTO L-SL1: overflow, shift,
-#      Div/Mod/Pow, Ref/Double/stringa/null/bool, dst==l, typed by-ref, IncDec ai
-#      limiti, CmpJmpSC 8 forme; oracle con `-d log_errors=0 -d display_errors=1`
-#      perché il CLI oracle DUPLICA i diag su stderr con log_errors — dichiarato,
-#      la fixture bilaterale non emette diag) + **fx-sl1-div NUOVA** pin==stash
-#      s171 (forme con diag: §3.11 + §3.13 PRE-esistenti, INVARIATE);
-#  (5) conferma POST-PIN: arith-dq R=5 pin s172 vs stash phpr-s171 (attesa segno
-#      +, intorno del D_dq dell'A/B; rumore > attesa/2 ⇒ SOLO SEGNO);
-#  (6) disasm run_loop AGLI ATTI in fase A/B (ab-out/disasm-sl1.out), non qui;
-#  (7) micro R=5 = run-micro.sh (scoreboard al pin nuovo) — INVARIATO.
+# s177-promozione.sh <cand_hash16> — gate di promozione del TREE INTERO «flag gc-idle (S-176) + L-CM1 (S-177)»
+# (criterio wp177-harness/s177-criterio-cm1.md p.5, esito rc=0 dell'A/B s177-cm1-verdetto.out). COPIA DICHIARATA di
+# ../wp174-harness/s174-promozione.sh (manifest s177-promozione-copia.diff) coi SOLI adattamenti: (1) tag s177
+# (harness wp177: verdetto s177-promo-verdetto.out, promo-out; fixture ereditate INVARIATE in wp172-harness e
+# wp174-harness/fixtures); (2) stash di riferimento = phpr-s175 (5de14d6856d760a8) per i gate «pin==stash»; candidato =
+# braccio C dell'A/B (ab-out/s177-leva/phpr-C, hash argomento; registro BRACCIO via pin-phpr.sh --braccio s177-cm1
+# PRIMA di questo copione); (3) PRE: nessun mutante (criterio p.6) — al suo posto l'A/B a rc=0 (ESITO rc=0 in
+# s177-cm1-verdetto.out); (4) conferma POST-PIN sul GIUDICE della leva = prop-dq (pin s177 vs stash s175, attesa segno +,
+# D nell'intorno di D_comp dell'A/B; rumore > attesa/2 ⇒ SOLO SEGNO); (5) pin-phpr.sh s177 / pin-server.sh s177 /
+# micro-pin-s177.out; (6) corpus: ZERO flip attesi (L-CM1 non cambia alcun esito: stesso epoch, stesso checked_add).
+# Tutto il resto INVARIATO (testo S-174/175 conservato sotto).
 set -u
 export PATH=/usr/bin:/bin:/usr/sbin:/opt/homebrew/bin:"$HOME/.cargo/bin"
 SRC="/Volumes/Extreme Pro/Claude/php-rust-experiment/php-rust"
 H="$SRC/wp172-harness"
 H4="$SRC/wp174-harness"
+H7="$SRC/wp177-harness"
 BIN="$HOME/Claude/php-rust-output/release/phpr"
 RUNNER="$HOME/Claude/php-rust-output/release/phpt-runner"
 STASH="/Volumes/Extreme Pro/Claude/phpr-old-target/release"
@@ -52,10 +23,11 @@
 GATES="/Volumes/Extreme Pro/Claude/wp9-harness/gates"
 QUIESCE="$SRC/wp129-harness/s129-quiescenza.sh"
 SP="${PROMO_SP:?PROMO_SP (workdir APFS per i gate ORM/hk) richiesto}"
-OUT="$H4/promo-out"; mkdir -p "$OUT"
-VERD="$H4/s174-promo-verdetto.out"
-CAND_EXP="${1:?uso: s174-promozione.sh <cand_hash16> <braccio B|C>}"; ARM="${2:?braccio promosso (B=sweep-in-op, C=B+back-edge fuso)}"
-REF="$STASH/phpr-s173"; REF_EXP="da4921a52eba0187"
+OUT="$H7/promo-out"; mkdir -p "$OUT"
+VERD="$H7/s177-promo-verdetto.out"
+CAND_EXP="${1:?uso: s177-promozione.sh <cand_hash16>}"
+CAND="$H7/ab-out/s177-leva/phpr-C"
+REF="$STASH/phpr-s175"; REF_EXP="5de14d6856d760a8"
 note(){ echo "$1"; echo "$1" >> "$VERD"; }
 stop(){ note "$1"; echo 1 > "$OUT/rcb"; exit 1; }
 
@@ -63,16 +35,17 @@
 for f in "$H/fx-ce.php" "$H/fx-am.php" "$H/fx-af.php" "$H/fx-refl.php" \
          "$H/fx-sm.php" "$H/fx-sm-div.php" "$H/fx-au.php" "$H/fx-au-div.php" \
          "$H/fx-mc.php" "$H/fx-mc2.php" "$H/fx-mc2-fib.php" "$H/fx-mck.php" \
-         "$H/fx-sl1.php" "$H/fx-sl1-div.php" "$H/fx-sl2.php" "$H/fx-sl2-div.php" "$H/fx-sl3.php" "$H/fx-sl3-div.php" "$H/prop-dq.php" "$H4/fixtures/fx-sw1.php" "$H4/s174-mutante-sw-verdetto.out" \
+         "$H/fx-sl1.php" "$H/fx-sl1-div.php" "$H/fx-sl2.php" "$H/fx-sl2-div.php" "$H/fx-sl3.php" "$H/fx-sl3-div.php" "$H/prop-dq.php" "$H4/fixtures/fx-sw1.php" "$H7/s177-cm1-verdetto.out" \
          "$H/sonda-bt-autoload.php" "$SRC/wp164-harness/arith-dq.php" \
          "$H/empty.php" "$QUIESCE" "$WD" "$GATES/orm-work.tgz" "$GATES/hk-work.tgz" \
          "$SRC/wp125-harness/orm-baseline-failnames.txt" \
          "$SRC/wp125-harness/promo-out/batteria-nomi.txt" \
-         "$REF" "$STASH/phpr-s174-sw-$ARM"; do
+         "$REF" "$CAND"; do
   [ -s "$f" ] || stop "PRE: path d'ingresso MANCANTE: $f — STOP"
 done
-[ "$(shasum -a 256 "$REF" | cut -c1-16)" = "$REF_EXP" ] || stop "PRE: stash phpr-s173 hash != $REF_EXP — STOP"
-grep -q "^ESITO rc=0" "$H4/s174-mutante-sw-verdetto.out" || stop "PRE: mutante MS/MC non a rc=0 (s174-mutante-sw-verdetto.out) — STOP"
+[ "$(shasum -a 256 "$REF" | cut -c1-16)" = "$REF_EXP" ] || stop "PRE: stash phpr-s175 hash != $REF_EXP — STOP"
+[ "$(shasum -a 256 "$CAND" | cut -c1-16)" = "$CAND_EXP" ] || stop "PRE: candidato phpr-C hash != $CAND_EXP — STOP"
+grep -q "^ESITO rc=0" "$H7/s177-cm1-verdetto.out" || stop "PRE: A/B composto non a rc=0 (s177-cm1-verdetto.out) — STOP"
 
 cd "$SRC" || exit 4
 git diff --quiet -- crates/ || stop "PRE: crates/ sporco — STOP"
@@ -109,9 +82,7 @@
 if [ "$HB" = "$CAND_EXP" ]; then
   note "promozione: build ricetta riproduce il candidato $HB AL BYTE"
 else
-  CSTASH="$STASH/phpr-s174-sw-$ARM"
-  [ "$(shasum -a 256 "$CSTASH" | cut -c1-16)" = "$CAND_EXP" ] || stop "stash candidato != $CAND_EXP — STOP"
-  ident_contenuto "$BIN" "$CSTASH" > "$OUT/ident-contenuto.txt" 2>&1     || { cat "$OUT/ident-contenuto.txt" >> "$VERD"; stop "build $HB DIVERGE dal candidato OLTRE il meccanismo nominato — STOP"; }
+  ident_contenuto "$BIN" "$CAND" > "$OUT/ident-contenuto.txt" 2>&1     || { cat "$OUT/ident-contenuto.txt" >> "$VERD"; stop "build $HB DIVERGE dal candidato OLTRE il meccanismo nominato — STOP"; }
   cat "$OUT/ident-contenuto.txt" >> "$VERD"
   note "promozione: identità candidato a CONTENUTO (cluster LC_UUID/firma/banner) — pin effettivo = $HB (dichiarato)"
 fi
@@ -146,15 +117,15 @@
 [ "$H2" = "$HB" ] || stop "re-hash post-batteria $H2 != $HB (churn) — STOP"
 note "promozione: churn batteria neutralizzato (build ricetta → $H2 al byte)"
 
-"$SRC/scripts/pin-phpr.sh" s175 > "$OUT/pin.log" 2>&1
+"$SRC/scripts/pin-phpr.sh" s177 > "$OUT/pin.log" 2>&1
 prc=$?; echo "$prc" > "$OUT/pin.rc"
 [ "$prc" = 0 ] || stop "pin-phpr.sh rc=$prc"
 note "promozione: $(tail -1 "$OUT/pin.log")"
 
 "$SRC/scripts/corpus-gate.sh" "$RUNNER" "$OUT/corpus"
 crc=$?; echo "$crc" > "$OUT/corpus-rc"
-[ "$crc" = 0 ] || stop "corpus-gate rc=$crc — divergenza (3) non ammette flip: STOP"
-note "promozione corpus-gate: rc=0 — nomi==congelato (1412), CONTENUTO==golden, off-on zero (ZERO flip come atteso, Sweep-in-op)"
+[ "$crc" = 0 ] || stop "corpus-gate rc=$crc — divergenza (6) non ammette flip: STOP"
+note "promozione corpus-gate: rc=0 — nomi==congelato (1412), CONTENUTO==golden, off-on zero (ZERO flip come atteso, flag gc-idle + L-CM1)"
 
 PHPR_PIN_ATTESO="$H2" "$SRC/wp109-harness/s109-fixture-chain.sh" > "$OUT/fixture-chain.out" 2>&1
 frc=$?; echo "$frc" > "$OUT/fixture-chain.rc"
@@ -179,18 +150,18 @@
     stop "gate $n: DIVERGE (promo-out/$n.diff)"
   fi
 }
-# gate INVARIANTE: pin==stash s172 byte-id (+ marcatore quando preteso)
+# gate INVARIANTE: pin==stash s175 byte-id (+ marcatore quando preteso)
 invar(){ # $1=nome $2=file $3=marcatore("" = nessuno) $4=descrizione
   local n="$1" f="$2" m="$3" d="$4"
-  "$REF" "$f" > "$OUT/$n-s173.out" 2>&1
+  "$REF" "$f" > "$OUT/$n-s175.out" 2>&1
   "$BIN" "$f" > "$OUT/$n-pin.out" 2>&1
   [ -s "$OUT/$n-pin.out" ] || stop "gate $n: output pin VUOTO"
   [ -z "$m" ] || grep -q "$m" "$OUT/$n-pin.out" || stop "gate $n: MARCATORE $m assente"
-  if diff -q "$OUT/$n-s173.out" "$OUT/$n-pin.out" > /dev/null; then
-    note "promozione gate $n: pin==stash s173 BYTE-ID ($d)"
+  if diff -q "$OUT/$n-s175.out" "$OUT/$n-pin.out" > /dev/null; then
+    note "promozione gate $n: pin==stash s175 BYTE-ID ($d)"
   else
-    diff "$OUT/$n-s173.out" "$OUT/$n-pin.out" > "$OUT/$n.diff" || true
-    stop "gate $n: pin DIVERGE dallo stash s173 (promo-out/$n.diff)"
+    diff "$OUT/$n-s175.out" "$OUT/$n-pin.out" > "$OUT/$n.diff" || true
+    stop "gate $n: pin DIVERGE dallo stash s175 (promo-out/$n.diff)"
   fi
 }
 bilat fxce "$H/fx-ce.php" "" "12 forme class/interface/trait_exists"
@@ -221,11 +192,11 @@
 done
 [ "$QOK" = 0 ] || stop "quiescenza MAI PASS in 30 tentativi — STOP prima delle misure"
 
-PHPR="$BIN" R=5 "$SRC/wp97-harness/micro/run-micro.sh" > "$OUT/micro-pin-s175.out" 2>&1
-note "promozione micro pin s175: $(grep -E '^rapporto_' "$OUT/micro-pin-s175.out" | tr '\n' ' ')"
+PHPR="$BIN" R=5 "$SRC/wp97-harness/micro/run-micro.sh" > "$OUT/micro-pin-s177.out" 2>&1
+note "promozione micro pin s177: $(grep -E '^rapporto_' "$OUT/micro-pin-s177.out" | tr '\n' ' ')"
 
-# ---- conferma POST-PIN arith-dq (divergenza (5)): R=5 pin s174 vs stash s173 ----
-DQ="$SRC/wp164-harness/arith-dq.php"
+# ---- conferma POST-PIN prop-dq (divergenza (4)): R=5 pin s177 vs stash s175 ----
+DQ="$H/prop-dq.php"
 NDQ=$(awk 'match($0, /\$i<[0-9]+/) {print substr($0, RSTART+3, RLENGTH-3); exit}' "$DQ")
 ucpu(){ { /usr/bin/time -p perl -e 'alarm 900; exec @ARGV or die' -- "$1" "$2" > /dev/null; } 2>&1 | awk '/^user/{print $2}'; }
 floor3(){ local a b c; a=$(ucpu "$1" "$2"); b=$(ucpu "$1" "$2"); c=$(ucpu "$1" "$2"); printf '%s\n%s\n%s\n' "$a" "$b" "$c" | sort -n | awk 'NR==2'; }
@@ -249,7 +220,7 @@
 print(f"D={d:+.2f} rumore={noise:.2f} segni={segni}/5 A={med(na):.2f} B={med(nb):.2f} ns/iter (N={int(n)} dal driver)")
 PY
 )
-note "conferma post-pin arith-dq (pin s175 vs stash s173): $CONF (attesa: segno +, D nell'intorno del D_dq del braccio promosso nell'A/B; rumore > attesa/2 ⇒ SOLO SEGNO)"
+note "conferma post-pin prop-dq (pin s177 vs stash s175): $CONF (attesa: segno +, D nell'intorno di D_comp dell'A/B; rumore > attesa/2 ⇒ SOLO SEGNO)"
 
 # ---- gate ORM per NOME ----
 rm -rf "$SP/orm-work" && mkdir -p "$SP" && tar xzf "$GATES/orm-work.tgz" -C "$SP" || stop "untar orm-work"
@@ -279,11 +250,11 @@
   stop "gate http-kernel: E=$HE F=$HF (attesi 0/0) · $HSUMM"
 fi
 
-"$SRC/scripts/pin-server.sh" s175 > "$OUT/pin-server.log" 2>&1
+"$SRC/scripts/pin-server.sh" s177 > "$OUT/pin-server.log" 2>&1
 src_rc=$?; echo "$src_rc" > "$OUT/pin-server.rc"
 [ "$src_rc" = 0 ] || stop "pin-server.sh rc=$src_rc"
 note "promozione server: $(grep '^PIN server' "$OUT/pin-server.log" | tail -1)"
 
 echo 0 > "$OUT/rcb"
-note "PROMOZIONE COMPLETA rc=0 (da promo-out/rcb): pin s175 = $H2"
+note "PROMOZIONE COMPLETA rc=0 (da promo-out/rcb): pin s177 = $H2"
 exit 0
