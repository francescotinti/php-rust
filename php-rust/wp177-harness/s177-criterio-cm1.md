# S-177 criterio — LEVA COMPOSTA su prop-dq «L-CM1» (NEXT §S-177 p.1) — PRE-registrato PRIMA di build e misura
1. QUESITO: il tree porta il flag gc-idle (+2,73/+2,67 su prop-dq, sola direzione, S-176) e si promuove solo COMPOSTO con una leva prop-dq che porti D(pin→tree) ≥ max(4, rumore, SL). Candidata (a) «fetta 4 typed-skip» ESCLUSA prima della build: perimetro ZERO sul giudice (classe `C` plain e non tipizzata, P4 prende a ogni iterazione; census micro col binario census 80e4293c: prop_set ic_hit_plain 149 999 999 · typed 0 · miss 1 — conteggi, mai tempo; nel binario census i probe sono spenti: 8 op/iter, 1,2G op). Candidata (b) corpi, dal DISASM del pin (wp177-harness/disasm-pin-run_loop-stats.txt): run_loop ha frame ≥12 KB e 11 270 accessi [sp] su 71 745 istr; OGNI hit IC (30 siti) = `ldr [sp,#0x440]` (indirizzo TLV di IC_EPOCH salvato nel prologo) → `ldr` epoch → `cmp`; `long_arith_i64` inlinato = 3 confronti prima dell'`adds` per `Add`. L'ipotesi «chiamata TLV a ogni hit» è REFUTATA dal disasm (6 `blr`, tutti nel prologo): il costo residuo è UNA load dipendente per hit.
2. OGGETTO L-CM1 (safe Rust, ZERO unsafe, semantica IDENTICA per costruzione): (a) `Vm::ic_epoch: u64` letto UNA volta in `Vm::new` subito dopo `bump_ic_epoch()` (unico sito di bump a runtime, A-DS15: mai a metà richiesta); `PropIc::get/fill` e `MethodIc::get/fill` ricevono l'epoch come argomento e ogni sito passa `self.ic_epoch` — è lo STESSO valore che leggerebbero dal TLS per tutta la vita del Vm (Vm annidati sullo stesso thread sono dichiarati impossibili; se mai, il confronto diventa più restrittivo, mai meno: ogni Vm accetta solo celle riempite da sé); (b) `long_arith_i64`: `Add` provato PRIMA del match (stesso `checked_add`, stesso `None`). Nessun altro testo toccato.
3. BRACCI nello stesso run (copia dichiarata di s176-ab-flag.sh → s177-ab-cm1.sh): A = pin s175 (5de14d68) · Z = wp174 ab-out/phpr-C (b6c4b587, gemello a contenuto ⇒ SL in-run) · B = tree col flag (wp176 ab-out/s176-flag/phpr-B 6c7bbb55, 33,47 in S-176) · C = tree + L-CM1 (commit dichiarato per hash; ricetta di s176-flag-build.sh: `git archive` + SOURCE_DATE_EPOCH=0 + CARGO_INCREMENTAL=0 + target separata). R=5, rotazione dei 4 bracci, floors per binario, mediane per colonna E appaiate, rumore drop-1; PREV A same-binary = A di S-176 (20,28 / 36,20).
4. GIUDICE prop-dq (bersaglio): (i) **D_comp = A−C** = criterio di PROMOZIONE del tree intero: nominata se ≥ max(4, rumore, SL) con ENTRAMBI gli stimatori; (ii) **D_lev = B−C** = la leva sola, attesa **[1; 3] ns/iter** (3 hit IC/iter × 1 load dipendente + 2 `long_arith` × 2-3 rami); (iii) D_flag = A−B = replica same-binary della direzione S-176, attesa [2; 4]. GUARDIA arith-dq a SOLA regressione (Add-first: BinarySCSCDst usa Mul/Shr/Sub/Add ⇒ attesa direzione ≈0, |D| < 1; nessun hit IC nel loop).
5. ESITI: **rc=0 PROMOZIONE** del tree (D_comp nominata da entrambi gli stimatori, arith senza regressione) ⇒ copia dichiarata di s174-promozione.sh (tag s177, stash di riferimento phpr-s175) + coppia t21 (copia di s175-pair.sh) + ORM E3/E4 (s176-orm-coppia.sh, token s177) · **rc=3 SOLA DIREZIONE** (max(1,rumore,SL) ≤ D_comp < soglia): L-CM1 resta nel tree se D_lev ≥ max(rumore, SL), altrimenti revert al byte · **rc=4 KILL L-CM1** (D_lev < max(1, rumore, SL) ⇒ le due load/rami non pesano): revert al byte del commit L-CM1, il flag resta com'è · rc=5 regressione arith (leva SOSPESA) · rc=8 SL in-run > 4.
6. PARITÀ prima della misura (s177-leva-build.sh, copia dichiarata di s176-flag-build.sh senza mutante): C == oracle byte-id su fx-sw1 (marcatore FX-SW1 DONE) e su fx-sl1/fx-sl2/fx-sl3 (bilaterali S-171..S-173, presidio delle forme Long e delle IC prop), C == pin a blocchi su fx-sw2-gc (invarianza); batteria: i test `prop_ic_equality_is_structural_only` e `method_ic_equality_is_structural_only` riscritti con epoch ESPLICITA (invalidazione provata con epoch diversa) — nessun mutante: la leva non ha predicato da mordere, il presidio è la parità bilaterale + batteria + corpus alla promozione.
7. IGIENE: lock col TOKEN s177 · CI: coda potata a HEAD (39 job = replay S-174 col dente storico 7274<7361, archiviati in queue-pruned-s177), batteria+corpus sul tree = job CI su 710d823c letto PRIMA dell'A/B (rilievo 7 S-176; non è gate di record: la promozione li rifà) · quiescenza s129 a gate separato + watchdog disco + sentinelle Data/swap/updater/top nel .out (lanciatore = copia dichiarata di s176-lancio-flag.sh) · nessuna build durante la misura · disasm run_loop di C agli atti (bl-count vs pin 5993 e conteggio dei `ldr [sp,#0x440]` atteso 0) · dente run.rs cap 7361: se morde, alzato DICHIARANDO (REGOLE §5).
