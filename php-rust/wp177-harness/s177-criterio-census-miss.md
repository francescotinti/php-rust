# S-177 criterio CENSUS «IC miss di prop_set per CAUSA» su ORM (+ WP media) (NEXT §S-177 p.2) — PRE-registrato, CONTEGGI e mai tempo
1. QUESITO: il 55,4 % delle 8,99M scritture di proprietà di ORM (census S-176) NON passa dall'IC di `prop_set_entry` (né plain né typed). Quale causa domina? La causa dominante è il perimetro della prossima leva sui CORPI prop di ORM (rotta S-169: il 7× è nei corpi, non nel dispatch).
2. STRUMENTO (feature `op-census`, `census.rs`, contatori AtomicU64 come i typed S-176; una riga per ramo in `prop_set_entry`, nessun effetto sul binario di parità): al MISS dell'IC si classifica lo STATO della cella e dell'oggetto — `miss_ic_empty` (cella mai riempita: cid1==0 o epoch stantia), `miss_ic_scope` (scope_key ≠), `miss_ic_class` (classe dell'oggetto ≠ classe cachata: sito POLIMORFO), `miss_obj_lazy` (lazy ghost/proxy Doctrine), `miss_obj_enum`, `miss_slot_absent` (slot dichiarato assente/unset o prop dinamica), `miss_ref_typed` (slot Ref con typed_refs vive), `miss_init_props` (thunk prop_init: ramo INIT_PROPS, prima dell'IC) — e l'ESITO della risoluzione lenta — `res_plain_fast` (fast path WP-25, riempie l'IC), `res_np_fill_untyped` / `res_np_fill_typed` (ramo NP S-134, riempie l'IC), `res_dynamic` (PropAccess::Dynamic, nessuno slot), `res_private_mangled` (key ≠ name), `res_readonly`, `res_hook_or_magic` (set hook / `__set` / virtual), `res_denied_or_error`. Dump nella sezione «-- prop_set miss (S-177 causa) --» di PHPR_OP_CENSUS; aggregazione per NOME su tutti i processi dal copione (COPIA DICHIARATA di s176-census-typed.sh → s177-census-miss.sh, manifest; parser esteso alla sezione nuova).
3. GATE: gli stessi di s176-criterio-census.md p.3 (parità per NOME ORM vs baseline16 e media vuota; smoke a esito ESATTO: un sito polimorfo a 2 classi ⇒ `miss_ic_class` ≥1; una prop dinamica ⇒ `res_dynamic` ≥1; una classe plain al primo passaggio ⇒ `miss_ic_empty` ≥1 e `res_plain_fast` ≥1).
4. LETTURA (pre-registrata): causa dominante = la più alta tra le `miss_*` (somma = miss totale, a meno dell'INIT_PROPS contato a parte) e, incrociata con `res_*`: se `miss_ic_class` ≥ 40 % ⇒ leva = IC POLIMORFICA (2-4 entry per sito, come Zend) su ORM; se `miss_ic_empty`+`res_*_fill` dominano ⇒ siti freddi (una scrittura per sito: leva sui corpi della risoluzione lenta, non sull'IC); se `miss_obj_lazy` domina ⇒ perimetro = proxy Doctrine (cammino `lazy_prop_access`); se `res_dynamic`/`res_private_mangled` dominano ⇒ perimetro = scritture non cachabili per costruzione (leva sulla risoluzione per nome). Percentuali = conteggi, MAI tempo (veto S-176).
EMENDA dichiarata S-177 (rilievo 2 revisione): i contatori psm_*/psr_* sono PER ESECUZIONE, non per sito: `psm_ic_empty` alto NON significa «siti freddi» ma «siti (caldi) mai riempibili» — la lettura p.4 «ic_empty+fill ⇒ siti freddi» è invalida per costruzione; la lettura valida è psm_ic_empty × psr_* (private_mangled/readonly/np_nofill = cause di non-riempimento). Perimetro della leva = TETTO finché non si contano private∩readonly e private∩¬readonly per classe (S-178 p.1b).
5. IGIENE: si esegue SOLO a promozione chiusa (nessun edit del sorgente mentre il tree è in gate) e MAI in sovrapposizione con misure di tempo; build census su target separata `phpr-census-target`; lock s177; esiti da census-miss-out/census.done e s177-census-miss-verdetto.out.
