# S-176 criterio CENSUS «op in place + Sweep» (NEXT §S-176 p.3) — PRE-registrato, CONTEGGI e mai tempo
1. QUESITO: quanti Sweep, sui workload della coppia (WP media, ORM), seguono un op fuso in place su Long (BinarySCSCDst, BinarySTDst, BinaryTCPropSetPop e le altre forme P1/P3/P4) — cioè la portata REALE della fusione Sweep-in-op (S-174/175) e del flag gc-idle (S-176) fuori dai giudici micro — e quanti IncDecSlotJmp hanno CmpJmpSC come successore (back-edge fuso). Il predicato non decide nulla qui: nella build census `sweep_skip_next` risponde sempre falso, ogni Sweep è dispatchato e contato ⇒ X→Sweep = siti POTENZIALI.
2. STRUMENTO: build op-census da HEAD (il flag non cambia i conteggi) con la sezione nuova di census.rs «X -> Sweep (all)» + «IncDecSlotJmp -> X (all)» (la top-40 taglia le righe); dump su file per processo, aggregazione per NOME (copione s176-census.sh, copia dichiarata di s147-census-orm.sh + census53.sh + guardia uploads).
3. GATE: parità per NOME (ORM == baseline16; media zero nomi falliti) — se diverge i conteggi restano a verbale ma non alimentano decisioni; smoke a esito ESATTO sulle sezioni (rc=8 se mute).
4. LETTURA (pre-registrata): quota = Σ(X→Sweep con X ∈ forme in place su Long) / Σ(X→Sweep). Se la quota è <5 % su ENTRAMBI i workload, la coppia WP/ORM non può vedere Sweep-in-op né il flag (attese «direzione ≤0, sotto-risoluzione» confermate: nessuna cifra da cercare lì); se ≥5 % su uno, quel workload diventa giudice candidato per la promozione composta del flag. Nessuna cifra di tempo si deriva dai conteggi.
5. IGIENE: lock s176; MAI durante una finestra di misura (l'A/B flag è chiuso: rc=3 alle 23:21); build in target separata; uploads via guardia; SP APFS in /private/tmp.
