# S-176 criterio coppia dbal+ORM — CANONE EMENDATO (E3 anti-flare + E4 sentinella), PRE-registrato prima della prossima coppia
1. Vale per la prossima coppia ORM (dovuta a ogni pin nuovo: se la leva «flag gc-idle» viene promossa in S-176, subito dopo pin-server; altrimenti alla prima promozione successiva). Copione = `s176-orm-coppia.sh`, COPIA DICHIARATA di s175-orm-coppia.sh (manifest s176-orm-coppia-copia.diff); criteri s173/s175 EREDITATI salvo quanto sotto.
2. E3 (lezione 3 S-175): gate di quiescenza per gamba preceduto dall'assestamento a STREAK di mediaanalysisd (4 campioni consecutivi <5 % a passo 5 s, tetto ~15 min, retry ×3) — blocco copiato da s175-pair.sh; il gate s129 resta autoritativo e invariato. Il lanciatore non ha più bisogno della pre-attesa esterna (s175-lancio-orm-calmo.sh).
3. E4 (istruttoria s176-istruttoria-sentinella-orm.md): ORA_REF = 4,94 (mediana delle 18 gambe oracle pulite S-162…S-175 — rilievo 6 revisione S-176: era scritto 4,96, sanato in chiusura); banda sentinella oracle = [4,84; 5,04] (ORA_REF ± 2 % = spread storico delle gambe pulite); REF2 di phpr ri-normalizzata = [34,73; 34,81] (= [34,27; 34,35] × 4,94/4,875, con 4,875 la media oracle della finestra di riferimento s166-mattina). Scaletta a due estremi, RES 0,293, regola E1 (ictx per motore), rodaggio non giudicante: INVARIATI.
4. ATTESA della leva flag gc-idle su ORM (se promossa): direzione ≤ 0 su net_norm (mai peggiorare); magnitudine NON pre-registrabile (quota di Sweep fusi in place su Long nel workload ORM ignota finché il census p.3 non la conta): COMP o GIU accettati; REGR o NR ⇒ istruttoria.
5. Chiusura della voce ORM riaperta in S-175 (regola 4): con la prima coppia sotto E4 che dia ENTRAMBE le gambe oracle in banda; se una gamba esce anche con E4 ⇒ istruttoria macchina (loadavg e livello termico per gamba nel verdetto).
6. Igiene INVARIATA: lock col token di sessione, MAPPA_SP su APFS, watchdog phpunit, rc solo da orm-out/rimisura.done; il lanciatore gata sul CONTENUTO `rc=0` dei `.done` a monte (rilievo 2 S-175).
