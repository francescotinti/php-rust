# S-176 criterio CENSUS «typed» (NEXT §S-176 p.4: perimetro typed PRIMA di una fetta 4) — PRE-registrato, CONTEGGI e mai tempo
1. QUESITO: quanta parte delle scritture di proprietà dei workload della coppia (WP media, ORM) passa dall'IC di `prop_set_entry` con il bit TY (classe con proprietà TIPIZZATE: coercizione `coerce_typed_prop_write` nel ramo IC, S-134) rispetto al ramo IC plain e al cammino pieno (miss). È il perimetro reale di una «fetta 4» sul sito `$o->x = $o->y OP C` (BinaryTCPropSetPop) e sulle altre forme P1/P4: se le classi typed dominano (ORM: entità Doctrine con proprietà tipizzate), una fetta che salta la coercizione non è ammissibile e la leva successiva va cercata nella coercizione stessa; se dominano le plain, la fetta 4 ha perimetro.
2. STRUMENTO: contatori nella build op-census (feature `op-census`, `census.rs`): `prop_set_ic_hit_plain`, `prop_set_ic_hit_typed`, `prop_set_miss` incrementati in `prop_set_entry` (una riga per ramo, nessun effetto sul binario di parità che non ha la feature), stampati nel dump PHPR_OP_CENSUS come sezione «-- prop_set entry (S-176 typed) --»; aggregati per NOME su tutti i processi dal copione census (copia dichiarata di s176-census.sh: solo la sezione in più nel parser).
3. GATE: gli stessi di s176-criterio-census.md p.3 (parità per NOME ORM/media; smoke a esito esatto: su una classe typed la riga `typed` ≥1 e su una plain la riga `plain` ≥1).
4. LETTURA (pre-registrata): quota typed = typed / (plain + typed + miss) per workload. ≥50 % su ORM ⇒ la fetta 4 «salta la coercizione» è FUORI perimetro su ORM (si ridisegna sulla coercizione); <10 % su entrambi ⇒ fetta 4 ammissibile con presidio bilaterale typed; in mezzo ⇒ la fetta si disegna a due rami (plain fast, typed coerce) e il census ne fissa l'attesa in direzione.
5. IGIENE: come s176-criterio-census.md p.5; si esegue DOPO il census p.3 (stessa target, build incrementale), mai in sovrapposizione con misure di tempo.
