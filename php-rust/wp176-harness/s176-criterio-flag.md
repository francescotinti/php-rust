# S-176 criterio leva «flag gc-idle» (NEXT §S-176 p.2) — PRE-registrato PRIMA della build e della misura
1. QUESITO: il predicato `sweep_idle` (3 load + somma + 2 confronti, letto a ogni op fuso in place su Long e dal handler `Op::Sweep`) vale fino a 4,67 ns/iter su prop-dq (tetto MS, S-175 CIFRA) e 2,36 su arith-dq (direzione). La leva: un flag `gc_idle: [bool; 2]` (indice = `main`) sul Vm, portato a `[false; 2]` dai siti che possono rendere FALSO il predicato (push di note/ctr-root, insert di cycle-root fuori dallo sweep, `gc_enable`/ini, refresh del bound) e ricalcolato ESATTO in coda a `gc_sweep` (ogni uscita, anche per errore) e al reset; `sweep_idle` = IN_DESTRUCTOR || `gc_idle[main]` (UN testo, handler + siti fusi). Semantica: sotto-approssimazione conservativa (falso ⇒ lo Sweep gira), esatta dopo ogni Sweep eseguito.
2. BRACCI: A = pin s175 (5de14d6856d760a8) · Z = ab-out/phpr-C (b6c4b5876971d76d, gemello a contenuto ⇒ |A−Z| = SL in-run) · B = flag (build con la ricetta di s175-mutante-gc.sh: `git archive` del commit della leva, SOURCE_DATE_EPOCH=0, CARGO_INCREMENTAL=0, target separata) · C = MS (dcd73bf42232e01f, tetto S-175, mutante SCORRETTO: solo contrasto B−C, mai promozione).
3. GIUDICI: prop-dq BERSAGLIO a nomina (attesa D_B = A−B ∈ [2;5] ns/iter, quota del tetto 4,67; soglia max(4, rumore drop-1, SL)) · arith-dq GUARDIA a sola regressione (attesa direzione [1;2,5]) · R=5, rotazione dei 4 bracci, floors per binario, mediane per colonna e appaiate, PREV = A del mock (20,08 / 36,13). Copione = COPIA DICHIARATA di s175-ab-mock.sh (manifest s176-ab-flag-copia.diff): soli tag, etichette dei bracci, attese.
4. ESITI: CANDIDATA a promozione (rc=0) se D_B prop ≥ soglia con ENTRAMBI gli stimatori e arith senza regressione (D < −max(4,rumore) = regressione) · SOLO DIREZIONE (rc=3) se max(1,rumore) ≤ D_B < soglia: guadagno parziale tenuto (feedback keep-partial-wins), promozione rimandata a rerun · KILL (rc=4) se D_B prop < max(1, rumore): il costo sta nei load/cmp singoli non nella somma ⇒ revert al byte, leva morta.
5. PARITÀ PRIMA della misura (gate separato, rc da file): B su fx-sw2-gc.php == A per blocchi (invarianza) e su fx-sw1.php == oracle byte-id (FX-SW1 DONE) · MUTANTE M-flag = `gc_idle_set` reso no-op (una riga; il flag resta `[true; 2]` dall'init ⇒ Sweep sempre saltato): fx-sw2-gc DEVE rompere almeno gcp-fused e gcp-loop (come MS) e fx-sw1 DEVE divergere dall'oracle; attese non rotte ⇒ il flag non è letto dai siti ⇒ rc=1 e niente misura. Batteria/corpus/coppia SOLO alla promozione (copia dichiarata di s174-promozione.sh, tag s176).
6. IGIENE: lock col TOKEN s176 · quiescenza s129 nel copione · Data ≥10G e watchdog disco nel lanciatore (copia di s176-micro-record.sh p.2-4) con sentinelle nel .out · nessuna build durante la misura · esiti da ab-out/flag.rc e s176-flag-verdetto.out · lanciatore committato PRIMA del run, STOP a commit fallito.
