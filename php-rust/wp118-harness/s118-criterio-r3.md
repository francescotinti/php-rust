# s118-criterio-r3.md — R3-Pedersen: giudizio di confine WP/per-request sotto A′ (PRE-registrato, S-118)

1. Perimetro: pin phpr s117 **1656580e** (release AL pin); server ripinnato sotto A′ da `scripts/pin-server.sh` EMENDATO (env ricetta `SOURCE_DATE_EPOCH=0 CARGO_INCREMENTAL=0`; profilo A′ già nel Cargo.toml di root). Churn di release/phpr dalla build server ⇒ build ricetta e H1==1656580e AL BYTE, pena STOP.
2. Grado server (`s118-grado-server.sh` = ricetta s109 invariata salvo harness/pin/stash): sentinella estesa PASS; option=413 e restapi=3508 IDENTICI per NOME oracle↔phpr nei 2 modi espliciti; `rc=0 voids=0` per modo.
3. `pair109.sh off|on` STESSA SERA col release==pin: `gate_void=0` ×2 modi; parità = media 0 fail ×2 lati, full failnames diff == SOLO `Tests_Functions::test_wp_is_stream` (catalogo, pre-esistente); i rapporti dai `.time` sono il NUOVO riferimento sotto A′ (nessuna soglia: tra-sere = solo direzione, REGOLE §4 — il rif 1,867/1,869 DECADE come previsto da S-110).
4. Tripla census per-request (ricetta `probe72-tripla.sh` + `analyze72.pl` INVARIATE; binario census dal SORGENTE del pin, build separata memgc): 3 leg VALIDI (uc-steady, no cron, body stabile) e verdetto macchina PASS con **obj/req = 0,000 spread 0,000**; KiB/req a referto (banda T-72 del giudice, nessuna soglia nuova).
5. Esito: R3 PASSA solo se 1–4 TUTTI verdi ⇒ la dicitura «parità CLI» (S-117) si estende a «parità WP per-request» e il verdetto WP della pipeline A′ si CHIUDE. Un gate rosso = debito APERTO, si dichiara nel report; nessuna cifra fuori dai `.out`/verdict emessi dagli script giudici.
