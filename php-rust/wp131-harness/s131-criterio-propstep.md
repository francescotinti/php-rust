# Criterio S-131 p.2 — modello del costo prop_step NON-resolve + quota resolve per call-site (az.rev. S-130 #4; commit PRIMA del codice sonda)

1. **Oggetto**: sul pin s130 (0fdf1c49) partizionare E−E2 (~165 ns/iter su objdatains, di cui ~120 non-resolve) in segmenti NOMINATI dentro `field_write_prop_step` — la forma E1/E-prop di S-132 si nomina SOLO da questo modello.
2. **Sonda**: build EMENDATA (patch `time-probes-propstep.patch` scritta col tree e ripristinata; modulo `wp131-harness/timeprobes.rs` NSEG=12 via `#[path]`; target separato `phpr-time-l1-target` CALDO; tree verificato pulito post-build; parità stdout vs pin per categoria). Segmenti: 0=TOT arm · 4=E field_set · 5=E2 walk interno · 7=CAL (numerazione S-130 per confronto) · **1=prop_step INTERO (call-site unico del driver) · 2=borrow (Rc::clone+try_borrow_mut+header) · 3=trio guardie leaf (is_declared_slot+hooked+typed) · 6=prop_key leaf · 8=replace+dropped · 9=resolve DENTRO prop_is_declared_slot · 10=resolve DENTRO prop_key · 11=resolve TUTTE (= E1a S-130)**; `cntvct_el0` mrs senza isb (dichiarato).
3. **Quota per call-site (az.rev. S-130 #4, CON rerun)**: statement-site = c9+c10; altri-siti/iter = (c11−c9−c10)/N con quota ctor letta sul controllo objalloc; se sul cammino statement restano resolve non attribuite (c11/iter − ctor − c9 − c10 > 0), i siti si ENUMERANO per NOME (find_referencing_symbols, a verbale) prima di ogni lettura. La p.2 del criterio E1a S-130 è EMENDATA a verbale (certificazione «solo cammino FieldAssign» falsa — controllo objalloc k=4); emenda annotata nel file s130.
4. **Categorie e ricetta**: {objalloc (0 stmt, controllo ctor), objdatains, p2-append, p5-two, p6-overwrite}; N=3e6 dal sorgente; R=3, mediane + spread; conteggi per segmento DETERMINISTICI tra R (scarto ⇒ indagine); zeri sintetici SOLO su objalloc (lezione S-129).
5. **Lettura**: quote = MODELLO (build emendata ⇒ nessuna cifra promuovibile); risoluzione = max(4 ns/iter, spread); overhead sonde ANNIDATE sottratto nel giudice DAI CONTEGGI (cal × note interne per segmento, nesting statico dichiarato nel giudice); chiusura: (2+3+6+8) vs (1−5) ≥90% dichiarata, resto = «prop_step altro».
6. **Igiene**: quiescenza gate SEPARATO prima del run sonda e rc citato nell'HEADER del verdetto (az.rev. S-130 #3); patch applicata SOLO a coppia WP conclusa (mai edit .rs in finestra di misura); run sequenziali foreground-of-script; output non tracciati; rc autoritativo = SOLO `tempo-out/tempo-propstep.rc`.
