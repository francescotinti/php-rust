# Criterio S-131 — LEVA E1-KO «resolve-once» in field_write_prop_step (forma NOMINATA dal modello propstep; commit PRIMA di ogni run)

1. **Forma**: in `field_write_prop_step` UNA `resolve_prop_access` dopo l'header (ra), riusata dai 4 siti helper: key0 ≡ `prop_key` (stesso match, per costruzione), declared0 ≡ `prop_is_declared_slot`, denied0 ≡ `prop_key_read().is_none()` (None ⇔ Denied, per ispezione del sorgente); `prop_indirect_guard` INVARIATA (5° sito, costo ≈0 misurato). Nessun cambio alloc previsto (Cow Borrowed ovunque). UB dal modello = 40,3 − 10,4 ≈ **30 ns/statement** (s131-propstep-lettura p.5; converge con UB E1a 31–35).
2. **Giudice** objdatains; A = stash pin s130 (`phpr-s130`, 0fdf1c49), B = build canonica post-edit (hash a verbale); segno: B CALA; smoke R=2 con early-stop a segno opposto → conferma R=5. D = medianaA − medianaB (serie piene, net floor med3 per binario).
3. **Rumore**: trimmed drop-1 SIMMETRICO su A e B (R≥4; R≤3 range pieno), rumore = max(rangeA′, rangeB′); soglia giudice = max(4 ns/iter, rumore, SL prop 0,80) — criterio S-130 p.3 riusato invariato.
4. **Bande guardie** SOLO-REGRESSIONE: le sei storiche su SL committate (max s123/s125); micro-ORM FONDATE sul pin s130 = range drop-1 delle **7 gambe B committate S-130** (smoke R=2 + R=5, stesso binario 0fdf1c49 che qui è A, raw datati 2026-08-11): **objalloc 6,7 · objchurn 20,0 · objmap 13,3** ns/iter; soglia_reg = −max(4, banda).
5. **Invarianti**: arbitro = `s131-ab.sh` COPIA DICHIARATA di s130-ab.sh coi SOLI adattamenti: A=stash s130, bande p.4, header che stampa file+valore dell'rc di quiescenza (az.rev. S-130 #3); quiescenza = gate SEPARATO `wp129-harness/s129-quiescenza.sh` PRIMA di ogni run; disasm bl-count n/a dichiarato (la leva non tocca run_loop; parità stdout bilaterale per categoria nell'arbitro); rc autoritativo = SOLO `ab-out/<TAG>.rc`.
6. **Promozione** SOLO se giudice sopra soglia e guardie ok: catena `s131-promozione.sh` copia dichiarata di s130-promozione.sh (con inventario fixture per NOME già emendato) → pin s131 via scripts/pin-phpr.sh + pin-server.sh. Esito avverso ⇒ verdetto committato PRIMA di ogni lettura/emenda; revert che DEVE riprodurre 0fdf1c49 al byte.
