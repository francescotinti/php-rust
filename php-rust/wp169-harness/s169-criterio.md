# S-169 criterio (az.rev. revisione S-168, lente semantica) — PRE-registrato PRIMA di build/misura; stessa conversazione di S-168
1. OGGETTO (sola misura, nessuna leva): **m5** = 8 `Op::Nop` per statement-espressione PRIMA dello Sweep (`s169-m5.patch`; il dump del loop DEVE mostrare CmpJmpSC, BinarySCSCDst, 8×Nop, Sweep, IncDecSlotJmp) ⇒ costo puro del dispatch per-op = −D_m5/8 · **m7** = m2 SENZA guardia sulla tupla (`s169-m7.patch`, `if true`: semantica NON preservata fuori dal driver) ⇒ costo della guardia = D_m7 − D_m2 · **ri-misura m4b e m3** a R=9 (stessi binari stash `phpr-s168-m4b`/`phpr-s168-m3`) · m3-puro (hoist senza cambio di forma) DICHIARATO NON SCRIVIBILE in safe Rust (il borrow di `self.frames[top].slots` non sopravvive ai `&mut self` del funnel): la componente frames[top] resta «confusa».
2. GIUDICE/A/B: `s169-ab-mock.sh` (copia dichiarata, R parametrico), A = m0 (36d73812), arith-dq N=250M, ns/iter=(med raw−floor)/N, ABAB alternato; pavimento 4 stampato COME PRIMA e, accanto, **soglia di DECOMPOSIZIONE = max(rumore drop-1, SL 0,94)** (az.rev.1 S-168): una componente è «a direzione firmata» se |D| ≥ soglia_dec, «nominata» se D ≥ 4. Guardia E2 |e2B−e2A| ≤ max(4, rumore). Output A==B (rc=2 STOP).
3. XCTRACE-2 (`s169-xctrace.sh`, copia dichiarata): p/o-dq, p/o-rbm (`arith-rbranchmut.php`, LCG bit 30: mutante branch NON predicibile) e p/o-c0 (`arith-c0mut.php`: crc32 di 4 KB L1-residenti per iterazione = lavoro retiring puro, N=2M). Attese: rbm alza c3 (se c3 = discarded) su ENTRAMBI i motori; c0mut alza c0 (se c0 = useful) e NON c1/c2. Se rbm non alza c3 su nessuno dei due ⇒ c3 resta NON fissata e «mispredict» resta NON FIRMATO.
4. VERDETTO (meccanico, in `s169-verdetto.out`): dispatch/op = −D_m5/8 (con rumore/8); corpo dei 2 handler di controllo = (e2_A − 2·dispatch)/2; guardia m2 = D_m7 − D_m2; le cifre entrano nella DELIBERA R4 insieme a Σ nominata 9,92 e Σ grezza 12,9 (S-168): la delibera è dell'utente, questa sessione NON la prende. Kill/soglie ⚖️ S-167 invariati.
5. IGIENE: lock S-169 · quiescenza (la CI sta consumando 290 commit in coda: le misure aspettano il suo job corrente) · target dedicato rimosso in epilogo · esiti solo da file · raw xctrace fuori repo.
