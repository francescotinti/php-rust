# S-174 criterio — LEVA «Sweep-in-op» (residuo trasversale del loop, NEXT §S-174 p.3); PRE-registrato PRIMA di build/misura
1. CENSUS (dump `PHPR_DUMP_OPS=1` del pin s173, S-174): loop arith-dq = 4 op/iter (0005 CmpJmpSC · 0006 BinarySCSCDst · 0007 Sweep{main} · 0008 IncDecSlotJmp); loop prop-dq = 8 op/iter INVARIATI da S-173 (0013/0014 bigramma P1 · 0015 Sweep · 0016/0017 P3 · 0018 Sweep · 0019 IncDecSlotJmp). `Op::Sweep` (run.rs 6888) = sweep GC di fine statement: in destructor = no-op; altrimenti fast-path `noop` = buffer note vuoto ∧ (¬main ∨ light_demoted vuoto) ∧ (gc off ∨ radici < bound), e SOLO se ¬noop chiama `gc_sweep`. Mock m4b (S-168/169, arith-dq, R=9): Sweep/iter ≈2,9 = dispatch + predicato.
2. OGGETTO (safe Rust, ZERO `unsafe`, semantica PRESERVATA per costruzione): **B = «sweep-in-op»**: il predicato del handler viene ESTRATTO in un helper `sweep_noop_next(top, func, at)` `#[inline(always)]` (un solo testo: il handler Op::Sweep lo richiama, il predicato non è duplicato) che risponde vero SOLO se `ops[at]` è `Sweep{main}` E (in destructor ∨ noop); lo si invoca a FINE corpo (dopo ogni effetto, gc_note dei miss compresi) di `BinarySCSCDst`, `BinarySTDst`, del peephole P3 (`PropGetSlot`+`BinarySTDst` ⇒ ip+3) e delle due uscite in place del probe P1/P4 (`PropGetSlotRecv`+`BinaryTCPropSetPop` ⇒ ip+3): se vero, `ip` scavalca lo Sweep (un dispatch in meno), altrimenti lo Sweep gira come oggi. NESSUNA elisione statica: si salta SOLO uno Sweep il cui corpo non farebbe nulla, deciso a runtime dopo l'op. Disabilitato sotto `op-census`/`gc-census` (contatori intatti). **C = B + «back-edge fuso»**: in `IncDecSlotJmp`, dopo l'incremento (invariato), se `ops[addr]` è `CmpJmpSC` col fast path in dominio (Const::Int, slot Long: `long_cmp_i64` VERBATIM) ⇒ `ip = addr2` se `jump == when`, altrimenti `addr+1`, senza dispatch; ogni miss ⇒ `ip = addr` (storico). Nessun corpo `#[cold]` nuovo.
3. GIUDICE (bersaglio): **arith-dq** (`wp164-harness/arith-dq.php`, N LETTO dal driver, tick 0,04 ≤ soglia/4). **GUARDIA prop-dq** a SOLA regressione; il suo D è riportato come «direzione+magnitudine non ripartita» (2 Sweep/iter fusi: attesa DICHIARATA [2;6], NON giudicata). Bracci nello STESSO run: **A = pin s173** (da4921a52eba0187, hash MISURATO), **A0 = stessa sorgente del pin ricostruita** (az.rev. (a): SL in-run), **B**, **C** (commit consecutivi; ricetta pin; target dedicato `/Volumes/Extreme Pro/Claude/s174-leva-tgt`, `git archive` + touch; build SEQUENZIALI, detached, MAI durante il corpus né le misure). **R=5, ordine dei QUATTRO bracci RUOTATO per coppia** (C mai in posizione fissa) su giudice e guardia; oracle per coppia. ns/iter=(med raw−floor)/N, floor per-binario; rumore drop-1; mediane per COLONNA e APPAIATE (az.rev. (b)); **nominata** D ≥ max(4, rumore, SL_inrun); direzione firmata |D| ≥ max(rumore, SL_inrun). Parità A/A0/B/C vs ATTESO oracle (rc=2). Copione `s174-ab-leva.sh` = COPIA DICHIARATA di `s173-ab-leva.sh` (manifest).
4. ATTESE (nessuna cifra composta): **D_B ∈ [1; 3]** (un dispatch ≈1,75 dal m4b 2,9, meno il predicato che RESTA); **KILL-B**: D_B < max(1, SL_inrun) ⇒ il costo dello Sweep non è il dispatch ma il predicato (3 load + 2 confronti): la fusione non paga, il bersaglio diventa il predicato (flag cache), fuori sessione; **D_C ∈ [2,5; 6]**; contrasto C−B ∈ [1; 3] (direzione attesa; cifra solo se nominata da ENTRAMBI gli stimatori). **Promozione** = braccio con D maggiore purché NOMINATO e senza regressione su prop-dq (C atteso; C−B < −rumore ⇒ B; nessuno nominato ⇒ rc=4, solo direzione, niente promozione).
5. GUARDIE alla promozione (REGOLE §5-6): micro R=5 (arith/prop/calls/str/arr/re ≤ pin + tick) · disasm `bl` run_loop pin vs candidato (attesa Δ ≤ +8; ≥ +20 = inliner flippato, si dichiara) · dente `wc -l run.rs` (cap 7274: se morde, alzato DICHIARANDO) · gate pieni `s174-promozione.sh` = COPIA DICHIARATA di `s173-promozione.sh` con **fx-sw1 NUOVA bilaterale** (oracle==pin==candidato): distruttori di temporanei rilasciati dall'op che PRECEDE lo Sweep (dst oggetto con `__destruct` sovrascritto dal risultato Long via `reg_store_slot` in BinarySTDst/BinarySCSCDst/P3, prop oggetto sovrascritta via P1 con dst non-Long; `echo` prima/dopo per l'ORDINE), forme in loop ≥2, Sweep main e non-main (dentro funzione), op eseguito DENTRO un distruttore, pressione GC (cicli ≥ soglia, `gc_collect_cycles` a valle), loop `for` con CmpJmpSC su slot non-Long / const non-Int (C: miss) e `when` in entrambi i sensi, overflow al back-edge, `for` con corpo vuoto · **mutante abortivo** PRIMA della promozione: **MS** = predicato reso sempre vero nel helper (Sweep SEMPRE saltato dopo gli op fusi) ⇒ i blocchi con distruttore/ordine DEVONO rompersi (dominio), quelli senza restano INTATTI per NOME; **MC** = `long_cmp_i64` negato nel back-edge fuso ⇒ i `for` Long/Int si rompono, gli altri intatti. http-kernel 0E/0F + ORM 16 nomi == baseline a gate. Coppia WP+ORM DOVUTA se il pin cambia (attesa direzione ≤0; sentinella ORM: 4,94 estremo osservato — fuori banda ⇒ voce riaperta).
6. IGIENE: lock `s174` VIVO; quiescenza s129 prima dell'A/B; bracci via symlink neutri `/private/tmp/s174-bins/{A,A0,B,C}`; NESSUNA build né misura durante il corpus di gh-status-sync né durante l'A/B; esiti da file; output di run mai nel repo; target dedicato rimosso in epilogo; xctrace NO (Data < 20G).
7. ORDINE: gh-status-sync (corpus, in corso) → az.rev. (c)(d) → questo criterio (commit) → codice B (commit) → codice C (commit) → copioni (build/A-B/mutante, copie dichiarate) → build A0, B, C (DOPO il corpus) → parità + dump (op INVARIATI: peephole runtime) → `--braccio` col commit → disasm → A/B R=5 → sintesi `s174-verdetto.out` → SOLO SE ammessa: fx-sw1 + mutanti MS/MC + `s174-promozione.sh` (detached) → coppia (detached, in catena) → revisione (lente SEMANTICA, rotazione: S-173 misura) → rotazione.
EMENDA p.3 (2026-09-13 22:41, prima dell'A/B): A0 = Z = ricostruzione della sorgente del pin = 19c0540e03d590a8 (gemello a contenuto del pin, non layout diverso: vedi s174-azrev-criterio.md) ⇒ SL_inrun = controllo nullo tra binari identici; le soglie restano max(4, rumore, SL_inrun) e max(rumore, SL_inrun).
