# S-174 criterio — AZ.REV. della revisione S-173 (wp172-harness/revisione-s173.md, lente MISURA); PRE-registrato PRIMA di ogni run (NEXT §S-174 p.2)
1. **(a) banda tra run + 0,94 ri-derivato**: il copione `s174-ab-leva.sh` (COPIA DICHIARATA di `s173-ab-leva.sh`, manifest `s174-ab-leva-copia.diff`) stampa per OGNI giudice, accanto al rumore drop-1: (i) **banda tra run** = |A_pin(oggi) − A_pin(ultima misura dello STESSO binario a contenuto: prop-dq 41,13 f3-C / 41,27 conferma; arith-dq 23,68 f3-C)|, valori PASSATI come argomenti dichiarati; (ii) **SL in-run** = |med(A) − med(A0)| con A0 = STESSA sorgente del pin ricostruita (HEAD pre-leva: `.rs` identici al commit 64c55cc2, hash diverso per i path ⇒ layout diverso) misurata nello STESSO run. La soglia di direzione per giudice = max(rumore, SL_inrun); il 0,94 (arith, S-168) è solo RIPORTATO a confronto, mai più importato. SL_inrun > 4 su un giudice ⇒ rc=8 (banda-layout sopra il pavimento: si estende R, nessun verdetto).
2. **(b) mediane appaiate**: per ogni contrasto il copione stampa la mediana per COLONNA e la mediana delle DIFFERENZE APPAIATE (stessa coppia); una cifra si dichiara come INTERVALLO [min;max] dei due stimatori se ENTRAMBI nominano; se ne nomina uno solo ⇒ «solo direzione» (cifra negata). Retroattivo: P2 = [4,87;5,60] già in s173-abbc-verdetto.out (nota post-revisione), nessun rerun.
3. **(c) decomposizioni = ipotesi**: VERIFICA (nessun edit se già marcato) in `s173-verdetto.out:4`, NEXT §S-174 p.3, `gaps/REPORT_GAP_173.md` §Residuo: esito in questo file, riga in coda.
4. **(d) blocchi -each intatti**: nota post-revisione in `s173-mutante3-verdetto.out` che NOMINA i blocchi `-each` (INT3: p3-null-dst-each p3-bool-dst-each p3-numstr-dst-each; INT4: p4-dst-double-each p4-dst-str-each) come INTATTI, letti da `ab-out/s173-mut3/MP3.rotte`/`MP4.rotte` (assenza = intatto); il copione mutante S-174 (`s174-mutante-sw.sh`) stampa gli INTATTI attesi per NOME nel verdetto, non solo il conteggio.
5. ORDINE e IGIENE: (c)(d) subito (sola lettura + note); (a)(b) nel copione S-174 PRIMA della sua prima corsa (copia-gate per TOKEN s174); nessun rerun dei verdetti S-173; lock `s174` VIVO; esiti da file.
ESITO (c) 2026-09-13: decomposizioni del residuo marcate IPOTESI in s173-verdetto.out:4 («IPOTESI (cifre da altri binari, S-169), non misure»), NEXT §S-174 p.3 («IPOTESI di riparto, cifre da altri binari»), gaps/REPORT_GAP_173.md §Residuo («IPOTESI: le cifre per-op vengono da altri binari») — nessun edit necessario. ESITO (d): nota scritta in s173-mutante3-verdetto.out (5 blocchi -each nominati, 0 in MP3/MP4.rotte).
