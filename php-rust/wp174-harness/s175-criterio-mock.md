# S-175 criterio MOCK MAGRO «flag gc-idle» (NEXT S-175 p.3a) — PRE-registrato PRIMA della misura
1. QUESITO: sul giudice arith-dq restano 20,4 vs 8,68 (2,35×) dopo C; il predicato di `sweep_idle` (3 load + 2 confronti + 1 somma) è letto a ogni op fuso in place su Long. Un flag «gc idle» mantenuto dai siti che notano/demotono/abilitano lo ridurrebbe a 1 load. PRIMA di scrivere la leva si misura il suo TETTO con un mock: quanto vale il predicato intero.
2. BRACCI (tutti dallo STESSO sorgente 883cb598 = braccio C = pin s175 a contenuto): **A = ab-out/phpr-C** (b6c4b5876971d76d, misurato S-174), **Z = pin s175** (5de14d6856d760a8: gemello a contenuto ⇒ |A−Z| = controllo NULLO), **B = MS** (predicato sempre vero nel helper `sweep_skip_next`: ai siti fusi lo Sweep è saltato SENZA leggere il predicato = tetto di un flag a costo zero; SCORRETTO fuori dal giudice), **C = M3** (SOLA 3ª clausola `!gc_enabled || radici < bound` tolta = quota di quelle 2 load + somma + confronto; SCORRETTO sotto pressione GC). Binari MS/M3 = quelli di s175-mutante-gc.sh (hash nel suo verdetto). Sui giudici arith-dq/prop-dq i mutanti sono a parità (nessun distruttore, radici < bound): parità contro l'oracle verificata dal copione (rc=2 altrimenti).
3. GIUDICI: arith-dq (1 Sweep/iter fuso) e prop-dq (2 Sweep/iter fusi) ENTRAMBI a nomina; R=5, rotazione dei 4 bracci, floors per binario, mediane per colonna e appaiate, SL in-run |A−Z|, banda tra run vs C di sw2 (20,92 / 36,47); copione s175-ab-mock.sh = COPIA DICHIARATA di s174-ab-leva-bis.sh (manifest s175-ab-mock-copia.diff). ATTESE: arith D_B ∈ [0,5;3] ns/iter (5-6 istruzioni scalari su ~21 ns/iter), prop D_B ∈ [1;6]; D_C ≤ D_B su entrambi (la 3ª clausola è un sotto-costo).
4. ESITI (mai promozione: B e C sono mutanti): **KILL-MOCK** = D_B < max(1, rumore, SL) su ENTRAMBI ⇒ il predicato costa sotto la risoluzione ⇒ leva flag gc-idle MORTA (rc=4, non si scrive); **tetto NOMINATO** (D_B ≥ max(4, rumore, SL) con entrambi gli stimatori su almeno un giudice) ⇒ la leva si disegna in S-176 col suo criterio (rc=0); **solo direzione** (rc=3) ⇒ il flag da solo non è nominabile: ammesso SOLO come componente di una leva più larga (es. col census «op in place + Sweep» p.3b).
5. IGIENE: dopo la coppia (rimisura.done rc=0), lancio s175-lancio-mock.sh (quiete + anti-flare, poi s175-ab-mock.sh); lock s175; nessuna build durante la misura; esiti da ab-out/mock.rc e s175-mock-verdetto.out.
