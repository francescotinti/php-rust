# S-170 criterio-b — residuo ~17,5 ns/iter di BinarySCSCDst a BRACCI (criterio p.6, aperto DOPO m8/m9 nominati); PRE-registrato PRIMA di build/misura; SOLA MISURA
1. OGGETTO: 4 bracci sul handler fuso `Op::BinarySCSCDst` (driver arith-dq: tupla Mul/Shr/Sub/Add su Long), ciascuno vs A=m0 (36d73812): **m10 «guardie»** = le due `!matches!(lv, Undef|Ref)` davanti ai `binary_fast(opa/opb)` RIMOSSE (per Mul/Shr `binary_fast` ha solo arm Long/Long: semantica del driver preservata; fuori dal driver NON garantita); **m11 «store magro»** = `reg_store_slot` sostituito, quando `slots[dst]` è Long e il risultato è Long, da scrittura IN PLACE del payload (niente typed_refs check, `store_slot`, `gc_note`, Result/`?`), altrimenti corpo originale; **m12 «funnel i64»** = catena Long INTERA su `i64` (slot la/lb/l Long + const Int + tupla del driver ⇒ `checked_mul`/`>>`/`checked_sub`/`checked_add` senza Zval temporanei, senza `binary_fast` Option<Zval>×4, senza `to_zval`×2, senza `read_slot` clone, senza guardie; su overflow/shift fuori range/tipi diversi cade al corpo ORIGINALE; guardia tupla TENUTA = +0,4 conservativo dichiarato, S-169) e store originale; **m13 «corpo magro totale»** = m12 + store in place (m11) + bounds ELISI (`get_unchecked` su frames/slots/consts nel solo fast path; unsafe: braccio di MISURA, MAI promuovibile). Patch `s170-m10..m13.patch`; dump del loop dq DEVE restare CmpJmpSC · BinarySCSCDst · Sweep · IncDecSlotJmp.
2. GIUDICE: **arith-dq** N=250M (`wp164-harness/arith-dq.php`) = bersaglio; **guardia arith-e2** a sola-regressione (|e2B−e2A| ≤ max(4, rumore): i bracci non toccano i 2 op del controllo-loop ⇒ attesa e2 INVARIATA; una variazione firmata su e2 = layout, si dichiara). Copione `s170-ab-dq.sh` = COPIA DICHIARATA di `s170-ab-mock.sh` con giudice/guardia scambiati di nuovo (dq↔e2), manifest v3. R=5 ABAB, ns/iter=(med raw−floor)/N, rumore drop-1; nominata D≥4; direzione firmata |D|≥max(rumore, 0,94).
3. ATTESE pre-registrate (S-169: corpo BinarySCSCDst ≈27,4 = consts 4,7 + BinOp 5,4 + ~17,5 non nominati): m10 guardie ≤ 1 (sotto pavimento, direzione al più); m11 store: 2-5; **m12** = consts+BinOp+funnel+read_slot+guardie ⇒ atteso ≥ 10,1 (m1+m2 grezzi) — la parte «funnel+read_slot+guardie» = **D_m12 − D_m123 (8,00, S-168)** è CIFRA DA DIFFERENZA tra bracci dichiarata, non ripartita; **m13** = tetto della campagna «corpo del handler» sul fuso: residuo = dq_m13 − e2 − Sweep 2,9 − dispatch 1,75 = «accesso slot + resto» NON nominato. **KILL pre-registrato**: D_m13 < 15 (meno della metà del corpo 27,4 + m4b) ⇒ il corpo magro NON basta: la campagna sul fuso non regge da sola, va a R4 con le cifre; D_m13 ≥ 20 ⇒ il corpo è ≥73% del residuo e la fetta «forma sigillata Long» (regola 8) diventa la prossima leva DA MISURARE come codice (S-171), non da spedire.
4. ORDINE: xctrace c0pos FINITO (CPU pulita) → chain m10→m11→m12→m13 → dump dq (4 op) → --braccio s170-m10..m13 → disasm bl → A/B m10, m11, m12, m13 R=5 (quiete s129 prima di ognuno) → sintesi in `s170-verdetto-b.out`. IGIENE come criterio p.5 (lock s170 vivo, esiti da file, output di run fuori repo). Nessun codice di leva in S-170.
