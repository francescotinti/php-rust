# S-170 criterio — DELIBERA R4 dell'utente = opzione (i) «campagna corpo del handler»; PRE-registrato PRIMA di build/misura; SOLA MISURA, nessun codice di leva
1. OGGETTO: mock «handler banale MAGRO» sui 2 op del controllo-loop E2 (dump pin: `CmpJmpSC{Lt}` + `IncDecSlotJmp`, 2 op/iter). **m8** (`s170-m8.patch`, safe Rust): IncDecSlotJmp = `wrapping_add` IN PLACE sul Long (niente checked_add, niente riscrittura del Zval intero, niente Result/`?`); CmpJmpSC = const `Const::Int` letto SENZA `to_zval`, slot Long SENZA guardia Undef/Ref (ridondante col match Long), confronto a `bool` diretto SENZA `Zval::Bool`/`to_bool`/diags, SENZA `?`; fuori dal cammino Long ENTRAMBI cadono al corpo originale (semantica preservata sul driver; NON preservata sull'overflow: braccio di MISURA). **m9** (`s170-m9.patch`) = m8 + bounds ELISI con `get_unchecked` su `frames[top]`, `slots[i]`, `consts[cidx]` (unsafe: braccio di MISURA, MAI promuovibile, RULEBOOK §0). Il dump del loop di m8/m9 DEVE restare `CmpJmpSC`+`IncDecSlotJmp` (2 op, nessun Nop).
2. GIUDICE: **arith-e2** (`wp168-harness/arith-e2.php`, N=250M, loop nudo) = bersaglio; **guardia arith-dq** bilaterale a SOLA-REGRESSIONE (rc=5 se dqB−dqA > max(4, rumore); un miglioramento su dq è atteso — stessi 2 op nel suo loop — e si REGISTRA, non si conta). A = **m0 RICOSTRUITO in S-170** a path `/private/tmp/s170-mock-src` con la ricetta di `s169-mock-build.sh` (hash misurato; == 36d73812 si dichiara «riproducibile»; ≠ ⇒ braccio nullo di S-170 e la coppia m0-s170 vs m0-s168 R=5 su E2 = banda-copia, letta prima dei mock). R=5 ABAB alternato, ns/iter=(med raw−floor)/N, rumore drop-1; **nominata** D≥4; **direzione firmata** |D|≥max(rumore, 0,94). Copione `s170-ab-mock.sh` = COPIA DICHIARATA di `wp169-harness/s169-ab-mock.sh` (giudice E2↔dq scambiati, lock per TOKEN, `[ -s ]`, parità vs ATTESO) con manifest `s170-ab-mock-copia-v3.diff`.
3. ATTESE (pre-registrate): corpo banale misurato 5,6 ns/op (S-169), bersaglio dell'utente ~2 ns/op ⇒ e2 14,7 → ~7,5, D_m9 ≈ 7. Letture: **D_m8 ≥ 4** ⇒ «guardie + materializzazione Zval + Result» NOMINATE sui handler banali; **D_m9 − D_m8 ≥ soglia_dec** ⇒ bounds a direzione firmata (cifra = differenza tra bracci, non ripartita); **residuo** = e2_m9 − 2×1,75 = «accesso slot (doppia indirezione) + dispatch» NON nominato. **KILL pre-registrato**: **D_m9 < 4** ⇒ l'ipotesi «il divario è il corpo di ogni handler» è REFUTATA sui banali (il costo sta nell'accesso slot/dispatch, tetto 1,75/op): la campagna (i) NON si apre, R4 torna all'utente con le cifre. **Nessuna cifra da contrasto tra bracci diversi oltre D_m9−D_m8** (REGOLE §3).
4. PARITÀ vs ATTESO (az.rev. S-169 #3): output di A e B su dq ed e2 confrontati con `ab-out/expected-{dq,e2}.out` generati dall'ORACLE nel copione stesso (`[ -s ]` su driver e attesi; rc=2 se ≠ atteso, non solo se A≠B). Lock per TOKEN: `grep -qw s170`. **copia-gate v3 per TOKEN** (`scripts/copia-gate-v3.sh`): ogni token `s1XX`/`pair1XX`/`wp1XX-harness` ≠ famiglia attesa è residuo ANCHE su righe che contengono il token corrente; manifest rigenerati per ab-mock, mock-build, xctrace.
5. IGIENE: lock `/private/tmp/phpr-measure.lock` «s170» creato DOPO il job CI in corso (6845173c; coda potata a 1 per delibera utente, backup `phpr-ci/queue-pruned-s170/`) e RIMOSSO come passo esplicito di chiusura; quiescenza s129 prima di ogni A/B; target dedicato `/private/tmp/s170-mock-tgt` e `s170-mock-bin` rimossi in epilogo; esiti solo da file; output di run mai nel repo; bracci a stash via `pin-phpr.sh --braccio s170-m8/m9`; disasm bl `s160-disasm-bl.sh` m0 vs m8/m9 (delta atteso PICCOLO e negativo).
6. ORDINE: chain m0→m8→m9 (detached) → dump loop → --braccio → disasm → A/B m0-s170 vs m0-s168 (solo se hash ≠) → A/B m8 R=5 → A/B m9 R=5 → sintesi meccanica in `s170-verdetto.out` → SOLO SE m8/m9 leggibili: criterio SEPARATO `s170-criterio-b.md` per il residuo ~17,5 di BinarySCSCDst a bracci (guardie · read_slot clone · store+gc_note · funnel) → mutante c0 POSITIVO (`arith-c0pos.php`: `str_repeat` L1, throughput retiring; attesa: sale c0 e NON c1/c2 su ENTRAMBI i motori) con `s170-xctrace.sh` copia dichiarata + purge ktrace.
7. VERDETTO meccanico (`s170-verdetto.out`: A, B, D, rumore, soglia, soglia_dec, e2/dq, esito per TAG); revisione a fine sessione = lente MISURA (rotazione: S-168 semantica, S-169 processo). Kill/soglie ⚖️ S-167 invariati; F1/F2 restano SOSPESE.
