# s121-criterio-st1.md — leva L-ST1 (classifica C-lite: str +3 alloc/iter, voce 3), PRE-registrato

1. Leva: args-Vec per chiamata di `Op::CallBuiltin` (split_off in vm/mod.rs:8188 via run.rs:3331; 32 B esatti = 1 dei 5 alloc/iter di str — somma istruttoria: 2 concat + 1 args-Vec + 2 substr) → scratch riusabile sul `Vm` (`mem::take` → drain-into → clear+restore dopo `run_value_builtin`; i rami freddi `continue`/`?` lasciano cadere il Vec, re-entrancy coperta dal take). Runtime-only (vm/mod.rs, vm/run.rs), php-types INTOCCATO; pavimento PhpStr 2-alloc FUORI da questa leva (NEXT_SESSION); composta su HEAD, target canonico via build-script, MAI il pin.
2. Meccanismo (firma, PRIMA del tempo): census C-lite (patch wp119 + st1.patch, target separato) su str: alloc/iter 5,00 → atteso 4,00 ESATTO (free idem); nessuna categoria non-bersaglio con alloc/iter in AUMENTO (arr atteso in CALO — builtin by-value anche lì: si registra, non promuove); R=2, conteggi identici; stesso denominatore dei raw s119 (lezione verdetto v2).
3. Giudice del tempo: forma s120-ab (floor med3 per-binario, famiglia 1,3×min, coppie extra ≤6); A = pin s120 (stash phpr-s120-re1 == 885d2c64), B = candidato (stash phpr-s121-st1); N dal sorgente; R=5 ABAB.
4. Bersaglio str — segno atteso D_med > 0 (B più veloce); soglia promozione: D_med ≥ max(4,00 pavimento REGOLE §3; banda-zavorra-str N=3 = 2,50 — zavorra-out, debito NEXT_SESSION assolto, SOSTITUISCE banda-v2 7,50 sul bersaglio; 2×spread_A; 2×quanto).
5. Guardie non-bersaglio a SOLO-REGRESSIONE: arith 0,80 · prop 3,33 · calls 0,50 · arr 6,67 (banda-v2) · re 10,00 (banda micro N=2, forma S-120); soglia = −max(2×spread; banda; 2×quanto), tie ⇒ tiene; morso calls dentro 0,50 si REGISTRA.
6. Admission: 6/6 parità output + dump INTERO byte-id — deroga NOMINATA forma S-118 (leva runtime-only a emissione INVARIATA), citata qui.
7. PRECEDENZA smoke↔banda (az. rev. S-120 #3, decisa QUI e PRIMA): smoke R=2 di guardia; early-stop SOLO se (a) regressione concorde ≤ −1,00 su TUTTE le coppie della categoria E (b) |D_med smoke| > banda della categoria (p.5). Solo (a) senza (b) ⇒ si REGISTRA e il full giudica. (a)+(b) ⇒ bisezione, NIENTE full — nessuna deroga in corsa.
8. Giudice del rischio (promozione): batteria `cargo test --release` rc da FILE · corpus-gate canonico ×2 modi (fail-set 1415 per NOME) · fixture bilaterali · pin SOLO via `scripts/pin-phpr.sh` (ricetta A′), churn dichiarato · re-pin php-server in chiusura (la build di promozione rilinka i fratelli — lezione S-120).
9. Bisezione: leva a UN componente ⇒ meccanismo mancato o guardia sfondata = REVERT secco (ripristino verificato al byte), nessun giro.
10. Cifre solo dai `.out` (`s121-st1-verdetto.out`); strumentazione MAI nei sorgenti del pin.
