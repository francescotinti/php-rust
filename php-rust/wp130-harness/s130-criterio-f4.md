# Criterio S-130 — RIENTRO F4 prelude-gate (emenda del criterio S-129 per riesecuzione, rev. S-112; commit PRIMA di ogni run)

1. **Forma INVARIATA**: codice = f4143a6 riapplicato SENZA riscrittura (revert del revert); census F4 11/11 resta valido (nessun cambio di forma, s129-ab-f4-lettura.md p. d).
2. **Giudice** objdatains, A = stash pin s127b (ccb63dca), B = build canonica post-reapply (hash a verbale; atteso 0fdf1c49 se riproducibile al byte); segno: B CALA; smoke R=2 con early-stop a segno opposto → conferma R=5.
3. **EMENDA (a) — rumore robusto**: per ciascuna serie netta ns/iter, con R≥4 si scarta il SINGOLO campione più lontano dalla mediana della propria serie (drop-1 SIMMETRICO, stesso trattamento su A e su B) e si prende il range dei restanti; rumore = max(rangeA′, rangeB′); con R≤3 range pieno. Soglia giudice = max(4 ns/iter, rumore, SL prop 0,80). D = medianaA − medianaB sulle serie PIENE.
4. **EMENDA (b) — bande fondate**: banda = range drop-1 delle 7 gambe A (pin s127b) dei verdetti COMMITTATI S-129 (smoke R=2 + conferma R=5, stessa ricetta, raw datati 2026-08-11): **objmap 6,7 · objalloc 6,7 · objchurn 13,3** ns/iter; soglia_reg = −max(4, banda). Le sei storiche restano sulle SL committate (max s123/s125). Niente default 4 sulle micro-ORM.
5. **Invarianti**: stesso giudice, stesse guardie SOLO-REGRESSIONE, stesso segno, stessi N e sorgenti; arbitro = `s130-ab.sh` (copia dichiarata di s129-ab.sh con le SOLE emende 3–4); quiescenza = gate SEPARATO `wp129-harness/s129-quiescenza.sh` prima di ogni run; rc autoritativo = SOLO `ab-out/<TAG>.rc`.
6. **Promozione** SOLO se giudice sopra soglia e guardie ok: catena `wp129-harness/s129-promozione.sh` INVARIATA (batteria→pin→corpus→fixture→micro→ORM→hk→server). Esito avverso ⇒ verdetto committato PRIMA di ogni lettura/emenda; revert che DEVE riprodurre ccb63dca al byte.
