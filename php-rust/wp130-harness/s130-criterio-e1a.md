# Criterio S-130 — sonda E1a: resolve_prop_access MISURATA, non residuale (az.rev. S-129 #1; commit PRIMA del run)

1. **Oggetto**: sul pin s130 (0fdf1c49) misurare DIRETTAMENTE il costo delle chiamate `resolve_prop_access` nel cammino `Op::FieldAssign` — finora E1 era solo il residuo E−E2 (~155 ns) con «resolve-per-NOME» come indizio da disasm. Esito: UB della leva resolve-once (E1 di S-131) FONDATO su misura.
2. **Sonda**: build EMENDATA (patch `time-probes-e1a.patch` scritta col tree e ripristinata; modulo `wp130-harness/timeprobes.rs` via `#[path]`; target separato `phpr-time-l1-target`; tree verificato pulito post-build; parità stdout vs pin s130 per ogni micro). Segmenti: 0=TOT arm · **1=E1a dentro `resolve_prop_access` (TUTTE le chiamate del processo: nel micro-loop provengono dal solo cammino FieldAssign — i conteggi c1 lo certificano, ε di setup dichiarato dal k misurato)** · 4=E field_set · 5=E2 walk in prop_step · 7=CAL sottratta per nota; `cntvct_el0` mrs senza isb (dichiarato).
3. **Categorie e ricetta**: {objalloc (0 stmt: zeri sintetici dichiarati, lezione S-129), objdatains, p2-append, p5-two, p6-overwrite}; N=3e6 dal sorgente; R=3, mediane + spread; conteggi per segmento DETERMINISTICI tra R (scarto ⇒ indagine); k = c1/N a verbale (atteso ~intero + ε).
4. **Lettura**: quote = MODELLO (build emendata ⇒ nessuna cifra promuovibile); risoluzione = max(4 ns/iter, spread); **UB resolve-once (collasso a UNA resolve per statement-sito) = E1a·(k−1)/k**; E1a confrontata con E−E2 di S-129 (155) SOLO come ordine (pin diverso: s127b vs s130; E ed E2 non toccati da F4).
5. **Igiene**: quiescenza gate SEPARATO prima del run; nessun edit .rs fuori dalla patch; run sequenziali foreground-of-script; output non tracciati; rc autoritativo = SOLO `tempo-out/tempo-e1a.rc`.
