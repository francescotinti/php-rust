# Criterio PRE S-117 — spike A′ stadio-1 (committato PRIMA di toccare Cargo.toml; concilio S-116 vincolante)

1. **Oggetto**: `[profile.release] lto="fat" + codegen-units=1` nel Cargo.toml di root; sorgente = HEAD 22bc82a (verificato: zero diff su src/Cargo.* vs sorgente pin c04c273 ⇒ l'A/B isola la SOLA pipeline); order_file ld64 solo se lo stadio-1 chiude entro il timebox (½ sessione); PGO = stadio-2 con criterio proprio.
2. **Determinismo**: 2 build a sorgente invariato, target FRESCO ciascuna (`CARGO_TARGET_DIR=/Volumes/Extreme Pro/Claude/phpr-s117-aprime-target`, `CARGO_INCREMENTAL=0`) → shasum-16 di phpr IDENTICO; riproducibilità rotta ⇒ KS-A immediato (niente A′, pipeline vecchia, si tiene solo ciò che è gratis).
3. **Gate PIENI sul candidato** (rc SOLO da FILE, esiti appesi dallo script): batteria `cargo test --release` rc=0 + inventario per NOME identico a S-116 · corpus `--isolate --list-fails` ×2 modi col phpt-runner A′: fail-set per NOME == congelato s109 (1415) e chunk off↔on byte-id · smoke parità arith 2 modi vs oracle (ricetta pin-phpr.sh).
4. **Micro R=5 su A′** = NUOVE baseline (rapporti pubblicati, N emessi dal sorgente); l'uplift per KS-A esce da A/B interleaved ABAB pin↔A′ stessa sera sui sei giudici, ns/iter al netto dei pavimenti PER-binario; uplift% per categoria = (D_pin−D_A′)/D_pin·100, giudice = MEDIANO delle 6; il numero serve SOLO a KS-A, non è magnitudine di leva.
5. **KS-A** (dissenso E/O sciolto QUI con la logica della sintesi vincolante): scatta se uplift mediano <2% **E** banda_new max >5 ns/iter (banda_new dalla ri-banda: ≥2 nulle micro + held-out N≥2) ⇒ A′ archiviata, treno B su pipeline vecchia, C-lite anticipata a S-118. Nessun verdetto «ripara-metro» né di leva prima della ri-banda; «metro riparato» = banda_new max ≤5.
6. **Workload PGO stadio-2** (pre-registrato ora; dissenso Klabnik/Pedersen sciolto con la sintesi §3): WP request-loop + corpus misto, DEVE includere request_end/teardown/distruttori; MAI le sei micro né gli held-out; profdata hashato e CONGELATO, stesso profdata per entrambi i bracci di ogni A/B futuro.
