# S-117 criterio PRE — RIGIUDIZIO L-A sotto pipeline A′ (ordine concilio p.3): riesecuzione INTERA del criterio S-116 con bande NUOVE (s117-banda-v2.md) e le sole modifiche (f)-(h); committato PRIMA di build/misura del candidato

1. Oggetto: rigiudizio L-A «trigramma prop» sotto A′ — è ANCHE il test di separazione della tassa calls (R5-iii Gregg): tassa-di-questa-build vs tassa-di-codice. A = `phpr-s117-aprime` (8135dcf8, CONSERVATO). B = `phpr-s117-la` da COSTRUIRE: tree pulito verificato → `git cherry-pick -n 2c18b2e` (MAI checkout parziale) → build ricetta A′ (target dedicato, INCREMENTAL=0, SOURCE_DATE_EPOCH=0) → conserva → `git reset --hard HEAD` + verifica tree pulito. Smoke R=2 early-stop prima del full.
2. Banda micro = **banda-v2 N=2 su A′**: arith 0,80 · prop 3,33 · calls 0,50 · str 7,50 · arr 6,67 · re 0,00. Soglia promozione prop = max(4 ns/iter; spread_A depurato; banda(prop)=3,33), segno + su TUTTE le coppie in famiglia (≥5). Guardie non-bersaglio SOLO-REGRESSIONE: sfondano se D_med < −max(2×spread_A dep; banda(cat); **2×quanto(cat)**), quanto(cat)=0,01/N·10⁹ ns/iter — modifica (f), az.3 rev. S-116: una guardia non può essere più fine del cronometro.
3. Modifica (g): pavimento per categoria = **MEDIANA di 3** misure per binario (non 1) — az.3 rev. S-116, il floor a campione unico trasla tutte le D.
4. Held-out R=5 (poly·err·wploop): REGRESSIONE se net_cand (2 dec) STRETTAMENTE > baseline **A′** (poly 9,44 · err 2,99 · wploop 6,38 s) + max(2×spread; 0,12 s; banda_ho **N=2**: 0,01/0,06/0,08), spread ≝ max(spread_pin, spread_cand), ENTRAMBI pubblicati — modifica (h): baseline e banda dalla pipeline A′, N=2, spread_pin pubblicato (chiude l'azione 5 rev. S-115).
5. Tie ESATTI su valori a 2 decimali (=soglia ⇒ PASS promozione; =soglia ⇒ tiene guardia; =limite ⇒ ok held-out). rc di OGNI gate in FILE (`la-out/*.rc`), esiti appesi al verbale `s117-la-verdetto.out` DALLO script.
6. Resto INVARIATO da s116-criterio-la.md p.6: famiglie 1,3×min con esclusione per NOME + extra max +6; <5 in famiglia ⇒ MISURA_INVALIDA, rerun 1 volta poi R=7; parità stdout+stderr per categoria PRIMA del cronometro; admission 5 casi (hit+4miss, wp115-harness/admission-cases/) con parità output+rc, dump {main} identico A↔B, bigramma nel {main} di A. Batteria: il codice L-A ha batteria 1742/0/2 (S-114) e la pipeline A′ ha batteria 1742/0/2 (S-117) — la batteria sul TREE L-A×A′ si esegue SOLO alla promozione (§7), deroga DICHIARATA per l'A/B.
7. Verdetto: PROMOZIONE solo se admission+micro+guardie+held-out reggono TUTTI. Se PROMOSSA: cherry-pick 2c18b2e committato + batteria+corpus ×2 sul tree promosso + pin SOLO via `scripts/pin-phpr.sh` (ricetta A′) §6 PIENO. Se NON promossa: esito coi numeri, candidato conservato, release non toccata. Tassa calls: entro banda_new(calls-guard) ⇒ ipotesi layout FIRMATA (la tassa S-114..116 era di quella build); sfondamento ≥2 quanti ⇒ tassa di CODICE stabilita (N layout=2).
8. Caveat dichiarati: guardie str/arr poggiano su bande larghe (7,50/6,67) — poco diagnostiche; banda held-out N=2 ma da zavorre di taglia diversa dalla leva; famiglia veloce ≈ P-core resta ipotesi nominata.
