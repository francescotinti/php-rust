<?php
// A-TH8 fixture (S-78.1.4): runtime fatal — uncaught Error. The HTTP body must
// be byte-identical to the oracle CLI stdout (render_fatal uncaught form with
// Stack trace + "thrown in"), status 500, and the worker must survive.
echo "pre\n";
nope_missing_fn();
