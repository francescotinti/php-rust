2,5c2,5
< # s134-submicro.sh — rimisura micro-ORM sul pin s134 POST-leva-IC-non-plain.
< # COPIA DICHIARATA di wp133-harness/s133-submicro.sh (collaudo: copia-gate +
< # manifest s134-submicro-copia.diff): soli cambi = pin atteso
< # s134 (hash dal pin promosso, iniettato via PIN_S134), OUT/VERD in wp134-harness. Metodo INVARIATO
---
> # s135-submicro.sh — rimisura micro-ORM sul pin s135 POST-leva-AP1.
> # COPIA DICHIARATA di wp134-harness/s134-submicro.sh (collaudo: copia-gate +
> # manifest s135-submicro-copia.diff): soli cambi = pin atteso
> # s135 (hash dal pin promosso, iniettato via PIN_S135), OUT/VERD in wp135-harness. Metodo INVARIATO
14c14
< VERD="$H/s134-submicro-verdetto.out"
---
> VERD="$H/s135-submicro-verdetto.out"
21c21
< [ "$(shasum -a 256 "$PHPR" | cut -c1-16)" = "${PIN_S134:?PIN_S134 richiesto}" ] || { echo "rc=9 pin!=s134" > "$OUT/micro.done"; exit 9; }
---
> [ "$(shasum -a 256 "$PHPR" | cut -c1-16)" = "${PIN_S135:?PIN_S135 richiesto}" ] || { echo "rc=9 pin!=s135" > "$OUT/micro.done"; exit 9; }
30c30
< echo "== s134 micro-ORM POST-leva-IC-non-plain sul pin s134 (metodo s127-criterio-submicro.md INVARIATO) =="
---
> echo "== s135 micro-ORM POST-leva-AP1 sul pin s135 (metodo s127-criterio-submicro.md INVARIATO) =="
