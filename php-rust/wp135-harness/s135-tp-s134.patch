diff --git a/php-rust/crates/php-runtime/src/vm/arrays.rs b/php-rust/crates/php-runtime/src/vm/arrays.rs
index 35afe6f..4225c67 100644
--- a/php-rust/crates/php-runtime/src/vm/arrays.rs
+++ b/php-rust/crates/php-runtime/src/vm/arrays.rs
@@ -1232,9 +1232,12 @@ impl<'m> Vm<'m> {
         let mut aa = None;
         let mut refw = None;
         let result = path_apply(cell, &keys, last, &mut self.diags, &mut dropped, &mut aa, &mut refw);
+        // S-135 tp: seg 6 = gc_note del displaced.
+        let t6 = super::s135tp::t0(6);
         if let Some(d) = &dropped {
             self.gc_note(d);
         }
+        super::s135tp::add(6, t6);
         let mut result = result?;
         // H-70.1 drain: a leaf write whose target ref-cell was mid-write on
         // this very statement (a reference cycle) parks here; every walk
diff --git a/php-rust/crates/php-runtime/src/vm/mod.rs b/php-rust/crates/php-runtime/src/vm/mod.rs
index a561779..b34125f 100644
--- a/php-rust/crates/php-runtime/src/vm/mod.rs
+++ b/php-rust/crates/php-runtime/src/vm/mod.rs
@@ -84,6 +84,11 @@ mod tidy;
 mod xslt;
 mod tokenizer;
 mod websapi;
+// S-135 sonda conteggi (build emendata, worktree): MAI nel pin.
+#[path = "../../../../wp135-harness/s135probes.rs"]
+pub(crate) mod s135probes;
+#[path = "../../../../wp135-harness/s135tp.rs"]
+pub(crate) mod s135tp;
 use arrays::*;
 use calls::*;
 use oop::*;
@@ -11218,6 +11223,8 @@ impl<'m> Vm<'m> {
     /// dynamic property passes the value through unchanged. The strict-types mode of
     /// the *writing* frame governs coercion, mirroring the parameter binder.
     fn coerce_typed_prop_write(&mut self, ocid: ClassId, name: &[u8], value: Zval) -> Result<Zval, PhpError> {
+        // S-135 sonda: seg 10 = coercizioni typed (canale pagato).
+        s135probes::count(10);
         let Some((decl, hint)) = prop_type_decl(&self.classes, ocid, name) else {
             return Ok(value);
         };
@@ -17125,6 +17132,7 @@ enum PathWalk {
 /// recursion across plain containers keeps the nested borrows well-formed.
 fn path_apply(cell: &mut Zval, keys: &[Zval], last: Last, diags: &mut Diags, dropped: &mut Option<Zval>, aa: &mut Option<PathAa>, refw: &mut Option<RefLeaf>) -> Result<Zval, PhpError> {
     let mut walk = path_walk(cell, keys, 0, last, diags, dropped, aa, refw)?;
+    // (S-135 tp: seg 4/5 sono in apply_last, sotto.)
     loop {
         walk = match walk {
             PathWalk::Done(v) => return Ok(v),
@@ -17191,6 +17199,8 @@ fn path_walk(cell: &mut Zval, keys: &[Zval], i: usize, last: Last, diags: &mut D
 /// most ONE per path op — the `Set`/`OpSet` arms — WP-32: was a Vec that
 /// allocated on every displacing write just to hold it).
 fn apply_last(parent: &mut Zval, last: Last, diags: &mut Diags, dropped: &mut Option<Zval>, aa: &mut Option<PathAa>, refw: &mut Option<RefLeaf>) -> Result<Zval, PhpError> {
+    // S-135 tp: seg 4 = apply_last intero (chiuso sul braccio Set).
+    let t4 = s135tp::t0(4);
     // A string parent takes the byte-offset write path (`$s[0] = 'X'`), whose
     // expression value is the written byte; the other leaf ops are the PHP
     // errors for string offsets.
@@ -17245,7 +17255,11 @@ fn apply_last(parent: &mut Zval, last: Last, diags: &mut Diags, dropped: &mut Op
             // now be unreachable). `Busy` = the ref element's cell is
             // mid-write on this very statement (cycle, H-70.1): park the
             // store for `path_op`'s drain, outside every walk guard.
-            match arr.set_returning_displaced(k, value.clone()) {
+            // S-135 tp: seg 5 = set_returning_displaced.
+            let t5 = s135tp::t0(5);
+            let lw = arr.set_returning_displaced(k, value.clone());
+            s135tp::add(5, t5);
+            match lw {
                 LeafWrite::Done(Some(d)) => {
                     debug_assert!(dropped.is_none());
                     *dropped = Some(d);
@@ -17256,6 +17270,7 @@ fn apply_last(parent: &mut Zval, last: Last, diags: &mut Diags, dropped: &mut Op
                     *refw = Some(RefLeaf::Set { cell, value: val });
                 }
             }
+            s135tp::add(4, t4);
             Ok(value)
         }
         Last::Append { value } => {
diff --git a/php-rust/crates/php-runtime/src/vm/oop.rs b/php-rust/crates/php-runtime/src/vm/oop.rs
index 5054447..c50456e 100644
--- a/php-rust/crates/php-runtime/src/vm/oop.rs
+++ b/php-rust/crates/php-runtime/src/vm/oop.rs
@@ -425,6 +425,8 @@ pub(super) enum PropAccess<'a> {
 ///    an invisible private declared by the object's own class (or an invisible
 ///    protected) is `Denied`.
 pub(super) fn resolve_prop_access<'a>(classes: &[&'a CompiledClass], obj_class: ClassId, name: &[u8], scope: Option<ClassId>) -> PropAccess<'a> {
+    // S-135 sonda: seg 1 = TUTTE le resolve.
+    super::s135probes::count(1);
     if let Some(s) = scope {
         if let Some(pi) = prop_info(classes, s, name) {
             if pi.visibility == Visibility::Private
diff --git a/php-rust/crates/php-runtime/src/vm/run.rs b/php-rust/crates/php-runtime/src/vm/run.rs
index 6d203a1..8b2ed65 100644
--- a/php-rust/crates/php-runtime/src/vm/run.rs
+++ b/php-rust/crates/php-runtime/src/vm/run.rs
@@ -643,6 +643,8 @@ impl<'m> super::Vm<'m> {
         ic: &crate::bytecode::PropIc,
     ) -> Result<(), PhpError> {
         let cur = self.frames[top].class;
+        // S-135 sonda: seg 0 = entrate prop_set_entry.
+        super::s135probes::count(0);
         // H-C1b (S-101): move the owned handle (see PropGet).
         // INV-RECV-1 holds here too: `target` is owned through
         // lazy_prop_access and every write path below; the old
@@ -710,6 +712,12 @@ impl<'m> super::Vm<'m> {
                         }
                 };
                 if hit {
+                    // S-135 sonda: seg 2 = hit NP, seg 11 = hit plain.
+                    if raw & crate::bytecode::PropIc::NP != 0 {
+                        super::s135probes::count(2);
+                    } else {
+                        super::s135probes::count(11);
+                    }
                     // S-134 IC non-plain (s134-criterio-icnp p.1): il ramo TY
                     // coercisce nello stesso ordine del cammino pieno (coerce
                     // PRIMA della scrittura, stessa esposizione a __toString)
@@ -803,6 +811,8 @@ impl<'m> super::Vm<'m> {
             // An enum case is immutable: every property is readonly and
             // no dynamic property may be created (step 23).
             {
+                // S-135 sonda: seg 6 = enum-check borrow.
+                super::s135probes::count(6);
                 let ob = o.borrow();
                 if ob.info.is_enum_case {
                     let cls = String::from_utf8_lossy(ob.class_name.as_bytes()).into_owned();
@@ -823,6 +833,8 @@ impl<'m> super::Vm<'m> {
             // sotto con esito negativo — condizione di fill del ramo NP.
             let mut np_fillable = false;
             if !self.hook_guarded(oid, name) {
+                // S-135 sonda: seg 5 = hook-lookup eseguito.
+                super::s135probes::count(5);
                 if let Some(func) = self.prop_hook(cid, name, true) {
                     if !DISCARD {
                         self.frames[top].stack.push(value.clone());
@@ -849,6 +861,8 @@ impl<'m> super::Vm<'m> {
             // before, nothing between them mutates class or props.
             let ocid = o.borrow().class_id as usize;
             let access = resolve_prop_access(&self.classes, ocid, name, cur);
+            // S-135 sonda: seg 4 = magic-probe.
+            super::s135probes::count(4);
             if let Some((defc, midx, oid)) =
                 self.magic_applies_resolved(o, name, &access, MagicKind::Set, b"__set")
             {
@@ -892,6 +906,8 @@ impl<'m> super::Vm<'m> {
             // visibility excludes this scope cannot be assigned (the
             // readonly path below keeps its own message shapes).
             if declared_slot {
+                // S-135 sonda: seg 7 = asym check.
+                super::s135probes::count(7);
                 if let Some(err) = asym_write_error(&self.classes, cur, ocid, name, "modify") {
                     return Err(err);
                 }
@@ -901,6 +917,8 @@ impl<'m> super::Vm<'m> {
             // first, matching PHP). A permitted first initialisation is
             // recorded so any later write fatals.
             let ro_decl = if declared_slot {
+                // S-135 sonda: seg 8 = readonly decl-lookup.
+                super::s135probes::count(8);
                 prop_readonly_decl(&self.classes, ocid, name)
             } else {
                 None
@@ -942,6 +960,8 @@ impl<'m> super::Vm<'m> {
                             } else {
                                 0
                             };
+                        // S-135 sonda: seg 3 = IC-fill NP.
+                        super::s135probes::count(3);
                         ic.fill(ocid as u32, crate::bytecode::PropIc::scope_key(cur), i | bits);
                     }
                 }
@@ -953,7 +973,8 @@ impl<'m> super::Vm<'m> {
             // `unset` is absent from the store yet is not a dynamic
             // creation when re-assigned — `declared_slot` covers it.
             if !declared_slot
-                && !o.borrow().props.contains(&key)
+                // S-135 sonda: seg 9 = deprecation props.contains.
+                && { super::s135probes::count(9); !o.borrow().props.contains(&key) }
                 && !self.allows_dynamic_props(ocid)
             {
                 let cls = String::from_utf8_lossy(&self.classes[ocid].name).into_owned();
@@ -2373,10 +2394,18 @@ impl<'m> super::Vm<'m> {
                     self.frames[top].stack.push(read_dim_nullable(&base, &key));
                 }
                 Op::AssignPath { base, nkeys, append } => {
+                    // S-135 tp (criterio s135-criterio-tempo.md): seg 0 arm
+                    // intero · 1 pop value+keys · 9 span vuoto (calibrazione).
+                    let t9 = super::s135tp::t0(9);
+                    super::s135tp::add(9, t9);
+                    let t0a = super::s135tp::t0(0);
+                    let t1 = super::s135tp::t0(1);
                     let value = self.frames[top].stack.pop().expect("AssignPath value");
                     let mut keys = self.pop_keys(top, *nkeys);
+                    super::s135tp::add(1, t1);
                     // `$o[$k] = v` / `$o[] = v` on an ArrayAccess object dispatches
                     // `offsetSet` (a single step only); the expression yields `v`.
+                    let t2 = super::s135tp::t0(2);
                     if nkeys + *append as u32 == 1 {
                         if let Some(recv) = self.as_arrayaccess(self.base_cell(*base, top)) {
                             let key = if *append { Zval::Null } else { keys.pop().expect("set key") };
@@ -2385,13 +2414,17 @@ impl<'m> super::Vm<'m> {
                             continue;
                         }
                     }
+                    super::s135tp::add(2, t2);
                     let last = if *append {
                         Last::Append { value }
                     } else {
                         Last::Set { key: keys.pop().expect("AssignPath key"), value }
                     };
+                    let t3 = super::s135tp::t0(3);
                     let result = self.path_op(*base, top, keys, last)?;
+                    super::s135tp::add(3, t3);
                     self.frames[top].stack.push(result);
+                    super::s135tp::add(0, t0a);
                 }
                 Op::AssignOpPath { base, nkeys, op } => {
                     let rhs = self.frames[top].stack.pop().expect("AssignOpPath rhs");
