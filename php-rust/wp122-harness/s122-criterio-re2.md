# s122-criterio-re2.md — leva L-RE2 (re residuo, scalino 10→9), PRE-registrato

1. Leva: il Vec `Caps.groups` di preg (1 alloc/chiamata: collect in `caps_from_locations` preg.rs:176-184, idem gli altri 5 costruttori) passa a storage INLINE `SmallVec<[Option<CapMatch>; 4]>` — niente heap per ≤4 slot (re.php ne ha 3), niente swap/take (meccanismo L-ST1 REFUTATO), niente RefCell esteso: il buffer vive DENTRO `Caps`, per valore. Campo privato, 5 punti da toccare tutti in preg.rs (mappa: annotazione :311, resize_with :312→push-loop, into_iter :2213, IndexMut :318-324, iter_mut :55 — smallvec li copre). smallvec 1.15.2 GIÀ nel lock (transitiva): diventa dep diretta di php-runtime, NESSUN crate nuovo nel grafo. Runtime-only, php-types INTOCCATO. Composta su HEAD, candidato via script, MAI il pin.
2. Meccanismo (firma, PRIMA del tempo): census C-lite (census-clite.patch + re2.patch, target separato) — re alloc/iter **10,00 → 9,00 ESATTO** (free idem 9,00); NESSUNA categoria con alloc/iter in AUMENTO; predizioni secondarie A VERBALE anche se mancate (az. rev. S-121 #3): str = 5,00 invariata · arr = 245,01 invariata · arith/prop/calls = 0,00 invariate; R=2 conteggi identici.
3. Rischio nominato del meccanismo: `Caps` per-valore cresce ~24→~176 B (mosse memcpy sul ritorno) e `Vec<Caps>` di captures_iter porta ~176 B/el: il costo delle mosse è l'anti-tesi che il TEMPO giudica; il census non lo vede.
4. Admission: 6/6 parità output + dump INTERO byte-id (deroga forma S-118: leva runtime-only a emissione INVARIATA, citata QUI).
5. Giudice del tempo: forma s122 (floor med3 per-binario, famiglia 1,3×min, R=5, coppie extra ≤6); A = pin s120 (stash phpr-s120-re1 == 885d2c64), B = candidato (stash phpr-s122-re2); N dal sorgente (arr 6M); ABAB.
6. Bersaglio re — soglia promozione: D_med ≥ +max(4,00; BL_re 5,00; 2×spread_A; 2×quanto=10,00) — soglia effettiva ≥10,00, DICHIARATA giudicabile: prezzo L-RE1 ≈ 14 ns/alloc ⇒ attesa +10..+15. Sotto soglia con segno concorde ⇒ keep-partial-wins (reperto, non promossa).
7. Guardie non-bersaglio a SOLO-regressione: soglia = −max(2×spread; banda-v2 [arith 0,80 · prop 3,33 · calls 0,50 · str 7,50 (v2; zavorra 2,50 vale solo come bersaglio) · arr 6,67]; BANDA_LAYOUT [1,00 · 1,00 · 0,50→calls 0,00 · str 5,00 · arr 3,33]; 2×quanto); tie ⇒ tiene.
8. Smoke R=2 di guardia su re+str (precedenza S-121 p.7): early-stop SOLO se concorde ≤−1,00 su TUTTE le coppie E |D_med| > banda cat (re: max(BL 5,00; 2×quanto 10,00)=10,00; str: max(BL 5,00; 2×quanto 5,00)=5,00); solo (a) senza (b) ⇒ si registra e il full giudica.
9. Promozione (solo se bersaglio OLTRE soglia e guardie tengono): batteria rc da FILE · corpus-gate canonico ×2 modi (1415 per NOME) · fixture-chain CON gate preg §3.18 (8 gate, cablato S-122) · pin SOLO via scripts/pin-phpr.sh (ricetta A′) · re-pin php-server in chiusura. Meccanismo mancato o guardia sfondata ⇒ REVERT secco al byte (leva a UN componente, niente bisezione).
10. Cifre solo dai `.out` (`s122-re2-verdetto.out`); ordine: commit criterio → census → build+admission → smoke → full → verdetto; run detached sequenziali, rc da file; strumentazione MAI nei sorgenti del pin.
