# s122-criterio-layout.md — banda-LAYOUT micro + ri-giudizio full L-ST1 (az. rev. S-121 #1/#2), PRE-registrato

1. Oggetto A (SOLA misura, forma zavorra): BANDA_LAYOUT(cat) = max−min delle mediane-di-famiglia net/iter su K=4 layout dello STESSO sorgente-pin s120: P0 = stash `phpr-s120-re1` (==885d2c64) + P1/P2/P3 = ricetta A′ con no-op FIRMATO distinto — fn morta `#[no_mangle] #[inline(never)] __phpr_layout_probe_s122` (catena LCG da start opaco, 17/173/1731 statement ⇒ tre taglie .text) in coda a `crates/php-cli/src/main.rs`; MAI CHIAMATA — tenuta viva da una materializzazione del suo INDIRIZZO (`black_box(fn as usize)`) in testa a `main()`, costo una-tantum d'avvio assorbito dal floor per-binario [EMENDATO dichiarando, PRIMA di ogni misura: il primo tentativo senza riferimento è stato dead-strippato da ld64 — __TEXT identico al pin, build P1 NULLA, admission ha morso: a verbale in s122-layout-verdetto.out]; il pin NON è toccato (release ripristinato al byte a fine build).
2. Admission P1..P3 (fail-closed, `s122-layout-build.sh`): parità output 6/6 vs P0 + dump INTERO byte-id (deroga forma S-118: no-op a emissione INVARIATA, citata QUI) + hash ≠ pin + simbolo probe PRESENTE in `nm` (assente = dead-strip = build NULLA); violazione ⇒ build SCARTATA, tree ripristinato.
3. Misura (`s122-layout-band.sh`): floor med3 per-binario su empty; poi per ogni categoria rounds INTERLEAVED P0→P1→P2→P3 ×R=5; famiglia 1,3×min PER binario; N dal sorgente (arr forzato 6.000.000 = D2); cifre SOLO da `s122-layout-verdetto.out` (+ righe macchina `BANDA_LAYOUT <cat> <val>` in `layout-out/layout-bande.txt`).
4. La banda NON promuove né refuta alcuna leva: fissa le soglie del p.6-7 e delle leve micro future (veto NEXT_SESSION: leve ≤10 ns/iter senza banda-layout misurata).
5. Oggetto B: full A/B L-ST1 con misura INVARIATA di s121-ab-st1.sh (copia `s122-ab-st1.sh`, solo VERD/OUT rinominati: A=`phpr-s120-re1`, B=`phpr-s121-st1` 2e1eda8d, floor per-binario, famiglia, coppie extra ≤6, R=5); il verdetto del vecchio script è ADVISORY — giudica `s122-st1-giudice.sh` sul TSV, bande lette fail-closed da layout-bande.txt.
6. Giudizio full str (bersaglio, DUE lati — sana l'asimmetria rilevata dal revisore): REFUTAZIONE CONFERMATA se D_med ≤ −max(BANDA_LAYOUT_str; zavorra 2,50; 2×quanto); PROMOZIONE-ai-gate (batteria+corpus+fixture, REGOLE §5) se D_med ≥ +max(4,00; 2,50; BANDA_LAYOUT_str; 2×spread_A; 2×quanto); altrimenti NON-DISTINGUIBILE dal layout ⇒ la refutazione S-121 DECADE a non-provata, candidato resta reperto, retry solo con meccanismo a banda più stretta.
7. Guardie full non-bersaglio a SOLO-regressione: soglia = −max(2×spread; banda-v2 [arith 0,80 · prop 3,33 · calls 0,50 · arr 6,67 · re 10,00]; BANDA_LAYOUT_cat; 2×quanto); tie ⇒ tiene.
8. Interim (in vigore DA ORA fino all'emissione della banda): early-stop str = max(zavorra 2,50; 2×quanto = 5,00).
9. Ordine (rc SEMPRE da file, run detached SEQUENZIALI via `s122-chain.sh`): commit criterio → build P1..P3 → banda → full A/B → giudice; strumentazione MAI nei sorgenti del pin.
10. Predizioni a verbale (az. rev. S-121 #3): se l'ipotesi-layout è vera, BANDA_LAYOUT_str atteso ≳ |−6,25| dello smoke; se BANDA_LAYOUT_str < 2,50 l'ipotesi-layout è SFAVORITA e il full deciderà tra refutazione-confermata e smoke-bugiardo; predizione secondaria: BANDA_LAYOUT_re ≲ 10,00 (banda micro N=2 S-120 la conteneva).
