# S-141 criterio leva L-RD1 «teardown array inline» (pre-registrato PRIMA della build B e di ogni run)

1. **Meccanismo**: `Drop for PhpArray` drena Packed/Hashed con match ESAUSTIVO per variante (disciplina S-96: variante nuova ⇒ non compila): scalari senza call, portatori di Rc col dec inline; ORDINE della glue INVARIATO (avanti; Key prima del valore); PROFONDITÀ di ricorsione INVARIATA (il trampolino drop_bounded riguarda Props/Captures, non gli array — invariato). Canale dal profilo S-140 (INDIZIO, riquantificato S-141 sui raw datati 2026-08-15): teardown array ≈2% suite ORM (Repr leaf 0,9–1,1% + 21,6% dei subtree drop-glue Zval; drop-glue Zval leaf totale 4,5–4,7%).
2. **Giudice NUOVO m-arrdrop** (wp141-harness): N=1e7 dal sorgente — az.rev. S-140 #1: a N=1e7 il tick di `/usr/bin/time` (0,01 s) = 1,0 ns/iter; packed 6 elementi (2 long, 3 str, 1 double), teardown per iterazione, output deterministico (somma).
3. **Segno**: B (leva) < A (pin s140 f2708b75, stash) ⇒ D=+. **Soglia = max(4,0; rumore drop-1 del run; banda-layout + 2 tick)** [az.rev. #1]. Banda-layout PROPRIA del giudice: A vs NULL (pin-sorgente + feature `null-lever`, build fresca S-141 — mai i nulla di sorgenti vecchie), stesso giudice, R=5, banda = |D_null| + drop-1.
4. **Modello (az.rev. #2, dichiarato nell'header e nel check)**: risparmio ≈ una call outlined `drop_in_place<Option<Zval>>` per elemento × 6 ⇒ **D atteso 6–40 ns/iter; D>60 = FUORI MODELLO** (sonda dovuta prima di promuovere); lato BASSO non-gate (dichiarato). **Controllo positivo = disasm**: nel binario B le call per-elemento della glue devono SPARIRE dal cammino Repr-drop (bl-count prima/dopo); senza questa firma la leva non si promuove, qualunque D.
5. **A/B**: harness `s141-ab-rd1.sh` (COPIA DICHIARATA di s140-ab-hc1.sh, manifest copia-gate `s141-ab-copia.diff`); smoke R=2 solo-giudice con early-stop a segno opposto; poi R=5 ABAB, guardie SOLO-REGRESSIONE invariate (objdatains 13,3 · m-dimrmw/m-diminc 5,0 · sei micro SL) + riconciliazione smoke↔R5 in banda max(banda, 5,0).
6. **Promozione** SOLO con catena completa (batteria rc dal comando, pin via script, re-hash con ricetta A′ al byte, corpus 1414×2, fixture bilaterali, ORM 3E/13F per NOME, conferma post-pin). Se la finestra non basta: il verdetto A/B resta agli atti, catena a S-142 — nessuna spedizione parziale.
7. **Quota-suite** (az.rev. #5): dal profilo S-140 riquantificato ≈1–2%, DICHIARATA INDIZIO; il census dei contatori del meccanismo (arrays/elems al teardown) è DOVUTO prima della spedizione definitiva.
