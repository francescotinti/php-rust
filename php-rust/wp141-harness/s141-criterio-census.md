# S-141 criterio census TakeSlot (pre-registrato PRIMA di build e run — az.rev. S-140 #5: la quota-suite si misura PRIMA di ogni spedizione)

1. **Oggetto**: CONTEGGI (mai tempo) del meccanismo esatto della leva TakeSlot: esecuzioni di LoadSlot/LoadVar (+sito fuso BinaryDst) a ULTIMO USO sotto il perimetro F2 — contatori `would_take{,_rc}` e `would_take_safe{,_rc,_str,_ref}` (S-95/S-97, vm/zvalcensus.rs) — sulla SUITE doctrine/orm intera. Il census conta ESATTAMENTE le esecuzioni che l'opcode convertirebbe: il bound vale per LA LEVA, non per l'intero canale churn.
2. **Probe**: build feature-only `zval-census` su HEAD 2071492 (crates/ INVARIATI dal pin s140 ef66b79, verificato `git log ef66b79..HEAD -- crates/` vuoto), target ISOLATO `/Volumes/Extreme Pro/Claude/phpr-census-target` — mai parità/A-B, mai stash.
3. **Run**: 2 repliche, workspace `orm-work.tgz` (wp9-harness/gates) su APFS scratchpad, `PHPR_ZVAL_CENSUS` = raw datato per replica (append per-processo: i contatori si SOMMANO sulle righe del file); watchdog 1800 s; smoke preliminare: dump non muto su script banale, altrimenti rc=8.
4. **Gate di validità**: parità fail-set per NOME vs `wp125-harness/orm-baseline-failnames.txt` su ENTRAMBE le repliche (rc=1 se diverge); scarto replica-vs-replica dei contatori >1% = si DICHIARA (non gate, indaga).
5. **Quota (INDIZIO, REGOLE §4 — magnitudine non ripartita)**: quota_X = count_X × prezzo 1,1–1,7 ns/evento ÷ 42,5 s (T_suite phpr S-140). Prezzo = per-evento clone+drop Rc dal giudice HC1 (cross-giudice DICHIARATO: stesso meccanismo, mai cifra promossa).
6. **Decisione pre-registrata**: quota(would_take_safe_str) ≥2% ⇒ istruttoria opcode (F3/F4 design95; divergenze S-96 §3.10 e catalogo RILETTE PRIMA di scrivere codice). 0,5–2% ⇒ si confronta con Repr-drop 11% e frame-lifecycle 9% (profilo S-140) e si sceglie il filone col canale maggiore. <0,5% ⇒ TakeSlot delude sul suo meccanismo ⇒ filone alternativo (Repr-drop o frame). Lettura secondaria storica P2 (S-95): safe_rc/would_take_rc ≥60% o la leva vale meno della sua complessità.
7. **Sentinelle non-gate** (conteggi immuni da contesa): pgrep rustc/cargo/rust-analyzer registrato per finestra; MLOCK creato per cortesia verso la CI e rimosso a fine run.
8. Nessun cronometro in questo criterio ⇒ az.rev. S-140 #1 (giudici sub-tick) non pertinente qui; varrà per l'eventuale A/B dell'opcode.
