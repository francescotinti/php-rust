# s123-criterio-metro.md — METRO sanato (az. rev. S-122 #1-5), PRE-registrato

1. Timer: user CPU via `getrusage(RUSAGE_CHILDREN)` in µs (`ucpu.py` nel harness; verificato PRIMA: empty 0,027–0,032 s con 6 decimali ≠ multipli di 10 ms); quanto dichiarato PRUDENZIALE 1 ms ⇒ quanto_ns(cat) = 0,001/N×1e9; NESSUNA soglia sotto 4×quanto (az. #2, entrambi i rami: timer sub-ms E run ≥5 s).
2. N scalati (run bersaglio ≥5 s; file generati in `scaled/` da `s123-gen-scaled.sh` con verifica del bound, STESSI file per tutti i binari): arith 150M · prop 90M · calls 60M · str 28M · arr r=300 (N=30M) · re 12M.
3. Banda-LAYOUT v2 (`s123-layout-band-v2.sh`, SOLA misura): K=5 binari — P0 = stash pin `phpr-s120-re1` (885d2c64 fail-closed) · P0b = COPIA byte-id del pin (`phpr-s123-p0b`, creata NELLO script, hash==pin fail-closed, su Extreme Pro) · P1..P3 = probe s122 GIÀ ammessi (hash attesi 29565241/dc98d1b1/f3bc390e fail-closed; admission S-122 riusata, non ripetuta); ordine PERMUTATO per round = quadrato latino ciclico 5×5 (round r parte dal binario r−1 ⇒ ogni binario in ogni posizione ESATTAMENTE una volta, az. #1); R=5; floor med3 per binario su empty scalato; famiglia 1,3×min per binario.
4. Uscite banda (righe macchina in `metro-out/layout-bande-v2.txt`): BANDA_V2(cat) = max−min delle mediane-famiglia {P0,P1,P2,P3}; PAV_PIN(cat) = |med(P0)−med(P0b)| (pavimento pin-vs-pin, az. #1); SOGLIA_LAYOUT(cat) = max(BANDA_V2; PAV_PIN) — è la SOLA soglia-layout che scende a valle; diagnostica per POSIZIONE a verbale (mediana net/iter per posizione 1..5: se l'effetto-ordine è morto, NON monotona).
5. A/B L-ST1 rifatto (`s123-ab-v2.sh`; A = pin, B = `phpr-s121-st1` 2e1eda8d fail-closed): 6 categorie, R=5 + coppie extra ≤6, ordine ALTERNATO per coppia (dispari A→B, pari B→A; colonna `ord` a verbale nel TSV, az. #3); parità output sui file scalati PRIMA del tempo; fam≥5 o MISURA_INVALIDA.
6. Giudice ST1 (`s123-giudice-v2.sh`, DUE lati SIMMETRICI, az. #4): str REFUTAZIONE CONFERMATA se D_med ≤ −max(SOGLIA_LAYOUT_str; zavorra 2,50; 2×spread_A; 4×quanto); PROMOZIONE-ai-gate se D_med ≥ +max(4,00; 2,50; SOGLIA_LAYOUT_str; 2×spread_A; 4×quanto); altrimenti NON-DISTINGUIBILE ⇒ L-ST1 resta NON ACQUISITA (candidato reperto). Guardie solo-regressione: −max(2×spread; banda-v2 storica [arith 0,80 · prop 3,33 · calls 0,50 · str 7,50 · arr 6,67 · re 10,00]; SOGLIA_LAYOUT_cat; 4×quanto); tie ⇒ tiene; promozione con guardia sfondata ⇒ niente promozione.
7. L-RE2 (az. #5; B = `phpr-s122-re2` 4eda5d6b fail-closed): smoke re+str R=6 ALTERNATO senza extra, giudice del p.6 con bersaglio re (fam≥5/6, floor promo 4,00, zavorra solo-str): ARCHIVIA il candidato SOLO su REFUTAZIONE CONFERMATA; NON-DISTINGUIBILE o lato + ⇒ FULL RE2 nella stessa catena (6 cat, R=5+extra, stesso giudice) e il verdetto finale è del FULL.
8. Predizioni a verbale: se il confondente era l'ordine ⇒ BANDA_V2_str < 5,00, diagnostica di posizione NON monotona, PAV_PIN_str < 2,50; se BANDA_V2_str ≥ 5,00 anche permutata ⇒ il layout è REALE e le leve str restano il suo ostaggio.
9. Ordine (`s123-chain.sh` detached via nohup — NON task —, sequenziale, rc SEMPRE da file, mkdir degli out PRIMA di ogni tee): commit criterio → gen-scaled → banda v2 → A/B ST1 → giudice ST1 → smoke RE2 → [full RE2 per p.7] → cifre SOLO dai `.out`; pin MAI toccato; nessun binario nuovo su disco Data.
10. Perimetro: il metro NON promuove leve (gate REGOLE §5 restano); le bande v2 SOSTITUISCONO le PROVVISORIE S-122 in NEXT_SESSION; ogni soglia futura di leva micro cita SOGLIA_LAYOUT(cat) e 4×quanto del p.1.
