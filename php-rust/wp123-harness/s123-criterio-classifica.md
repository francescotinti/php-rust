# s123-criterio-classifica.md — classifica-v2 col census nel ramo FUSO (NEXT p.2), PRE-registrato

1. Difetto sanato: la build C-lite S-119 accendeva `zval-census`+`mem-census`; il cfg a `run.rs:4213-4283` compila VIA il peephole fuso L-A sotto `zval-census`/`op-census` (scelta S-114: «i contatori misurano il sentiero storico») ⇒ la classifica S-119 descrive un sentiero che il release non percorre (reperto S-122).
2. Cura: build census con **SOLO `mem-census`** (php-types: GA_* + funnel cloni Zval del census-clite.patch) — il fuso è spento SOLO da `zval-census`/`op-census` (`run.rs:4213`, unico sito di alternanza nel crate) ⇒ fusione VIVA, run.rs INTOCCATO, nessuna feature nuova. [EMENDATO PRIMA della build, mappa Explore S-123 a verbale: sotto mem-census restano (a) `panic!` in `reloc_unexpected_shared` (mod.rs:16875) — sentiero d'ERRORE, fail-loud accettato; (b) burst `mem::forget` 1 MiB (mod.rs:16347) gated da env `PHPR_MEMCENSUS_TEST_BURST` che questa run NON setta; (c) `#[global_allocator]` → CountingMi (php-cli/main.rs:30-38) — È lo strumento di conteggio, ed è il motivo per cui da questa build non esce NESSUNA cifra di tempo.]
3. Build (`s123-classifica-build.sh`, target SEPARATO `phpr-census-target`, mai il pin): tree pulito → apply census-clite.patch → `cargo build --release --features php-runtime/mem-census` → hash census-bin a verbale → revert tree al byte (`git checkout -- crates/` + `git status` pulito a verbale); il release-dir del pin NON è toccato (target-dir dedicato). Admission: parità output 7/7 (empty+6 micro) census-bin vs pin stash.
4. Run (`s123-classifica-run.sh`, forma s119-clite: SOLO CONTEGGI, nessuna cifra di tempo): R=2 su empty+6 micro ORIGINALI (N: arith 50M · prop 30M · calls 20M · str 4M · arr 6M · re 2M); gamba phpr: PHPR_MEM_CENSUS → riga `gacensus`; gamba oracle: count-interpose.dylib + USE_ZEND_ALLOC=0 (riuso wp119); R=2 deve dare conteggi phpr IDENTICI (deterministico) o la cifra è NON ACQUISITA.
5. Tabella-v2 (`s123-classifica-report.sh`): per categoria, alloc/iter = (galloc_n(cat)−galloc_n(empty))/N e idem free, zvclone_all, zvclone_rc; stesso netting per l'oracle (malloc_n interposti); classifica per delta alloc/iter phpr−oracle. Confronto ESPLICITO con la classifica S-119 (colonna v1 vs v2): le voci che CROLLANO (es. prop 5→~0 cloni attesi se il double-hit fuso domina) sono la misura di quanto la v1 sovrastimava.
6. Predizioni a verbale (PRIMA della run): prop zvclone/iter v2 ≪ 5,00 (double-hit fuso: atteso ~0-1); re alloc/iter v2 = 10,00 invariato (preg non passa dal peephole prop); arith/calls ≈ v1; str 5,00 invariata; arr da leggere (v1 245 lorde / +2,02 nette per op-int).
7. Perimetro: SOLO CONTEGGI (mai tempo da build census, REGOLE §3); la tabella-v2 alloca le prossime leve micro (PhpStr single-alloc p.3, prop IC/RefCell p.5) ma NON promuove nulla; cifre solo da `s123-classifica-verdetto.out`; run SEQUENZIALE dopo la catena METRO (mai in parallelo a misure di tempo).
