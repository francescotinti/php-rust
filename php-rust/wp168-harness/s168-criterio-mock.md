# S-168 criterio MOCK sottrattivi F0 (⚖️ concilio S-167 / team-meccanismo; az.rev.1 S-167) — PRE-registrato PRIMA di ogni build/misura
1. OGGETTO: fino a 4 build da tree==pin s166 con patch MINIME dichiarate (file `s168-mN.patch`, committati PRIMA del braccio): **m0** = tree NON patchato, stessa ricetta e target dedicato ⇒ hash atteso == pin 092dcff4 (braccio NULLO: se diverge, m0 diventa A dichiarato e il confronto è «stessa ricetta») · **m1** consts-predecode (`Func.consts_z: Vec<Zval>` popolato al lowering; il handler `BinarySCSCDst` legge `&consts_z[c]` per riferimento, niente `to_zval`) · **m2** BinOp «cotto» (guardia sulla tupla (Mul,Shr,Sub,Add) ⇒ i 4 `binary_fast(op,..)` sostituiti da 4 funzioni monomorfe Long; resto del handler IDENTICO; il costo della guardia resta nel mock = stima CONSERVATIVA dichiarata) · **m3** hoist di `frames[top].slots` su locale per le TRE letture del handler (store `reg_store_slot` INVARIATO) · **m123** (opzionale, additività) = m1+m2+m3 insieme.
2. GIUDICE: `wp164-harness/arith-dq.php` N=250.000.000 dal sorgente (tick 0,04 ns/iter); ns/iter=(med raw−floor)/N con floor `empty.php` PER-binario; R=5 coppie ABAB alternate; A = pin s166 dallo stash (`phpr-s166`, hash MISURATO 092dcff4), B = mock (hash misurato). SEGNO: D = A−B, +B = costo nominato dal mock. SOGLIA = max(4 ns/iter, rumore drop-1, banda-layout arith SL 0,94): D < soglia ⇒ componente NON nominata (vale 0); D<soglia con rumore>4 ⇒ rc=8, si estende R (az.rev. S-164 #5).
3. BRACCIO E2 bilaterale («loop nudo», `arith-e2.php` derivato dichiarato, corpo vuoto): su A, B e oracle nella stessa finestra. controllo-loop_X = e2_X; statement_X = dq_X − e2_X; gap-statement = statement_A − statement_O. GUARDIA: |e2_B − e2_A| ≤ max(4, rumore) (il mock non tocca il controllo-loop), altrimenti rc=5 e il D del mock è SOSPESO (arbitrato).
4. CHIUSURA (Gregg, gate PRIMA della prima leva): nominati = e2_A + Σ D_mock (solo ≥soglia); chiusura = nominati / dq_A. ≥90% ⇒ GO F1/F2 (F1 col suo criterio); 85–90% ⇒ decomposizione ammessa a completarsi in UNA sessione (S-169) prima di codice; <85% ⇒ nessun codice di leva. KILL (pre-registrato ⚖️ regola 4): Σ D_mock < 10 ns/iter ⇒ R1 «interno-handler via compilatore» ESAURITO su arith ⇒ delibera R4 al concilio. ADDITIVITÀ (se m123): |D_m123 − Σ D_mi| ≤ max(4, rumore) ⇒ componenti indipendenti; altrimenti sovrapposizione dichiarata e si conta D_m123 (mai la somma).
5. GATE EDIT per ogni mock: smoke parità 2 modi via `scripts/pin-phpr.sh --braccio s168-mN <bin>` (stash + registro); output A==B su dq ed e2 (rc=2 STOP); disasm `bl` run_loop A/B (`wp160-harness/s160-disasm-bl.sh`) Δ DICHIARATO a verbale; two-request parity (⚖️ regola 5) NON dovuto: i mock MISURANO, non si promuovono; albero ripristinato al byte dopo ogni build (`git status --porcelain crates` vuoto).
6. IGIENE: lock `/private/tmp/phpr-measure.lock` (S-168) · quiescenza s129 · sentinelle LS a inizio/fine finestra · copia-gate v1+v2 su `s168-ab-mock.sh` (base `wp167-harness/s167-f0ab.sh`) · target dedicato `/private/tmp/s168-mock-tgt` RIMOSSO nell'epilogo (lezione ENOSPC S-167) · esiti SOLO da file (`ab-out/*.rc`, verdetti `s168-<tag>-verdetto.out`); sintesi in `s168-mock-verdetto.out`.
7. EMENDA DICHIARATA (S-168, 22:50, PRIMA di ogni misura): m0 (tree non patchato, ricetta `-p php-cli`, copia in /private/tmp) ha hash 36d73812 ≠ pin 092dcff4 — i path della copia entrano nel binario (file!()/panic-location) ⇒ come pre-registrato al p.1, **A = m0** per tutti i mock (stessa ricetta, stessa copia a path FISSO `/private/tmp/s168-mock-src`, così m0..m123 differiscono SOLO per le patch); in più si misura m0 vs pin-stash (A/B «braccio nullo», TAG m0) per dichiarare la banda ricetta/path. `s168-ab-mock.sh` prende A come parametro (copia-gate v1+v2 rieseguiti, rc=0).
8. BRACCIO AGGIUNTO (S-168 23:33, dichiarato PRIMA della sua misura, dopo il reperto E2): il dump del bytecode mostra un `Op::Sweep {main}` PER ITERAZIONE dopo lo statement del driver (0007) — **m4** = niente Sweep dopo StmtKind::Expr (patch `s168-m4.patch`, semantica GC/dtor NON preservata: solo misura) nomina il costo del dispatch Sweep; stesso giudice/soglia/guardia dei mock; entra nella Σ mock (kill p.4) e nella chiusura come componente «dispatch Sweep». Con E2 (2 op = 14,7 ns) e m4 la decomposizione separa dispatch-per-op dal corpo del handler BinarySCSCDst.
9. EMENDA m4 → m4b (S-168 23:44, dichiarata PRIMA della misura di m4b): m4 è NULLO — il dump mostra lo Sweep del loop ancora emesso (0006: lo riemette il Block del corpo del `for`, non lo statement interno); D=0,00 lo conferma (nulla rimosso ⇒ nulla misurato). m4b elide anche StmtKind::Block (`s168-m4b.patch`); stesso giudice/soglia/guardia; il dump del loop DEVE mostrare 2 op + BinarySCSCDst senza Sweep, altrimenti m4b è nullo a sua volta.
