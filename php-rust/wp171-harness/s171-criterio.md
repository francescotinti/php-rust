# S-171 criterio — LEVA CODICE «forma sigillata Long», fetta 1 (prima leva dopo 3 sessioni di sola misura); PRE-registrato PRIMA di build/misura
1. OGGETTO (leva L-SL1, safe Rust, ZERO `unsafe`, semantica PRESERVATA per costruzione): (a) `Op::BinarySCSCDst` = cammino Long INTERO su `i64` in forma GENERICA (match per op via `long_arith_i64`: Add/Sub/Mul `checked_*`, BitAnd/Or/Xor, Shl/Shr con y∈[0,64) — SOLO gli arm Long→Long di `binary_fast`, VERBATIM; Div/Mod/Pow/Concat/y<0/overflow ⇒ `None`) quando i 3 slot sono `Long` e i 2 const sono `Int`; risultato scritto IN PLACE se `slots[dst]` è `Long` (= `store_slot` su Long: nessun typed-ref, `gc_note` di un Long è no-op), altrimenti `reg_store_slot`; ogni miss ⇒ corpo ORIGINALE spostato in un metodo `#[cold] #[inline(never)]` (slow path FUORI LINEA, rilievo #6 S-170); NESSUNA tupla cotta (Mul,Shr,Sub,Add): la forma driver-shaped di m12/m13 NON è una leva. (b) `Op::CmpJmpSC` = (Const::Int, Long) letti SENZA `to_zval`, confronto a `bool` diretto (Lt/Le/Gt/Ge/Eq/Identical/NotEq/NotIdentical: VERBATIM da `binary_fast`), niente `Zval::Bool`/`to_bool`/`Result`; miss ⇒ corpo originale `#[cold]`. (c) `IncDecSlotJmp`/`IncDecSlotPop` via `incdec_slot_discard`: `checked_add` IN PLACE sul payload Long (NON wrapping: overflow ⇒ slow path ESATTO `#[cold]`). `scn!`/`dcn!` restano dove i valori nascono (fast path: nessun Zval materializzato ⇒ nessun `dcn!`).
2. GIUDICI (entrambi BERSAGLIO, la leva tocca i 4 op del loop dq e i 2 di e2): **arith-dq** (`wp164-harness/arith-dq.php`) e **arith-e2** (`wp168-harness/arith-e2.php`), N LETTO DAL DRIVER (`$i<N`, az.rev. S-170 #4: mai cablato). Bracci nello STESSO run: **A = pin s166** (`~/Claude/php-rust-output/release/phpr` == 092dcff431bef876, hash MISURATO), **B = candidato** (albero di lavoro con la leva, target DEDICATO `/private/tmp/s171-leva-tgt`, ricetta pin: `SOURCE_DATE_EPOCH=0 CARGO_INCREMENTAL=0 cargo build --release -p php-cli`; hash misurato, stash `--braccio s171-sl1-B`), **C = tetto m13** (stash `phpr-s170-m13` == 85adbfc07e2c1fe4: binario di ALTRO albero ⇒ contrasto B−C = DIREZIONE+BANDA dichiarata, mai magnitudine). R=5 ABAB alternato su ogni giudice (C misurato una volta per coppia, dopo A e B); ns/iter=(med raw−floor)/N, floor PER-binario su `wp160-harness/empty.php` (`[ -e ]`), rumore drop-1; **nominata** D≥max(4, rumore, 0,94); direzione firmata |D|≥max(rumore, 0,94). Parità vs ATTESO oracle su entrambi i driver per A, B E C (rc=2 se ≠). Copione `s171-ab-leva.sh` = COPIA DICHIARATA di `wp170-harness/s170-ab-dq.sh` (manifest `s171-ab-leva-copia-v3.diff`, copia-gate v3 per TOKEN sess s171; adattamenti: braccio C, N dal driver, due giudici, nessuna guardia dq/e2 perché entrambi bersaglio).
3. SOGLIE (contro m0=pin E contro m13, az.rev. S-170 #3; NESSUNA cifra composta: solo D interni al run): **dq**: attesa D_dq ∈ [17; 26] (forma generica ≈17 + e2 ≈4; tetto m13 22,20 + m8 4,08); **KILL-1**: D_dq < 10 ⇒ la forma generica NON riproduce nemmeno metà del tetto: NON si generalizza, la fetta torna a R4 con le cifre (opzione fetta 1b = opcode specializzati per tupla); banda **B−C** (dq_B − dq_m13, contrasto): ≤5 «tetto riprodotto entro il match per op» · (5;10] «il match per op pesa: fetta 2 = specializzazione, a verdetto» · >10 «la forma NON riproduce il mock: xctrace m0/B/m13 (az.rev. #2) PRIMA di generalizzare». **e2**: attesa D_e2 ≈ +4 (m8 4,08, a filo); nominata ≥4, direzione firmata ≥0,94; **regressione** su un giudice (D<−max(4,rumore)) ⇒ rc=5, leva SOSPESA. Promozione = D_dq NOMINATA e D_e2 ≥ −rumore (nessuna regressione), anche se KILL-1 scatta (feedback utente: il guadagno misurato non si butta; il kill alloca S-172).
4. GUARDIE non-bersaglio a SOLA REGRESSIONE: micro R=5 alla promozione (prop/calls/str/arr/re: rapporto ≤ pin + tick; arith = scoreboard); disasm `bl` run_loop pin vs B (attesa: +2..+4 bl = le chiamate `#[cold]` nuove; un Δ ≥ +20 = inliner flippato ⇒ si dichiara, lezione WP-104); denti `wc -l run.rs` dichiarato.
5. GATE PIENI (REGOLE §5-6, pin nuovo s171): build ricetta canonica == candidato al byte (o identità a CONTENUTO, cluster nominati) → batteria `cargo test --release` rc dal comando (1748, inventario == baseline s125 + 2 denti) → build ricetta post-batteria == al byte (churn neutralizzato, S-166) → `pin-phpr.sh s171` → `corpus-gate.sh` (nomi==1412 ×2 modi + golden + off↔on; attesa ZERO flip: le forme fast sono sottoinsiemi ESATTI di `binary_fast`) → fixture chain s109 (10 gate) + fx-ce/am/af/sm/au bilaterali + sonda-bt/fx-refl/fx-sm-div/fx-au-div pin==stash s166 + **fx-sl1 NUOVO** (bilaterale oracle==pin: overflow Mul/Add/Sub a ±PHP_INT_MAX, `>>` con y<0/y≥64, `<<`, Div esatto/inesatto, Mod, slot Ref/Undef/Double/string numerica, dst==l, `$i++` a PHP_INT_MAX, confronto Long vs Int const in tutte le 8 forme, IncDec su Null/Double/string) → quiescenza → micro R=5 → gate ORM per NOME + http-kernel 0E/0F → `pin-server.sh s171`. Copione `s171-promozione.sh` = COPIA DICHIARATA dell'ultima catena di promozione (manifest + copia-gate v3).
6. COPPIA WP+ORM DOVUTA al pin nuovo (utente 2026-08-12): `s171-lancio-pair.sh`/`s171-pair.sh` t17 e `s171-lancio-orm.sh`/`s171-orm-coppia.sh` = copie dichiarate di S-166 con pin s171, mediane storiche aggiornate (t15/t16), attesa dichiarata: WP direzione ≤0 (quota Long-loop in WP non censita: SOTTO-risoluzione), ORM direzione ≤0 (banda sentinella oracle VINCOLANTE).
7. IGIENE: lock `s171` VIVO (creato 10:19, rimosso in chiusura come passo esplicito); quiescenza s129 prima di ogni A/B; esiti da file (`.out`/`.rc`/`.done`); output di run MAI nel repo; target dedicato rimosso in epilogo (binari tenuti nello stash); CI in quiete (coda vuota, runner fermo sul lock); ktrace purge dopo ogni xctrace.
8. ORDINE: criterio (questo, commit) → codice leva (Serena, `add -u` + `commit -F`) → `cargo build` target dedicato (detached) → parità A/B/C + dump loop (dq: CmpJmpSC·BinarySCSCDst·Sweep·IncDecSlotJmp invariato) → `--braccio` → disasm → A/B R=5 dq → A/B R=5 e2 → sintesi meccanica `s171-verdetto.out` → SOLO SE promozione ammessa (p.3): fx-sl1 + `s171-promozione.sh` (detached) → coppia (detached, in catena) → az.rev. #2 xctrace m0 vs B vs m13 su dq e #4 m8 a N=1G (se resta finestra) → revisione (lente SEMANTICA, rotazione: S-170 misura) → rotazione.
