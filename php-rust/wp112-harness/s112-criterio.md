# S-112 criterio PRE — leva H-A2 «fast-path Long del sentiero RMW» (scritto e committato PRIMA di ogni implementazione e misura; istruttoria: s112-istruttoria.md)

1. Siti (3, per NOME): **A2a** = braccio Shl/Shr (Long,Long) in `binary_fast` (VERBATIM ops::shl/shr; y<0 → `None`, l'errore resta al generico); **A2b** = guardia `binary_fast` inline sulla coda opd di `BinarySCSCDst`; **A2c** = idem sulla coda di `BinarySTDst` (scn!/dcn!/note_* INVARIATI di posto e di valori).
2. Giudice primario: **arith** (N=50M emesso dal sorgente). Segno atteso: Δ=A−B POSITIVO (B più veloce). Beneficiari attesi NON vincolanti: prop, arr (↓ o fermi). Nessuna magnitudine attesa nel verdetto (stime orientative FUORI).
3. Protocollo: A = stash `phpr-s109` (=pin 92909544, verificato), B = candidato CONSERVATO (`cp` → `phpr-s112-ab-candidate`); smoke R=2 con early-stop se 2/2 a segno opposto e |Δ|>max(rumore; 0,67); pieno R=5 ABAB interleaved, user CPU al netto del pavimento PER-binario (empty.php), mediana.
4. Soglia promozione (arith): Δ mediano ≥ max(4 ns/iter; spread lato A; banda-layout 0,67) E segno concorde 5/5.
5. Guardie non-bersagli (**calls, str, re**, stesso protocollo R=5): |Δ mediano| ≤ max(2×spread lato A della categoria; 0,67 ns/iter); una guardia sfondata ⇒ NIENTE promozione (verdetto lo dichiara).
6. Held-out (soglia PRE-registrata, az.5 rev. S-111): a leva conclusa `run-heldout.sh` R=5 su pin e candidato; REGRESSIONE = phpr netto candidato > baseline S-111 (poly 10,22 · err 3,02 · wploop 6,73 s) + max(2×spread della lettura corrente; 0,12 s); UNA regressione ⇒ NIENTE promozione. Miglioramenti ammessi e registrati.
7. Admission (bipartita, PRIMA dello smoke): batteria `cargo test --release` rc=0 DAL COMANDO; emissione INVARIATA — la leva non tocca il pass: dump ON {main} ×6 micro E dump OFF interi ×6 byte-identici al pin (metodo s109-admission.sh); taglia+bl-count di `run_loop` prima/dopo a verbale (nm/otool, metodo S-109 — non confrontabile col «29» storico).
8. Verdetto: PROMOZIONE (pin S-112 nato SOLO da `scripts/pin-phpr.sh` + gate REGOLE §5: batteria, corpus 1415 per NOME ×2, fixture bilaterali, micro R=5 sul pin) solo se 4+5+6+7 reggono TUTTI; altrimenti «direzione firmata, magnitudine non stabilita» (sotto soglia a segno concorde) o REFUTATA, e revert AL BYTE con release ripristinata dallo stash.
9. Commit+push a ogni passo; output di run FUORI dal repo (`wp112-harness/ab-out/`, gitignored); rc dei gate MAI da pipe.
