# S-115 criterio PRE — SECONDA leva-nulla di attribuzione layout (banda N=2; §S-115 p.2; scritto e committato PRIMA del codice e della misura)

1. Oggetto: NESSUNA semantica — variante della zavorra S-114 che cambia TAGLIA e POSIZIONE: ~+1,2 KB (32 round, metà di S-114) nel braccio `Op::Clone` (run.rs:4004, ASSENTE dai `{main}` dei sei micro per census S-114), guardia su env `PHPR_S115_BALLAST2` MAI settata. Scopo: secondo campione della banda-layout; il candidato si scarta a misura conclusa, nessuna promozione possibile per costruzione.
2. Attesa: Δ vero = 0 su tutte le categorie; ogni Δ osservato = layout+rumore. Le uscite SONO il secondo campione di banda.
3. Protocollo EMENDATO S-115 (eredita s115-criterio-la.md p.2-3): A = stash `phpr-s112`, B = candidato CONSERVATO (`cp` → `phpr-s115-nulla2` PRIMA della misura); R=5 ABAB sulle sei categorie, pavimenti per-binario; famiglia = 1,3× il MINIMO della propria serie, coppie escluse per NOME, integrazione max +6, <5 in famiglia ⇒ misura invalida → rerun; parità stdout+stderr A vs B per categoria PRIMA del cronometro (gate).
4. Uscite A MACCHINA: per categoria Δ mediano in famiglia, segni, spread depurati, esclusioni nominate; banda2(cat) = |Δ mediano in famiglia|; banda2 globale = max sulle 6.
4-bis. EMENDAMENTO pre-run (dopo l'esito held-out L-A, PRIMA di codice e build di questa leva): la misura include ANCHE i tre giudici held-out (poly/err/wploop, R=5 pin vs candidato-nullo, net per-binario, protocollo s115-heldout-la.sh): |Δ net| = PRIMO campione di banda-layout held-out in SECONDI per giudice. Movente dichiarato: la regressione poly di L-A (+0,27s) e' su sentiero MAI eseguito (0 PropGetSlotRecv nel dump) ⇒ famiglia layout, ma la soglia p.7 del criterio L-A non ha allowance layout perche' NESSUNA banda held-out e' mai stata misurata. Un eventuale rerun held-out di L-A si fa SOLO rieseguendo il criterio L-A emendato per intero (REGOLE §3), non in questa sessione sotto questo criterio.
5. USO della banda N=2 (pre-registrato): da S-116 banda(cat) = max(banda_S114(cat), banda2(cat)); se banda2 globale > 2× banda S-114 (6,67) la banda è DICHIARATA instabile e ogni soglia futura usa il max con nota d'instabilità.
6. Admission (metodo S-114, PRIMA della misura): dump ON `{main}` ×6 IDENTICI al pin E dump OFF interi ×6 al byte; taglia+bl/br di `run_loop` a verbale (target ≈ +1,2 KB). Batteria NON richiesta: nessuna semantica toccata, nessuna promozione — deroga DICHIARATA qui (come S-114 p.6).
7. Vincoli di build: la zavorra si applica SOLO a A/B L-A concluso (niente edit con misure in volo — veto S-114); patch versionata nei verbali (`s115-zavorra2.patch`) PRIMA del revert (az.2 rev. S-114: mai più sorgente irrecuperabile); revert AL BYTE verificato (release ricostruita = f71abd2a, diff crates/ vuoto).
8. Commit+push a ogni passo; raw in `wp115-harness/{ab-out,admission-out}/` (gitignored); rc di gate MAI da pipe.
