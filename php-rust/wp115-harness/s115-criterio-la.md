# S-115 criterio PRE EMENDATO — rimisura leva L-A «trigramma prop» (§S-115 p.1 lettere a-f; scritto e committato PRIMA di ogni codice e misura; eredita istruttoria s114-istruttoria-la.md e banda s114-nulla-verdetto.out)

1. Oggetto: RIMISURA della magnitudine di L-A (direzione già FIRMATA S-114, keep-partial-wins). A = stash `phpr-s112` (=pin f71abd2a), B = `phpr-s114-la` CONSERVATO (052ea417, codice 2c18b2e): STESSI binari di S-114, NESSUN rebuild per l'A/B — il layout è identico per costruzione, la banda S-114 resta applicabile.
2. (a) Scheduling OSSERVABILE per esclusione pre-registrata: una run è FUORI FAMIGLIA se ns/iter > 1,3× il MINIMO della PROPRIA serie (per braccio e categoria). Si sceglie il MINIMO e non la mediana dell'esempio §S-115 perché in S-114 la maggioranza del lato B era nella famiglia LENTA (78×2 vs 101-111×3): la mediana non la esclude — deviazione dichiarata QUI, prima della run. Una COPPIA ABAB è esclusa per NOME se UNO dei due lati è fuori famiglia; integrazione automatica con coppie extra (max +6) fino a ≥5 coppie in famiglia; ogni esclusione NOMINATA nei raw (coppia, lato, valore, soglia famiglia).
3. (b) Famiglie insufficienti invalidano la MISURA, non la leva: <5 coppie in famiglia dopo +6 ⇒ rerun completo della categoria (1 volta); se ancora insufficiente ⇒ (f) R=7 (una volta). MAI verdetto sulla leva da una misura invalida.
4. (c) Soglia promozione prop: Δ mediano sulle coppie in famiglia (≥5) ≥ max(4 ns/iter; spread_A DEPURATO = spread delle sole A in famiglia; banda(prop)=4,33) E segno POSITIVO su TUTTE le coppie in famiglia. Guardie non-bersaglio SOLO-REGRESSIONE con banda S-114 (arith 0,40 · calls 5,50 · str 5,00 · arr 6,67 · re 0,00): sfonda se Δ mediano in famiglia < −max(2×spread_A depurato; banda(cat)); una sfondata ⇒ niente promozione.
5. (d) Parità output DENTRO l'A/B: per ogni categoria, PRIMA del cronometro, stdout+stderr di A e B a `diff`, gate su rc DAL COMANDO; una divergenza ⇒ STOP leva (refutazione di fedeltà), non rerun.
6. (e) Admission runtime mirata PRIMA dello smoke: `hit.php` (trigramma `PropGetSlotRecv`+`BinaryTCPropSetPop` VERIFICATO adiacente nel dump `{main}` del PIN — gate) + `miss-{magic,undef,ref,str}.php` (presenza del bigramma riportata a verbale per ciascuno); per OGNI caso stdout+stderr candidato vs pin IDENTICI (diff, rc dal comando) e dump `{main}` candidato vs pin IDENTICO. Batteria: si RIUSA 1742/0/2 di S-114 (stesso binario byte-identico 052ea417) — deroga DICHIARATA qui.
7. Held-out invariato (criterio S-114 p.5): R=5 pin e candidato; REGRESSIONE = netto > baseline S-112 (poly 9,59 · err 2,95 · wploop 6,76 s) + max(2×spread corrente; 0,12 s); UNA regressione ⇒ niente promozione.
8. Verdetto: PROMOZIONE solo se 4+5+6+7 reggono TUTTI; pin SOLO via `scripts/pin-phpr.sh` in ordine §6 PIENO, riapplicando 2c18b2e alla release (cherry-pick, niente checkout parziale). Altrimenti l'esito resta «direzione firmata, magnitudine non stabilita» (o REFUTATA se morde il p.5) e i binari conservati restano.
9. Caveat dichiarati: banda(prop) 4,33 da N=1 perturbazione (si irrobustisce col p.2 dell'ordine S-115); esito in [soglia; soglia+banda] = «dentro l'incertezza layout» anche se promosso; la famiglia veloce ≈ P-core è IPOTESI nominata, l'esclusione è comunque valida come regola pre-registrata sui numeri.
10. Commit+push a ogni passo; raw in `wp115-harness/{ab-out,admission-out}/` (gitignored); verbali s115-*.out committati; rc di gate MAI da pipe.
