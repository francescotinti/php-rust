# s125-criterio-census.md — controllo ±zval-census STESSO head (az. rev. S-123 #1), PRE-registrato

1. Oggetto: effetto-fusione PURO controllato. Due build census dallo STESSO tree (crates @ fb140d1 = pin s124): **A** = `mem-census` (fusione VIVA) · **B** = `mem-census,zval-census` (fusione SPENTA dal cfg unico `run.rs:4213`). SOLO CONTEGGI, nessuna cifra di tempo (REGOLE §3).
2. Build (`s125-census-build.sh`): tree pulito → apply census-clite.patch (wp119) → build A in `phpr-census-target`, B in `phpr-census-zv-target` (ricetta SOURCE_DATE_EPOCH=0, CARGO_INCREMENTAL=0) → revert tree al byte. Admission fail-closed: parità output 7/7 (empty+6 micro) di A E di B vs pin stash `phpr-s124` (c5ba2573). B che non compila = STOP dichiarato (debito apparato), nessun fix fuori perimetro senza ri-commit del criterio.
3. Run (`s125-census-run.sh`): R=2, empty+6 micro ORIGINALI (N: arith 50M · prop 30M · calls 20M · str 4M · arr 6M · re 2M), `PHPR_MEM_CENSUS` → riga `gacensus`. Nessuna gamba oracle: il controllo è interno phpr (A vs B, stesso head).
4. Arbitro (`s125-census-report.sh`, committato QUI, prima del run): per categoria, netting vs empty: zvclone/iter e alloc/iter = (X(cat)−X(empty))/N, 2 decimali. R=2 deve dare cifre derivate IDENTICHE (2 decimali) per gamba o la cifra è NON ACQUISITA.
5. Predizioni a verbale (PRIMA della run): **A** zvclone/iter = arith 1,00 · prop 3,00 · calls 2,00 · str 2,00 · arr 5,08 · re 3,00 (regime S-123-v2/S-124). **B−A zvclone**: prop **+2,00** (il fuso assorbe il double-hit: 5,00→3,00), tutte le altre **0,00** (tolleranza ±0,05; arr ±0,10). **B−A alloc/iter**: 0,00 ovunque (±0,05; arr ±0,10) — la fusione muove cloni, non allocazioni.
6. Esito: dentro tolleranza ⇒ «prop 5→3 attribuito alla FUSIONE» diventa MISURA (chiude l'inferenza S-123); fuori ⇒ grado mancato, diagnosi PRIMA di ogni uso, nessuna leva promossa/bocciata da qui (è un controllo).
7. Cifre citabili solo da `s125-census-verdetto.out`; run sequenziale DOPO la coppia WP (mai in parallelo a misure di tempo).
