# s125-criterio-banda.md — ri-banda layout v2 sul regime POST-PATCH (az. rev. S-124 #4), PRE-registrato

1. Difetto sanato: le SOGLIA_LAYOUT v2 vigenti vengono da binari s120/S-123 (pre-PhpStr); i candidati futuri sono post-patch. Qui si RIEMETTE la banda con K=5 binari del regime s124: **P0** = stash `phpr-s124` (c5ba2573) · **P0b** = copia byte-id fresca · **P1..P3** = probe layout no-op FIRMATI (K=17/173/1731, protocollo s122 INVARIATO, simbolo `__phpr_layout_probe_s125`) costruiti dal head s124.
2. Build (`s125-layout-build.sh`): PRE release==pin al byte (o riallineamento ricetta A′ dichiarato) → per ogni K: append probe a `crates/php-cli/src/main.rs` + keep-alive black_box → build ricetta → admission fail-closed (output 6/6 + dump `PHPR_DUMP_OPS` byte-id vs pin + hash≠pin + simbolo presente) → stash `phpr-s125-lay1..3` → ripristino finale release==pin AL BYTE. Hash dei probe scritti in `banda-out/layout-hashes.env` (input dell'arbitro, non tolleranze).
3. Misura (`s125-layout-band.sh`, arbitro committato QUI prima del run): R=5, ordine PERMUTATO (quadrato latino ciclico 5×5), timer `ucpu.py` (getrusage µs), workload SCALATI wp123 (N: arith 150M · prop 90M · calls 60M · str 28M · arr 30M · re 12M), pavimenti med3 per-binario, famiglia ≤1,3×min, mediane di famiglia.
4. Uscite per categoria: **BANDA_V2** = max−min mediane tra {P0,P1,P2,P3} · **PAV_PIN** = |P0−P0b| · **SOGLIA_LAYOUT** = max(banda, pav). Queste sostituiscono le v2 di S-123 per OGNI leva micro futura (REGOLE §3: soglia = max(4 ns/iter, rumore, banda-layout)).
5. Nessuna predizione di magnitudine (è una banda: si pre-registra la PROCEDURA, l'esito è la banda qualunque sia — lezione S-103). Nessuna promozione/bocciatura da qui.
6. Cifre citabili solo da `s125-banda-verdetto.out`; run sequenziale, mai in parallelo ad altre misure; build SOLO dopo la coppia WP.
