# s125-criterio-cbargs.md — leva L-HD2 «cbargs»: args inline nel braccio CallBuiltin, PRE-registrato

1. Bersaglio: apertura per NOME «str residuo +1,00 alloc/iter vs oracle». Diagnosi dal sorgente: `substr` è slice-fed (string.rs:161, 1 alloc) e il concat è single-alloc ⇒ il +1 è l'**args-Vec di `Op::CallBuiltin`** (`pop_keys` = `split_off`, mod.rs:8184-8188) — il canale H-D già cifrato (1 alloc×32 B/chiamata, WP-103/104) e già battuto su Op::Call in S-105 (forma 2 promossa; SmallVec caduta; pool refutato Leijen).
2. Forma (coerente col veto «contenitori sul call path»): NESSUN contenitore — per argc≤4, buffer `[Zval; 4]` sullo stack nativo riempito svuotando la pila VM; argc>4 → `pop_keys` invariato. Perimetro: SOLO il braccio `Op::CallBuiltin` in run.rs (+eventuale helper locale); `pop_keys` e gli altri chiamanti INTOCCATI. Oltre il perimetro ⇒ ri-commit del criterio emendato PRIMA di rieseguire gli arbitri (vincolo rev. S-124 #2).
3. Modello del costo SOSTITUTIVO: rimossa la coppia malloc+free per chiamata; aggiunti: branch argc≤4 (predetto), init 4×Undef, +64 B stack frame; la memcpy degli argc resta (drain ≡ split_off). Attesa su str (1 substr/iter): **Δ ∈ [+6,+14] ns/iter** (modello S-105, stesso canale).
4. Admission census (arbitro `s125-cbargs-census.sh`, committato QUI): build census+leva (census-clite.patch, target `phpr-census-l1-target`), R=2, baseline = gamba **A** del controllo ±zval (stesso head senza leva). Predizioni: **str alloc/iter 3,00→2,00 ESATTO (= PARITÀ oracle 2,00), free analogo −1,00**; empty/arith/prop/calls/arr/re INVARIATE (preg_match è host-path, mod.rs:9694/15234) — tolleranza ±0,05, arr ±0,10. Fuori predizione ⇒ diagnosi, niente A/B.
5. A/B (`s125-ab.sh`, A = pin s124 c5ba2573): bersaglio str, R=5 alternato ABAB, scalati wp123, floor med3, parità output; giudice `s125-giudice.sh` (v3): promo = max(4,00; SOGLIA_LAYOUT_str **v2-s125** da `banda-out/layout-bande-v2.txt`; 2×spread_A; 4×quanto); guardie non-bersaglio SOLO-REGRESSIONE con SL v2-s125. La banda v2-s125 DEVE esistere prima del giudice (leva micro senza banda v2 = vietata).
6. Admission codegen: disasm `run_loop` prima/dopo (bl-count) a verbale + smoke R=2 early-stop a segno opposto.
7. Promozione ⇒ gate REGOLE §5-6 (batteria/corpus/fixture/micro, pin via `scripts/pin-phpr.sh`); registro nel verdetto del run PROMOSSO (vincolo rev. S-124 #3). Ogni cifra citabile solo dai verdetti `.out`.
