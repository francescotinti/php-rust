# S-116 criterio PRE — PROMOZIONE L-A: riesecuzione PER INTERO del criterio s115-criterio-la.md con le SOLE modifiche (a)-(e) (azioni revisore S-115); scritto e committato PRIMA di ogni script e misura

1. Oggetto: promozione L-A «trigramma prop». A = stash `phpr-s112` (f71abd2a = pin), B = `phpr-s114-la` CONSERVATO (052ea417, codice 2c18b2e). Binari CONSERVATI, ZERO rebuild per l'A/B: layout identico per costruzione, bande S-114/S-115 applicabili. Smoke R=2 (early-stop a segno opposto) prima del full.
2. (a) Banda micro = **max N=2** (s115-nulla2-verdetto.out): arith 0,40 · prop 4,33 · calls 5,50 · str 5,00 · arr 6,67 · **re 10,00**. Soglia promozione prop = max(4 ns/iter; spread_A depurato; banda(prop)=4,33) con segno + su TUTTE le coppie in famiglia (≥5); guardie non-bersaglio SOLO-REGRESSIONE: sfondano se D_mediano in famiglia < −max(2×spread_A depurato; banda(cat)).
3. (b) Held-out R=5 (poly·err·wploop): REGRESSIONE se net_cand > baseline S-112 (poly 9,59 · err 2,95 · wploop 6,76 s) + max(2×spread; 0,12 s; banda_heldout 0,20/0,01/0,06 — N=1 DICHIARATO), con **spread ≝ max(spread_pin, spread_cand)** ed ENTRAMBI gli spread PUBBLICATI nei raw e nel verbale.
4. (c) Tie ESATTI pre-registrati, confronto su valori ARROTONDATI a 2 decimali (la cifra del verbale, niente epsilon float): promozione prop PASS se D_mediano ≥ soglia (uguale ⇒ PASS); guardia sfonda SOLO se STRETTAMENTE < soglia (uguale ⇒ tiene); held-out REGRESSIONE solo se net_cand STRETTAMENTE > limite (uguale ⇒ ok).
5. (d) rc di OGNI gate scritto DALLO SCRIPT in FILE (`admission-out/rc`, `ab-out/full-rc`, `heldout-out/rc`), mai letto da echo/pipe; le righe di esito nel verbale `s116-la-verdetto.out` le APPENDE lo script a esito acquisito, mai il conduttore in anticipo.
6. (e) Resto INVARIATO da s115-criterio-la.md: famiglie 1,3×min per braccio con esclusione coppie per NOME + extra automatiche max +6 (p.2-3); <5 in famiglia ⇒ MISURA_INVALIDA, rerun categoria 1 volta poi R=7 (p.3); parità stdout+stderr A/B per categoria PRIMA del cronometro, una divergenza ⇒ STOP leva (p.5); admission hit+4miss con parità output, dump {main} identico, bigramma nel pin (p.6, casi di wp115-harness/admission-cases/); batteria RIUSATA 1742/0/2 di S-114 (binario byte-identico 052ea417 — deroga DICHIARATA).
7. Verdetto: PROMOZIONE solo se admission+micro+guardie+held-out reggono TUTTI. Se PROMOSSA: cherry-pick 2c18b2e (MAI checkout parziale) + pin SOLO via `scripts/pin-phpr.sh` §6 PIENO + micro R=5 + held-out sul pin nuovo. Altrimenti: esito dichiarato coi numeri, binari conservati restano, release non toccata.
8. Caveat dichiarati: banda_heldout N=1 e da zavorra NON paragonabile (+1.676 B in Op::Clone vs +3.176 B) — esito held-out entro ±banda dal limite = «dentro l'incertezza layout», dichiarato nel verbale; famiglia veloce ≈ P-core resta IPOTESI nominata.
9. Commit+push a ogni passo; raw gitignored in `wp116-harness/{ab-out,admission-out,heldout-out}/`; verbale `s116-la-verdetto.out` committato SOLO a esiti acquisiti.
