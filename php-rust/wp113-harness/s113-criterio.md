# S-113 criterio PRE — leva H-P1 «probe IC col ricevitore in prestito» (scritto e committato PRIMA di implementazione e misura; istruttoria: s113-istruttoria.md)

0. Precondizione: coppia WP S-113 dentro banda (s113-pair-criterio.out) — la leva parte solo a debito saldato.
1. Siti (2, per NOME): **P1a** = probe IC inline nel braccio `PropGetSlot`; **P1b** = idem in `PropGetSlotRecv` DOPO il push del recv (ordine di pila identico). Forma di `ThisPropGet` (hit in Option<Zval> sotto borrow); condizioni REPLICATE VERBATIM dall'hit di `prop_get_entry` (slot direttamente Object — Ref/Undef al funnel —, `ic.get(sk)`, `class_id+1==cid1`, `lazy.is_none()`, `get_slot` non-Undef, `deref_clone`); census note_prop_val(0) INVARIATO di posto; OGNI miss → `reg_load_slot` + `prop_get_entry` INVARIATI (warning Undef compreso).
2. Giudice primario: **prop** (N=30M emesso dal sorgente). Segno atteso: Δ=A−B POSITIVO (B più veloce). Nessuna magnitudine attesa nel verdetto (stime orientative FUORI). Held-out poly beneficiario possibile, NON vincolante.
3. Protocollo: A = stash `phpr-s112` (=pin f71abd2a, verificato), B = candidato CONSERVATO (`cp` → `phpr-s113-ab-candidate`); smoke R=2 con early-stop se 2/2 a segno opposto e |Δ|>max(rumore; 0,67); pieno R=5 ABAB interleaved, user CPU al netto del pavimento PER-binario (empty.php), mediana.
4. Soglia promozione (prop): Δ mediano ≥ max(4 ns/iter; spread lato A; banda-layout 0,67) E segno concorde 5/5.
5. Guardie non-bersagli (**arith, calls, str, arr, re**, stesso protocollo R=5, stesura a SOLO-REGRESSIONE come da REGOLE §3 rev. S-112): sfonda SOLO Δ mediano < −max(2×spread lato A della categoria; 0,67 ns/iter); una guardia sfondata ⇒ NIENTE promozione (il verdetto la dichiara).
6. Held-out (soglia PRE-registrata): a leva conclusa R=5 su pin e candidato; REGRESSIONE = phpr netto candidato > baseline S-112 (poly 9,59 · err 2,95 · wploop 6,76 s) + max(2×spread della lettura corrente; 0,12 s); UNA regressione ⇒ NIENTE promozione. Miglioramenti ammessi e registrati.
7. Admission (bipartita, PRIMA dello smoke): batteria `cargo test --release` rc=0 DAL COMANDO; emissione INVARIATA — la leva non tocca il pass: dump ON {main} ×6 micro E dump OFF interi ×6 byte-identici al pin (metodo s112-admission.sh); taglia+bl-count di `run_loop` prima/dopo a verbale.
8. Verdetto: PROMOZIONE solo se 4+5+6+7 reggono TUTTI; pin S-113 nato SOLO da `scripts/pin-phpr.sh` + gate REGOLE §5 (batteria, corpus 1415 per NOME ×2, fixture bilaterali, micro R=5) in **ordine §6 PIENO: batteria→re-hash→stash sullo STESSO binario** (az.5 rev. S-112; ogni deroga dichiarata come tale). Altrimenti «direzione firmata, magnitudine non stabilita» o REFUTATA, revert AL BYTE.
9. Commit+push a ogni passo; output di run FUORI dal repo (`wp113-harness/ab-out/`, gitignored); rc dei gate MAI da pipe.
