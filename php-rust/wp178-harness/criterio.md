# S-178 — indagine ORM, criterio prima di build/run
1. Oggetto: attribuire le scritture di prop_set_entry a classe ricevente e file PHP chiamante; incrocio private×readonly esclusivo; ripetizioni per sito IC e per oggetto/proprietà. Nessuna ottimizzazione o promozione.
2. Sonda separata generata da HEAD a1eb40e8, Rust stable=1.98.1, release fat-LTO/1 CGU, SOURCE_DATE_EPOCH=0, CARGO_INCREMENTAL=0, target APFS dedicata; pin S-175 solo riferimento di parità, mai confronto compilatore→leva.
3. Count: entrate dopo lazy forwarding riuscito, init/hit/miss separati; private = key mangled effettiva, readonly = ro_decl; Weak identifica generazioni di oggetti senza prolungarne la vita. Ripetizione significa precedente tentativo osservato in questo handler (non tutte le scritture PHP).
4. Validazione: fixture private mutable/readonly, pubblico, ereditarietà, rewrite stesso oggetto, stesso sito su nuovi oggetti; stdout+rc contro oracle e pin; ORM 3484 test e fail-set 16 nomi esatto. Controlli negativi non devono diventare private o readonly.
5. Tempo: solo dopo count e senza build/census concorrenti, gate E2 <150% ×4 a 30s, Data≥10GB, watchdog, lock esclusivo proprio. CPU occupata ⇒ nessun costo valido né percentuale di beneficio; non fermare processi estranei.
6. Strumento costo: campioni temporizzati 1/1024 delle entrate, inclusivi del corpo sincrono (non del successivo hook PHP); no contatori per-op. Confrontare sonda spenta/count/time e frequenze 1/512,1/2048, floor timer su controllo vuoto; scartare se perturbazione o instabilità >5%. Tempi diagnostici non sono un A/B di ottimizzazione.
7. Per una cifra di beneficio servono A/B reale sulla suite (R=5 ABAB, floor per binario, fail-set identico, rumore e controllo nullo), non conteggi×costo micro. Qui raccomandazione condizionata e tetto di opportunità, mai speedup inventato.
8. Output esterno /private/tmp/phpr-s178-investigation; script nuovi, non copie dei vecchi lanciatori con path canonici. Preservare raw e hash; revisore adversariale finale ≤400 parole, lente processo.

Emenda prima del census (risposta esplicita utente 20/09): lasciare i quattro processi yes attivi e consegnare census + limite sui tempi. Fase temporale NON eseguita; nessun costo o beneficio percentuale sarà inferito. site_repeat è condizionato a sito×classe×file×percorso×categoria, non sito globale. private/readonly=-1 significa non classificato; ripetizione oggetto usa key effettiva dopo forwarding lazy.
Le righe hook/magic/denied non risolte non aggiornano lo storico oggetto/slot; private/readonly restano -1. I tentativi falliti prima del ritorno di lazy_prop_access sono fuori denominatore.
