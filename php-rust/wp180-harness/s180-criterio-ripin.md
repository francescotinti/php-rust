# S-180 — criterio PRE-registrato: ri-pin s180 sotto toolchain 1.98.1 (nessuna leva)

1. **Oggetto**: nuovo pin phpr+server dallo STESSO sorgente del pin s175 (+ flag gc-idle S-176 spento di default), L-CM1 revertata al byte (b3e48919), ricetta emendata `rust-toolchain.toml` 1.96.0→1.98.1 (4a6c78cf, richiesta utente 2026-09-20). Non è una leva: **leve spedite S-180 = 0**, anomalia dichiarata (sessione di ratifica+apparato dopo le due sessioni diagnostiche S-178/S-179 di ChatGPT Astra 6).
2. **Perché serve**: da qui ogni braccio si compila con 1.98.1; un A/B braccio-vs-pin s175 sarebbe un confronto fra toolchain, non fra leve (REGOLE §3: mai cifre da contrasti tra binari diversi). Il pin s180 è il nuovo riferimento same-toolchain.
3. **Gate** (REGOLE §5/§6, catena `s180-promozione.sh`, copia dichiarata di S-177): build ricetta → hash → batteria `cargo test --release` rc dal comando (inventario nomi ≥ baseline s125, `debug_backtrace_array_fields` verde, **`loc_dente` verde con i cap ALZATI E DICHIARATI per il flag gc-idle S-176: host.rs +1, mod.rs +59, run.rs +11**) → build2 re-hash al byte → `pin-phpr.sh s180` → corpus-gate (1412 nomi, golden, off↔on: **zero flip attesi**) → fixture chain (10 gate) → 19 fixture bilaterali/invarianti → quiescenza → micro R=5 → conferma prop-dq → ORM per nome (16) → http-kernel 0E/0F → `pin-server.sh s180`.
4. **Attese numeriche**: nessuna cifra. Micro s180 = nuovo scoreboard di riferimento; il confronto con s175 (arith 2,4 · prop 2,6 · calls 4,6 · str 4,1 · arr 3,0 · re 2,5) è **sola direzione** (toolchain): qualunque Δ è «attribuito alla toolchain, non ripartito». Conferma prop-dq pin s180 vs stash s175: lettura, nessuna soglia.
5. **Igiene**: lock `s180` presente per tutta la catena (CI in attesa); guardia disco Data ≥3G prima di build/batteria/corpus (apertura: Data 12G, swap 11,4/12G — dichiarato); CPU utente 1 % all'apertura; nessun altro cargo/misura in finestra.
6. **Esiti**: rc=0 ⇒ pin s180 registrato, tree == pin, NEXT/PIN_REGISTRY/PERF_MAP aggiornati; qualunque `stop` ⇒ pin resta s175, causa a verbale, niente stash (REGOLE §2).
7. **Coppia WP/ORM**: dovuta a ogni pin nuovo (utente 2026-08-12). Si esegue DOPO la catena se la finestra regge (E2 calma CPU); altrimenti dichiarata in coda a S-181 con i copioni t21/ORM E3/E4 già pronti (s177-pair.sh → copie a token s180).
