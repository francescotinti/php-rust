# S-114 criterio PRE — A/B LEVA-NULLA di attribuzione layout (scritto e committato PRIMA del codice e della misura; prerequisito promosso da S-113, az.3 revisione)

1. Oggetto: NESSUNA semantica — testo INERTE in `run_loop` (~+2 KB, cold-branch MAI preso: guardia su env `PHPR_S114_BALLAST` mai settata, collocato in un braccio d'opcode ASSENTE dai `{main}` dei sei micro). Scopo: MISURARE la banda-layout; il candidato si scarta a misura conclusa, nessuna promozione possibile per costruzione.
2. Attesa: Δ vero = 0 su TUTTE le categorie; ogni Δ osservato = layout+rumore. Nessun segno atteso, nessuna soglia di promozione: le uscite SONO la banda.
3. Protocollo: A = stash `phpr-s112` (=pin f71abd2a, verificato al pre-flight), B = candidato CONSERVATO (`cp` → `phpr-s114-nulla`: l'A/B non dipende dal churn di release); R=5 ABAB interleaved sulle SEI categorie, user CPU al netto del pavimento PER-binario (empty.php), N emesso dal sorgente (arr: 6M come S-113). Niente smoke/early-stop: non c'è segno da refutare.
4. Uscite A MACCHINA dallo script (az.2 rev. S-113, niente derivazioni a mano): per categoria Δ mediano, conteggio segni, spread_A, spread_B; riepilogo finale con banda per categoria e banda globale, tutto emesso da `s114-ab-nulla.sh`.
5. BANDA-LAYOUT pre-registrata: banda(cat) = |Δ mediano| della leva-nulla su cat; banda globale = max sulle 6. Uso da S-114 in poi: soglia promozione = max(4 ns/iter; spread lato A; banda(cat)); soglia guardia solo-regressione = −max(2×spread_A(cat); banda(cat)). Sostituisce lo 0,67 «raccontato».
6. Admission (PRIMA della misura, metodo s113-admission): dump ON `{main}` ×6 IDENTICI al pin E dump OFF interi ×6 byte-identici; taglia+bl/br di `run_loop` prima/dopo a verbale (target ≈ +2 KB, come l'ordine di grandezza di H-P1 +1.976 B). Batteria NON richiesta su questo candidato: nessuna promozione e nessuna semantica toccata — deroga DICHIARATA qui.
7. Limite dichiarato: UNA perturbazione = UN campione di layout (N=1 misurato); la banda si irrobustisce con varianti future, ma N=1 MISURATO sostituisce N=2 raccontati (+1,50 S-112, −5,50 S-113).
8. Chiusura: revert AL BYTE (release ricostruita = f71abd2a, diff crates/ vuoto) PRIMA di aprire la leva L-A; la banda misurata entra nel criterio PRE di L-A.
9. Commit+push a ogni passo; output di run in `wp114-harness/ab-out/` e `admission-out/` (gitignored); rc di gate MAI da pipe.
