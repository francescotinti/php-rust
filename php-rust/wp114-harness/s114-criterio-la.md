# S-114 criterio PRE — leva L-A «trigramma prop in prestito» (scritto e committato PRIMA di implementazione e misura; istruttoria: s114-istruttoria-la.md; banda: s114-nulla-verdetto.out)

1. Sito UNICO: peephole runtime nel braccio `PropGetSlotRecv` (run.rs:4168) su `ops[ip+1]==BinaryTCPropSetPop`; probe bipartito SOLO-borrow con condizioni get/set REPLICATE VERBATIM (istruttoria); double-hit ⇒ yv borrow + binary_fast + `write_property_at`(stessa funzione) + gc_note + `ip+2`; OGNI miss ⇒ sequenza storica VERBATIM da zero (nessuno stato toccato prima del commit). Emissione/pass NON toccati; fusione compilata FUORI sotto zval-census/op-census (dichiarato).
2. Giudice primario: **prop** (N emesso dal sorgente). Segno atteso: Δ=A−B POSITIVO 5/5. Nessuna magnitudine attesa nel verdetto.
3. Soglia promozione (prop): Δ mediano ≥ **max(4 ns/iter; spread_A corrente; banda(prop)=4,33 MISURATA S-114)** E segno concorde 5/5.
4. Guardie non-bersaglio (arith, calls, str, arr, re) SOLO-REGRESSIONE: sfonda solo Δ mediano < **−max(2×spread_A(cat); banda(cat) S-114 = 0,40/5,50/5,00/6,67/0,00)**; una sfondata ⇒ niente promozione.
5. Held-out (soglia PRE-registrata): R=5 pin e candidato a leva conclusa; REGRESSIONE = netto candidato > baseline S-112 (poly 9,59 · err 2,95 · wploop 6,76 s) + max(2×spread corrente; 0,12 s); UNA regressione ⇒ niente promozione.
6. Protocollo: A = stash `phpr-s112` (=f71abd2a), B = candidato CONSERVATO (`cp` → `phpr-s114-la`); smoke R=2 early-stop se 2/2 segno opposto e |Δ|>max(rumore; banda(prop)); pieno R=5 ABAB interleaved, user CPU al netto del pavimento PER-binario; mediane, spread e PASS/FAIL delle soglie EMESSI A MACCHINA da `s114-ab-la.sh` (az.2 rev. S-113).
7. Admission bipartita PRIMA dello smoke: batteria `cargo test --release` rc=0 DAL COMANDO con ELENCO test nei raw (serve anche all'attribuzione per NOME del delta 1741/1742, az.4 rev. S-113); dump ON `{main}` ×6 e OFF interi ×6 IDENTICI al pin; taglia+bl/br di run_loop a verbale.
8. Verdetto: PROMOZIONE solo se 3+4+5+7 reggono TUTTI; pin S-114 SOLO da `scripts/pin-phpr.sh` in ordine §6 PIENO (build→hash→batteria→re-hash→stash→corpus 1415 per NOME ×2→fixture bilaterali→micro R=5). Altrimenti «direzione firmata, magnitudine non stabilita» o REFUTATA, revert AL BYTE verificato.
9. Caveat dichiarato: banda(prop) 4,33 viene da N=1 perturbazione (limite del punto 7 del criterio-nulla); un esito in [soglia; soglia+banda] si dichiara «dentro l'incertezza layout» anche se promosso.
10. Commit+push a ogni passo; output di run in `wp114-harness/ab-out/`, `admission-out/`, `batteria-out/` (gitignored); rc di gate MAI da pipe.
