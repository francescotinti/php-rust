# Criterio S-129 — L-OL1 seg.3: MODELLO DEL TEMPO del costo per-statement Field* (PRIMA di ogni forma)

1. **Oggetto**: apportare in passi il Δins = objdatains−objalloc = **320,0 ns/iter** phpr (oracle 36,6; rif. s127-submicro run pulito) e spiegare: i ~2 alloc/statement residui, overwrite(+4) vs secondo insert(+3). Nessuna forma nominata prima della lettura s129-modello-tempo.md.
2. **Ipotesi H-cloni** (dal SORGENTE, siti nominati): i 2 residui = `n.clone()` (Box del nome) in `byref_hook_root` (vm/mod.rs:14333) e `field_lazy_root` (vm/mod.rs:12876), pagati da OGNI statement Field* con primo passo `Prop`. Corollario già dedotto: overwrite +4 = keys(1)+cloni(2)+`dropped.push`(1, primo push su Vec::new); secondo insert +3 = keys(1)+cloni(2)+push(0, entro capacità 4 del primo push). **Arbitro census** variante «name-borrow» (patch probe sul census target, tree ripristinato, parità stdout vs pin, R=2 identici): predizioni ESATTE alloc/iter = pin −2·(statement **FieldAssign**/iter — EMENDA pre-run, commit successivo al criterio: la mono-passo `$this->p=v` compila a `Op::PropSet` (assign.rs:462-465), quindi i ctor NON pagano i cloni; la premessa iniziale «ogni statement Prop-rooted» era errata e viene corretta PRIMA di ogni esecuzione): objalloc 7→**7** (INVARIATO) · objallocni 5→**5** · objdatains 12→**10** · objdropdef 8→**8** · objchurn 13→**11** · objmap 1→**1** · p2 11→**9** · p3-local 10→**10** · p4-int 12→**10** · p5-two 15→**11** · p6-overwrite 16→**12**. Qualsiasi scarto ⇒ H rigettata sul punto, indagine PRIMA di procedere.
3. **Sonde temporali PIN (bilaterali)**: metodo s127-submicro (user CPU, pavimento med-R per-binario, R=5 alternato, N=3e6 dal sorgente, parità stdout; pin s127b ccb63dca e oracle brew): categorie {objalloc, objdatains, p2-append, p4-int, p5-two, p6-overwrite, p3-local}. Derivate del modello PER MOTORE: Δins=datains−alloc · Δ2nd=p5−datains · Δover=p6−datains · Δapp=p2−alloc · Δloc=p3−alloc. Risoluzione dichiarata = max(4 ns/iter, spread del run); sotto risoluzione = «non risolto», nessun claim.
4. **Sonde per-passo** (build EMENDATA time-probes, patch `wp129-harness/time-probes.patch`, target separato `/Volumes/Extreme Pro/Claude/phpr-time-l1-target`, tree ripristinato e verificato, parità stdout vs pin per ogni micro): segmenti nell'arm `Op::FieldAssign`: TOT · A=pop+pop_field_keys · B=byref_hook_root · C=reject_indirect_hook · D=field_lazy_root · E=field_set · E2=walk interno (chiamata a `field_write_walk` in `field_write_prop_step`) · CAL=coppia di letture back-to-back. Contatore `cntvct_el0` (mrs, senza isb — dichiarato), accumulatori + conteggi, dump atexit su `PHPR_TIME_PROBES`; CAL SOTTRATTA per segmento; R=3, mediana + spread dichiarati. **Il per-statement puro si legge per DIFFERENZA tra micro sulla STESSA build** (datains−alloc = insert; p6−datains = overwrite; p5−datains = 2° insert; p2−alloc = append). Le quote sono MODELLO: build emendata ⇒ nessuna cifra promuovibile, il confronto col pin è solo ordine/direzione (REGOLE §3).
5. **Chiusura del modello**: A+B+C+D+E entro ±15% di TOT (E2⊂E); scarto maggiore ⇒ modello NON chiuso, dichiararlo. Conteggi per segmento = statement/iter ESATTI (deterministici) — scarto ⇒ indagine.
6. **Disasm mirato** (bl-count, ricetta s128-disasm) sul segmento dominante emerso, sul PIN.
7. **Esito**: `s129-modello-tempo.md` ranka le forme candidate (name-borrow / prelude-fusion / resolve-once in prop_step / insert-path) per upper-bound temporale dal modello; la forma per l'A/B (questa sessione se il margine è netto, REGOLE-ritmo) si nomina SOLO da modello chiuso, col SUO criterio A/B ri-committato PRIMA del codice (vincoli: giudice objdatains N=3e6, R=5 ABAB netto-pavimento, segno CALA, soglia max(4 ns/iter, rumore R=5, banda-layout prop), smoke R=2 early-stop, guardie SOLO-REGRESSIONE sei micro + objalloc/objchurn/objmap con bande «default 4 ns»+spread, disasm prima/dopo).
8. **Igiene**: quiescenza = GATE SEPARATO prima di ogni run di misura (script proprio: pgrep phpr|php-server|cargo|rustc vuoto; rust-analyzer e mediaanalysisd sotto il 5% CPU su campione 5 s; nessuna run detached); niente build né LSP in finestra di misura; rc di ogni script = SOLO il file rc proprio, nominato nel verdetto; verdetti avversi committati PRIMA di ogni emenda; output di run NON tracciati (.gitignore).
9. **Ordine**: census identità (p.2) → sonde pin (p.3) → per-passo (p.4) → disasm (p.6) → lettura (p.7). Le run sono corte (≤~30 s ciascuna): sequenziali in foreground, MAI concorrenti.
