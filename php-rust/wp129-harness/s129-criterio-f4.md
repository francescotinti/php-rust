# Criterio S-129 — L-OL1-F4 «prelude-gate» (istanzia p.7 del criterio-tempo; commit PRIMA del codice della forma)

1. **Forma** (dal modello CHIUSO s129-modello-tempo.md): nell'arm `Op::FieldAssign` SOLO, un gate `field_prelude_skip(base, top, steps)` salta il trio `byref_hook_root` + `reject_indirect_hook` + `field_lazy_root` quando: primo passo `Prop` ∧ slot base = `Zval::Object` DIRETTO (un `Ref` va in via lenta) ∧ `try_borrow` ok ∧ `lazy` None ∧ `proxy_instance` None ∧ `!classes[cid].has_prop_hooks`. Ogni condizione violata ⇒ via lenta INVARIATA. Correttezza per costruzione sotto il gate: byref → `Ok(None)` (prop_hook corto-circuita sul flag; `lazy_prop_access` fast-path senza effetti; con hook_guarded attivo l'esito è comunque None), indirect_hook_target → None (prop_hook×2), field_lazy_root → `Ok(None)` (`is_lazy_value` false). **UB modello 73 ns/iter** (B 36 + C 7,5 + D 29,6); costo sostitutivo NOMINATO = 1 match + 1 slot read + 1 try_borrow + 2 flag (lezione F2: non è alloc-removal cieco, il sostituto è ~2 ordini sotto l'UB).
2. **Census arbitro PRIMA dell'A/B** (ricetta s129-census fase B: clite.patch sull'albero CON F4 committata, target census, R=2 identici, parità stdout): predizioni ESATTE = colonna B di s129-census-verdetto.out: objalloc 7 · objallocni 5 · objdatains **10** · objdropdef 8 · objmap 1 · objchurn **11** · p2 **9** · p3 10 · p4 **10** · p5 **11** · p6 **12**. Scarto ⇒ STOP e indagine.
3. **A/B**: arbitro `s129-ab.sh` (copia dichiarata di s128-ab.sh, stesse SL committate): A = stash pin s127b, B = build canonica della forma committata (hash a verbale); giudice objdatains N=3e6, coppie ALTERNATE, user CPU netto floor med3 per-binario; segno: B CALA; soglia = max(4 ns/iter, rumoreB, SL prop 0,80); **smoke R=2 early-stop a segno opposto** (verdetto avverso committato PRIMA di ogni emenda) → conferma R=5.
4. **Guardie SOLO-REGRESSIONE**: sei storiche (SL committate) + objalloc/objchurn/objmap («default 4 ns» dichiarato + spread).
5. **Disasm prima/dopo** (pattern s129-disasm) a verbale; churn di build dichiarato.
6. **Promozione** SOLO se giudice sopra soglia e guardie ok (ricetta s127b-promozione): batteria (rc da comando, inventario nomi==pin) → build ricetta re-hash → pin scripts/pin-phpr.sh s129 → corpus-gate CANONICO (1414 per NOME ×2 modi) → fixture chain → micro R=5 → ORM 16 nomi == baseline → hk 0E/0F → pin-server (ricontrollo hash server DOPO ogni build canonica). Esito avverso dell'A/B ⇒ revert e rebuild canonico che DEVE riprodurre ccb63dca al byte.
7. **Igiene**: quiescenza gate separato prima di census e di ogni A/B; rc autoritativo = SOLO il file rc dello script; run sequenziali; niente edit coi build in volo.
